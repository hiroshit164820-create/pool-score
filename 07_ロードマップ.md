# 07 ロードマップ

## Phase 1.0 — 最小実戦投入 ✅ 完了（2026-08-19）

**目標**: 9ボールを1試合、ラック単位で記録して履歴に残せる。

| # | 項目 | 状態 |
|---|---|---|
| 1 | `data/` のルール定義（5種目分） | ✅ |
| 2 | `js/engine.js`（イベントログ＋リデューサ） | ✅ |
| 3 | `js/store.js`（localStorage・索引と本体を分離） | ✅ |
| 4 | 試合作成画面 | ✅ |
| 5 | 試合中画面（**スコアをタップで加算**・undo・訂正） | ✅ |
| 6 | 履歴一覧 | ✅ |
| 7 | **ブレイク方式の選択＋ブレイク権表示** | ✅ |
| 8 | **ショットクロック** | ✅ |
| 9 | JSON書き出し・読み込み（記録消失の保険として前倒し） | ✅ |
| 10 | 検証3種（169項目） | ✅ |

**次にやること**: 実際の台で1試合記録する。片手で押せるか、立ったまま画面が読めるかを確認し、出た問題を潰してからPhase1.1へ進む。

---

## Phase 1.1 — 種目の水平展開

10ボール・8ボールは既に `data/` に入っているため、実質は動作確認とダブルスの詰め。

- [ ] 各種目で1試合ずつ記録して破綻がないか確認
- [ ] ダブルスの名前表示（現在は「山田・佐藤」と連結表示）が実用に耐えるか確認

**検証ゲート**: `engine.js` を1行も触らずに種目が増えたか。触ったら抽象化が失敗しているので設計に戻る。

---

## Phase 1.2 — ボール得点型（ローテーション）★設計の山場

- [ ] ボールグリッド画面（15球をタップして加点）
- [ ] `RACK_START` の continuation 処理
- [ ] `rackEndsScoring: false` が効いているか確認

ローテーションが素直に入れば、JPA/JCLも同じ経路で入る。
エンジン側の `ballScore` 分岐は実装済みで、テストも通っている。

---

## Phase 1.3 — ハンデ機構の拡張

- [ ] ボールハンデのUI（現在エンジンは対応済み、UIが未実装）
- [ ] プリセット（9番のみ／7番以上／5番以上）

---

## Phase 1.4 — イニング単位モードとJPA

- [ ] イニング単位の入力画面
- [ ] `DEAD_BALLS` / `TIMEOUT` / イニングカウントのUI
- [ ] JPA 9ボール・9ボールダブルス・8ボール
- [ ] チームポイントの表示

---

## Phase 1.5 — JCLとカイルン

- [ ] JCL 9ボール・8ボール（目標点は手入力）
- [ ] カイルンの3ステップUI（未確認2点はハウス設定の選択式）

---

## Phase 1.6 — スタッツ画面

- [ ] 勝率・対戦相手別成績・マスワリ率など
- [ ] **「この種目では算出できない指標」をグレーで理由付きで表示する**
      （例: 10ボールのセーフティ成功率は、2026年6月改訂でセーフティコールが廃止されたため記録自体が取れない）

---

## Phase 1.7 — 仕上げ

- [ ] GitHub Pages 公開（スマホから使うために必須）
- [ ] 誤タップ・長時間利用の実地確認

---

## Phase 2 — クラウド共有

- API は `POST /matches/:id/events`（追記のみ）と `GET /matches?since=` の2本で足りる
- 認証は最小（合言葉 or マジックリンク）
- `syncState` を `pending` → `synced` に遷移させるだけ

**Phase1のデータ構造を変えずに移行できることが Phase1 の設計制約**。
そのため以下は最初から入れてある: クライアント生成ID／追記onlyのイベント配列／`syncState`・`updatedAt`・`deletedAt`／localStorageキー名を将来のテーブル名に合わせる。

---

## あえてやらないこと

| やらない | 理由 |
|---|---|
| フレームワーク導入 | ビルド不要が第一選択。この規模なら素のDOM操作で足りる |
| redo | 台の脇で押す場面がない |
| IndexedDB | localStorage 5MB で数千試合まで持つ（1試合あたり数KB） |
| リアルタイム同期 | Phase2でも「試合終了後にアップロード」で足りる |
| **ファーゴレート換算の実装** | 式が非公開。推測実装は誤りを生むだけ |
