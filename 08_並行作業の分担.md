# 並行作業の分担ルール

本人の指示（2026-08-22）で、**触るファイルが重ならない作業は同時に進める**ことにした。
同じファイルを2人以上が同時に書くと、あとから書いたほうが前のぶんを消す。
それを防ぐための取り決めをここに置く。

---

## 1. なぜ要るか

このアプリは1つのフォルダに全部入っている素のHTML/CSS/JSで、ビルドも分割も無い。
`index.html` と `css/v2.css` は**どの機能からも触りたくなる場所**なので、
何も決めずに並行すると必ずぶつかる。

過去にも同種の事故が起きている（`js/app.js` の冒頭コメント参照。
別セッションが同時に `index.html` と `style.css` を編集していたため、
CSSの読み込みを app.js 側から足して衝突を避けた）。

---

## 2. 担当の区分け（受け持ちファイル）

| 担当 | 書いてよいファイル | 読むだけ |
|---|---|---|
| **盤面・レイアウト係** | `js/ui_match.js` `js/ui_layout.js` `js/ui_sheet.js` `style.css` | それ以外 |
| **共有・QR係** | `js/share.js` `js/qr.js` `js/ui_import.js` | それ以外 |
| **記録・容量係** | `js/store.js` `js/engine.js` `js/csv.js` | それ以外 |
| **一覧・成績係** | `js/ui_history.js` `js/ui_home.js` `js/ui_players.js` | それ以外 |
| **検証係** | `_test/**` | `js/**` `css/**` |

**どの係も `data/**` は書き換えない**（競技規程の出典が付いた値なので、
変えるときは 04_種目ルール仕様.md の根拠とセットで、単独の作業として行う）。

---

## 3. 共有資源（取り合いになる3つ）

次の3つは**どの係も勝手に書かない**。まとめ役（親）だけが書く。

| ファイル | 理由 | やり方 |
|---|---|---|
| `index.html` | 画面の骨組み。全員が足したくなる | 係は「どこに何のIDの箱が要るか」を**報告する**。親がまとめて入れる |
| `css/v2.css` | 追記が末尾に集中してぶつかる | 同上。ただし**節が完全に分かれている**ときは、係ごとに節を決めて末尾追記してよい |
| `sw.js` | `CACHE_VERSION` を全員が上げようとする | 公開の直前に**親が1回だけ**上げる |

`_test/tests.tsv`（検証の一覧）も末尾追記が重なるので、**親がまとめて足す**。

---

## 4. 係に渡すときの決まり文句

サブエージェントに任せるときは、指示に必ずこの3行を入れる。

```
・新規ファイル以外は、担当表にある自分の受け持ちファイルだけを変更する
・index.html / css/v2.css / sw.js / _test/tests.tsv は変更しない（必要な箱は報告する）
・git commit と git push はしない
```

そして**最後に必ず「実際に実行した検証の結果」を報告させる**
（項目数と終了コード。通るまで直させる）。

---

## 5. 検証の流し方

一覧は `_test/tests.tsv` の1か所。`_test/run.py` が読んで並列で流す。

```
python _test/run.py                 全部（実測 約12分）
python _test/run.py --only layout   名前に layout を含むものだけ（数十秒）
python _test/run.py --only paste share
python _test/run.py --jobs 4        同時に走らせる本数を変える
python _test/run.py --serial        1本ずつ（並列で結果が変わるか確かめる用）
```

- **手直しの最中は `--only` で絞る**
- **公開の直前だけ全数**

1本ずつ順番に流していた頃は実測で約32分かかっていた（2026-08-22 04:38→05:10）。
並列にして12.3分になった（同 06:0x、51本 NG 0）。

いちばん重いのは `overlap_test.py`（502秒・種目11×時計3×向き2の総当たり）。
ここを更に速くするなら、このテストの中を分割して並列にする必要がある。
