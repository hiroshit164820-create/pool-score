# 引き継ぎ

---

## 2026-08-19（23:50時点）

### 1. 現在の目標

ビリヤードのスコア記録アプリを作る。このセッションでは Phase 1.0 の実装からスマホ公開までを行い、以下を追加した。

- 競技種目: 9/9ダブルス/10/10ダブルス/8ボール、14-1、JPA 9ボール・9ボールダブルス・8ボール（計9種目）
- スコア欄をタップして加算する操作方式
- ブレイク方式の選択（ウィナーズ／オルタネート）とブレイク権の常時表示
- 時計3択（使わない／ショットクロック／チェスクロック）とターン交代ボタン
- プレーヤー登録とプレーヤー別成績
- 明るくポップな配色への変更、スマホ公開（GitHub Pages・PWA）

**次のセッションの目標（本人の指示・2026-08-19）**:
**アイコン・ボタン・サイトデザイン全体をスーパーマリオの世界観で作り直す。**

---

### 2. 変更点（実際に変更したもの）

**新規作成したプロジェクト**: `D:\Claudecode\pool-score\`

| 種別 | ファイル |
|---|---|
| ルール定義データ | `data/rules_data.js` `data/scoring_data.js` `data/games_data.js` `data/handicap_data.js` |
| ロジック | `js/engine.js` `js/shotclock.js` `js/chessclock.js` `js/store.js` |
| 画面 | `index.html` `style.css` `js/ui_setup.js` `js/ui_match.js` `js/ui_history.js` `js/ui_players.js` `js/app.js` |
| PWA | `manifest.json` `sw.js` `icon-192.png` `icon-512.png` `icon-512-maskable.png` `apple-touch-icon.png` |
| 検証 | `_test/` 配下6本（下記) |
| 文書 | `README.md` `04_種目ルール仕様.md` `07_ロードマップ.md` `HANDOFF.md`(本ファイル) |
| 起動 | `起動する.bat` |

**外部の状態を変更したもの（重要）**

1. **GitHubリポジトリを新規作成し、公開状態にした**
   - `https://github.com/hiroshit164820-create/pool-score`
   - 当初は非公開で作成したが、**非公開リポジトリ＋GitHub Pagesは有料プラン(Pro)が必要**と判明したため、本人確認のうえ公開に変更した
   - 公開前に個人情報の混入がないことを確認し、コミット作者を noreply アドレス（`304265170+hiroshit164820-create@users.noreply.github.com`）に変更した
2. **GitHub Pages を有効化した**
   - `https://hiroshit164820-create.github.io/pool-score/`
3. **ローカルのgit設定を1件変更した**
   - `git config --global --add safe.directory D:/Claudecode/pool-score`（D:ドライブの所有権判定を通すため）

---

### 3. 検証済みの証拠

すべて **2026-08-19 23:44〜23:47 に実行** した結果。

| 検証 | 結果 | 内容 |
|---|---|---|
| `node _test/engine.test.js` | **87項目 成功/0失敗** | スコア計算。JPA1ラック=10点、ローテのラック跨ぎ得点、14-1の減点、スリーファール、訂正の整合性 |
| `node _test/shotclock.test.js` | **39項目 成功/0失敗** | ショットクロック。仮想時計使用 |
| `node _test/chessclock.test.js` | **35項目 成功/0失敗** | チェスクロック。ターン交代・秒読み・時間切れ |
| `python _test/browser_test.py` | **72項目 成功/0失敗** | 実ブラウザ通し操作 |
| `python _test/features_test.py` | **42項目 成功/0失敗** | JPA・3先・プレーヤー登録・成績・チェスクロック |
| `python _test/live_test.py` | **19項目 成功/0失敗** | **公開URLの実物**をiPhone相当画面で確認 |

**公開状態（23:44時点の観測）**
- GitHub Pages API: `status=built` / `commit=8a821f8` / `updated=2026-08-19T14:39:09Z`
- 公開URLへのHTTPリクエスト: **HTTP 200**
- git: 作業ツリーはクリーン。ローカルHEADとorigin/mainは共に `8a821f8` で一致

**ルールの数値について（一次情報で確認済み）**
- NBA「ポケットビリヤード競技規定」**2026年6月改訂版**の全33ページ本文をPDFから抽出して条文確認
- JPA 9ボールのSL別持ち点は、JPA公式PDFとAPA公式資料の**2ソースで一致**を確認
- JPAチームポイント表は全SL×全得点帯729通りで合計20になることをテストで検証

---

### 4. 一次情報の取り直しコマンド

再開時はまずこれを実行すること。

```bash
cd D:\Claudecode\pool-score

# 検証をまとめて実行（5本・245項目）
_test\run_all.bat

# 公開URLの実物が動いているか（上とは別。必ず実行すること）
python _test\live_test.py

# 公開状態の確認
gh api repos/hiroshit164820-create/pool-score/pages/builds/latest --jq "{status:.status, commit:(.commit[0:7])}"
curl -s -o /dev/null -w "%{http_code}\n" -L "https://hiroshit164820-create.github.io/pool-score/"

# gitの同期状態
git log --oneline -3 && git status --short
```

**注意**: 「テストが通った」ことと「公開URLで動いている」ことは別。`live_test.py` は公開URLの実物を見るので、デプロイ後は必ず実行する。

---

### 5. 未着手の範囲

**実装していない競技種目**（データ定義とエンジンは実装済み・テスト済みだが、**UIに出していない**）
- ローテーション（`rotation`）— ボールグリッド画面が未実装
- カイルン（`kailun`）— 3ステップUIが未実装。未確認事項2点はハウス設定の選択式にする方針
- JCL 9ボール／8ボール（`jcl_9ball` `jcl_8ball`）— 目標点は手入力にする方針

**実装していない機能**
- イニング単位の詳細入力モード（JPA公式スコアシート相当）。現在は全種目ラック単位（14-1のみ球単位）
- ボールハンデのUI（**エンジンは対応済み・テスト済み**、UIだけ未実装）
- JPAのチームポイント表示（`buildResult` で算出はしているが画面に出していない）
- Phase2のクラウド共有

**やったように見えるが未検証のもの**
- **実機のスマホでの動作確認は一度もしていない。** 確認したのは Playwright の Chromium（iPhone相当のUA・画面サイズ）のみ。実機のSafari/Chromeでの表示崩れ、ホーム画面追加、オフライン動作は**未確認**
- **実際のビリヤード台の脇で使っていない。** 片手で押せるか、立ったまま数字が読めるかは実地で未検証

---

### 6. 不確実な点・リスク

**テストが稀に落ちる（1回観測）**
- 23:44に `browser_test.py`（「取り消しでBのスコアが戻る」「スコアが出る」）と `features_test.py`（「Aの持ち時間が10:00」→実際は9:59）が同時に失敗した
- **直後に各3回ずつ再実行したところ、いずれも全項目成功した**（23:45〜23:47）
- 意図的に2本を並列実行して再現を試みたが、**再現しなかった**
- タイミング依存のゆらぎと考えられるが、**原因は特定できていない**。特に「9:59」は待ち時間の判定が厳しすぎる可能性がある
- → **1回の失敗で機能が壊れたと判断せず、まず再実行すること**

**時間経過で腐る可能性のある観測**
- 上記の「HTTP 200」「status=built」はすべて**23:44時点**のもの。再開時は取り直すこと

**推測で進めた箇所**
- **JPA 8ボールの対戦表はAPA（米国本部）公式のもの**。JPAはAPAのEqualizer®をライセンス使用しており9ボール表は完全一致を確認できたため同一と判断したが、**JPA公式資料での直接確認は取れていない**（JPAの該当PDFがスキャン画像で本文抽出できない）。`data/handicap_data.js` に注記あり
- カイルンの「1イニング内の連続得点可否」「ミス時のステップリセット」は一次情報が無く、ハウス設定の選択式にする方針（未実装）

**実装していないと明示したもの**
- **JCLのファーゴレート換算式は実装していない。** 算出方法が「ブラックボックス」と公表されているため、推測実装すると誤った目標点を出す。目標点は手入力とする方針

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v3`）
  → ファイルを変更したら**必ず数字を上げる**。上げないとスマホに古い画面がキャッシュされたまま残る。**デザイン全面変更では確実に上げる必要がある**
- **`data/` 配下の数値**（`handicap_data.js` のJPA表など）
  → 一次情報で確認した値。デザイン変更では触らない。変更するなら `04_種目ルール仕様.md` の出典を確認してから
- **`js/engine.js`**
  → 種目名の分岐を書かない設計。`engine.test.js` の最後で自動チェックしている。デザイン変更では触る必要がない
- **リポジトリの公開設定**
  → 現在**公開**。非公開に戻すとGitHub Pagesが止まり、スマホから使えなくなる
- **コミット作者のメールアドレス**
  → noreply アドレスを使う。生のGmailアドレスでコミットしない

---

### 8. 最後の判断・関門

**このセッションで下した主な判断**

1. **公開リポジトリにした** — 非公開＋Pagesが有料と判明したため。本人に確認して承認済み
2. **時計は3択で同時併用しない** — ショットクロックとチェスクロックは目的が違い、両方走らせると画面と操作が複雑になるため。本人指示「チェスクロックとショットクロックを選べるように」に沿った
3. **過去の試合はプレーヤー別成績に含めない** — 本人が「登録後の試合だけ集計」を選択
4. **スコアパネルを両方白にした際、橙の数字を濃くした（`#d98200`→`#c26a00`）** — 白地でコントラスト比が2.94:1となり基準（3:1）を下回ったため。3.92:1に改善
5. **ローテーションの「61点先取」をプリセットに入れなかった** — 日本の公式規程に存在しないことを条文で確認したため

**本人確認待ちの事項**: なし

---

### 次の安全なアクション（1つだけ）

**`_test\run_all.bat` と `python _test\live_test.py` を実行し、現在の全245項目＋公開19項目が通ることを確認する。**

これを基準線にしてからデザイン変更に入る。デザイン全面変更は影響範囲が広く、途中で何かが壊れたときに「元から壊れていたのか、自分が壊したのか」を切り分けられなくなるため。

### 再開者が仮定してはいけないこと

- **「テストが通っている＝スマホで動く」と仮定しないこと。** 実機での確認は一度もしていない
- **「デザイン変更はCSSだけ」と仮定しないこと。** アイコン（`icon-*.png`）は `_test/make_icons.py` で生成しており、`manifest.json` の `theme_color`・`background_color`、`index.html` の `<meta name="theme-color">` も配色に連動している。マリオ world 観にするならこれらすべてが対象
- **「1回テストが落ちた＝壊れた」と仮定しないこと。** 6.に記録したとおり、稀に落ちて再実行で通る事象を観測している。まず再実行する
- **`style.css` の可読性の制約を美観で上書きしないこと。** タップ領域56px以上、本文16px以上、コントラスト比4.5:1以上（数字は3:1以上）は、台の脇で立ったまま片手で使う道具としての要件。マリオ配色にする際もこれらは維持する
- **既存の配色トークンを消さないこと。** `style.css` 冒頭の `:root` に `--side-a`（青）`--side-b`（橙）などがあり、JSからではなくCSSから参照されている。変数名を変えるなら全箇所を追うこと
