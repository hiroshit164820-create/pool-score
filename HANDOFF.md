# 引き継ぎ

---

## 2026-08-22（01:55時点）※最新 — 新しい指示6件のうちA〜Cを公開。段階D（記録の共有）は実装済み・**未公開**

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開URL: https://hiroshit164820-create.github.io/pool-score/

本人が実機で使ったあとに出した**新しい指示6件**を4段階に分けて進めていた。

| 段階 | 指示 | 状態 |
|---|---|---|
| A | ①成績ページの既定を自分に ⑤プルダウンで「〇先」を選んだら黄色く | **公開済み（v42）** |
| B | ②横向き＋ショットクロックのボタン干渉（＋「他にもないかチェック」） | **公開済み（v42）** |
| C | ③ダブルスのペア組み替え ④選んだ名前だけ表示 | **公開済み（v42）** |
| D | ⑥試合記録を相手と共有する | **実装済み・検証中・未コミット・未公開** |

このセッションではほかに、**説明書 manual.html**（v41で公開済み）と
**イニングを数えるかの選択**（v39・v40で公開済み）も片付けている。

---

### 2. 変更点

#### 公開済み（`origin/main` = `9078f90`、01:54:32時点の `git log` で確認）

| コミット | 内容 | 公開 |
|---|---|---|
| `eeeac4a` | イニングの既定を「数えない」に。14-1だけ既定で数える | v40 |
| `7768aa0` | 説明書 `manual.html` を追加 | v41 |
| `9078f90` | 成績の既定・横向きのボタン干渉・プレーヤーの選び方（段階A〜C） | v42 |

#### **未コミットの変更（段階D）** — 01:54:32時点の `git status`

```
 M _test/run_all.bat    ... 49本目に share_test.py を足した
 M css/v2.css           ... 取り込み画面の見た目
 M index.html           ... #screenImport を足し、share.js / ui_import.js を読み込む
 M js/app.js            ... 起動時に IMPORTUI.checkHash() を呼ぶ
 M js/ui_history.js     ... 履歴に「相手に送る」ボタンと sendMatch()
 M sw.js                ... v42 → v43、新しい2ファイルをキャッシュ対象に
?? _test/share_test.py  ... 新規（35項目）
?? js/share.js          ... 新規（リンクの作成・読み取り・取り込み）
?? js/ui_import.js      ... 新規（届いた試合の取り込み画面）
```

**ローカルの `sw.js` は v43、公開版は v42**（01:54:32時点の curl で確認）。
＝ 段階Dはまだ本番に出ていない。

#### 段階Dの作り

- **サーバーを使わない。** 記録そのものをリンクに載せて渡す。
  JSON → gzip → base64url → URLの `#m=...`。
  「#」の後ろはサーバーに送られないので記録が外に出ない
- 実測（2026-08-22 01:4x）: 9ボール1試合＝**約1,100字**、
  JPA 9ボール58イベント＝**約2,000字**。`SHARE.MAX_CHARS`（6000）を超えたら
  1球ごとの記録を落として「結果だけ」にする
- **名前が違っても大丈夫にした**（本人の心配への答え）。
  取り込む前に「この人は誰ですか」で対応付ける。
  一致すれば最初から選ばれ、違えば**何も選ばれず「成績に入れない」**に倒す。
  一度対応付けたら `settings.sharedNameMap` に覚えて次から自動
- 同じ試合IDを持っていれば「もう持っています／二重になります」と出し、
  ボタンが「それでも取り込む」に変わる

#### 変更した外部状態

- **GitHub の `main` を4回更新した**（`2a31acd` → `9078f90`）
- **GitHub Pages が4回再ビルドされ、公開URLの中身が入れ替わった**（v39 → v42）
- **公開URLに `manual.html` が増えた**
- ローカルのデータ（localStorage）には触っていない

---

### 3. 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| 段階A〜Cの時点でローカル**全48本成功**（node 4＋実ブラウザ44） | 1本ずつ実行し**終了コードを直接**確認 | 22:5x〜23:00 |
| push が通り `origin/main` = `9078f90` | `git log` | 23:00:55 |
| Pagesのビルドが success | `gh run list` | 23:02 |
| **公開版の `CACHE_VERSION` が `v42`** | `curl`（キャッシュ回避つき） | 23:02:04 |
| 公開URLの実ブラウザ検証19項目が成功 | `python _test/live_test.py` 終了コード0 | 23:02 |
| **公開URLで段階A〜Cが動く** — 成績タブの既定が自分／プルダウンが金色／選択後は1人＋選び直す／1回でペアが入れ替わる／横向きで時計が上の帯・**スコア盤236px**・重なり0・画面外0・44px割れ0 | 公開URL相手の実ブラウザ確認 12項目 | 23:03 |
| **ボタン干渉の全数**（種目11×時計3×向き2＝66通り）が**0件** | `python _test/overlap_test.py` 終了コード0 | 22:4x |
| 段階Dの往復（送る側と受け取る側を別ブラウザで）**35項目すべて成功** | `python _test/share_test.py` 終了コード0 | 01:52 |
| 段階Dのリンクの長さ 1,126字・圧縮あり・結果だけに落ちていない | 同上のログ | 01:52 |
| 公開版が **v42**（＝段階Dは未反映） | `curl` | 01:54:32 |

**いま走っているもの（01:55時点で未完）**

- **全49本の検証をバックグラウンドで実行中**（`bp7ll6z1n`）。
  01:54:32時点で**11本目まで進み、そこまではすべて rc=0**。
  **残り38本の結果は分かっていない。**
  出力先: `C:\Users\hiros\AppData\Local\Temp\claude\c--Users-hiros-OneDrive-------\fefc4917-f291-458d-9f6b-4a1cb4879312\tasks\bp7ll6z1n.output`
  → **セッションが切れていれば、この実行はもう無い。もう一度回すこと。**

**確認していないこと（完了扱いにしていない）**

- **段階Dは公開していない。** 公開URLで動くかは未確認
- **本人の実機では何も見ていない**（v40・v41・v42・段階Dすべて）
- **`navigator.share` の実機動作は未確認。** テストは `SHARE.makeLink()` を
  直接呼んでおり、**LINEへ渡すところは通っていない**（Chromiumのヘッドレスに共有UIが無いため）
- **クリップボードへの写し込み（共有が使えない端末の道）も未確認**
- **iOS Safari で `CompressionStream` が使えるか未確認**（使えない端末では
  圧縮なしのリンクになり、長さが約4倍になる。6000字を超えれば結果だけに落ちる）
- 段階A〜Cの見た目を**本人が確認していない**

---

### 4. 一次情報の取り直しコマンド（疑わしいときはまずこれ）

```
cd D:\Claudecode\pool-score

# いま公開されているものが新しいか（v43以上なら段階Dが出ている）
curl -s -A "Mozilla/5.0" "https://hiroshit164820-create.github.io/pool-score/sw.js?cb=%RANDOM%" | findstr CACHE_VERSION

# 公開版が壊れていないか（実ブラウザで19項目）
python _test\live_test.py

# 段階Dの検証（送る側と受け取る側を別ブラウザで往復）
python _test\share_test.py

# ボタン干渉の全数（66通り・5分ほど）
python _test\overlap_test.py

# ローカルの全検証（49本・60分ほど）
_test\run_all.bat
```

**`git status` が clean でも「公開版が最新」とは限らない。** 必ず上の curl で実物を見る。
**テストをパイプ（`| tail`）に通すと終了コードがパイプ最後のものになり、失敗を見落とす。**
**`_test\run_all.bat` は Git Bash から `cmd /c` で呼ぶと文字化けして落ちる。**
1本ずつ回すシェルの雛形（このセッションで使ったもの）:
```
for f in $(grep -o 'python _test.[a-z0-9_]*\.py' _test/run_all.bat | sed 's/^python _test.//'); do
  python "_test/$f" >/tmp/plogs/$f.log 2>&1; echo "rc=$? $f"; done
```

---

### 5. 未着手の範囲

- **段階Dのコミットと公開**（次の作業。下の「次の安全なアクション」参照）
- **段階Dを本人が使えるようにしたあとの案内**。説明書 `manual.html` には
  共有機能のことを**まだ書いていない**。公開したら1節足すべき
- **QRコードで渡す道（案B）**。本人には「あとから足せる」と伝えてあるが**着手していない**
- **リアルタイム共有（案C）** は費用が発生しうるため提案のみ。着手していない
- **本人の26件の指示**（前セッションぶん）は段階1〜5で一巡済み

---

### 6. 不確実な点・リスク

- **段階Dの `navigator.share` は実機でしか確かめられない。**
  iOS Safari では `navigator.share` はユーザー操作の直後でないと拒否される。
  いまは `SHARE.makeLink()`（非同期）の**完了後**に `navigator.share` を呼んでいるので、
  **iOSで「ユーザー操作から離れすぎ」と判断されて弾かれる可能性がある**。
  実機で弾かれたら、リンクを先に作っておく（ボタンを押した瞬間に share を呼ぶ）作りに変える
- **`CompressionStream` が無い端末**では圧縮なしになり、リンクが約4倍に伸びる。
  6000字を超えれば自動で「結果だけ」に落ちるが、**1球ごとの記録は失われる**
- **取り込んだ試合は送り主のイベント列をそのまま持つ。**
  こちらで「スコア修正」から編集すると、送り主の記録と食い違う（同期はしない）
- **`sharedNameMap` は名前の文字列で覚えている。** 送り主が相手の登録名を変えると、
  別人として扱われる（取り込み画面で選び直せば直る）
- **`css/v2.css` が2700行を超えた。** 末尾に上書き用のブロックが積み上がっている
- 前セッションからの持ち越し（このセッションでは触っていない）:
  ブレイクエースを押すとマスワリも1増える／一般種目ではエースでマスワリが増えない／
  無効球は盤面の先頭の球を1つ外す／イニングの自動カウントを実データで確認していない

---

### 7. 触ってはいけない領域

**このセッションで増えたもの**

- **`js/share.js` の `VERSION`（"1"）と形式 `<版>.<g|r>.<本体>`**
  → 変えると、前に送ったリンクが開けなくなる
- **`js/ui_import.js` の `watchHash()`（hashchange の監視）**
  → **アプリを開いたままリンクを踏むとページが再読み込みされない**ため、
  これが無いと取り込みが動かない（01:4x に実測で確認した不具合）
- **`SHARE.importMatch()` で `playerIds` を空にする分岐**
  → 対応付けなかった側を成績に入れないための要。外すと他人の勝率が動く
- **`js/ui_match.js` の `inningsShown()`** → 古い記録に印が無いので「無ければ数える」
- **`js/store.js` の `indexEntry` の `innings`**（数えない試合は null）と **`g.inningRacks`**
- **`data/games_data.js` の `inningsDefault`**（14-1だけ既定ON）
- **`js/ui_setup.js` の `assignPlayer()`** → 他の欄にいる人を押したときの入れ替え。
  ペアの組み替えを1回で済ませるための要
- **`js/ui_setup.js` の `pickerOpenFor`** → 選んだあと候補を畳む状態
- **`css/v2.css` の `.shotclock.in-topbar` 一式と `js/ui_match.js` の `isTightLandscape()`**
  → 横向きで時計を上の帯へ寄せる仕組み。外すとスコア盤が236px→160pxに戻る
- **`_test/overlap_test.py`** → 66通りの干渉を見張る。デザインを触ったら必ず通す
- **`_test/helpers.py` の `pick_player()` / `set_innings()`**
  → 他のテストが依存している

**前からのもの（そのまま）**

- **`sw.js` の `CACHE_VERSION`**（ローカル v43／公開 v42）
  → アプリのファイルを変更したら**必ず数字を上げる**
- **`_test/run_all.bat` はUTF-8。パスの区切りは `chr(92)` かraw文字列で書く**
  → このセッションで `\t` をタブに変えてしまい、1行壊した（`une9_test.py` と表示された）
- **テストのスクショの保存先は `_test/shots`**（`_test/_shots` は .gitignore されていない）
- **`js/engine.js` に種目名を書かない**（`engine.test.js` の設計ゲートが落ちる）
- **`js/engine.js` の `ensureTurnBy()` / `applyDeadBalls()` の `deadPoints`**
- **`js/ui_match.js` の `recordInstantByBalls()` / `maybeAdvanceRack()`**
- **`js/ui_sheet.js` の `jpa-sheet-open` クラス**
- **`data/handicap_data.js` の `JPA_DOUBLES_9BALL`**
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

**このセッションで下した判断**

1. **共有はリンク方式（案A）にした** — サーバーを持たない構成を崩さず、費用ゼロ。
   本人に3案（リンク／QR／リアルタイム）を出し、Aで進める合意を得た
2. **名前が違うときは「成績に入れない」に倒した** — 本人の
   「万が一名前が違った場合はどうなる？」への答え。
   間違った人の勝率が動くくらいなら、記録だけ残すほうが害が小さい
3. **横向きの時計は上の帯へ移した** — 帯を薄くするだけでは足りず、
   スコア盤が160pxのままだったため。移した結果236pxに戻った
4. **プレーヤー選択は「候補から外す」をやめ、「押すと入れ替える」にした** —
   本人の「いちいちB側に別の人をあてる作業が発生する」を1タップで解決するため。
   この変更で旧仕様前提のテスト3本（slice1/features/stage3）の期待値を更新した
5. **イニングの既定は種目ごとに持たせた**（`inningsDefault`）— 本人が
   「既定は数えない」「14-1だけ数える」と2段階で指示したため

**本人確認待ちの事項**

- **段階D（共有）を公開してよいか** — まだ公開していない
- **v40〜v42 の実機での見え方**（イニング・説明書・段階A〜C）
- **説明書に共有機能の説明を足すか**

---

### 次の安全なアクション（1つだけ）

**走っている全49本の検証（`bp7ll6z1n`）の結果を確認する。**
セッションが切れていれば、`_test\run_all.bat` 相当を1本ずつ回し直す。
**0件になるまでは段階Dをコミット・公開しない。**

### 再開者が仮定してはいけないこと

- **「段階Dが公開されている」と仮定しないこと。** 01:54:32時点で公開版は **v42**、
  ローカルは v43。**まだ出ていない**
- **「全49本が通った」と仮定しないこと。** 01:54:32時点で確認できたのは**11本目まで**
- **「共有機能が実機で動く」と仮定しないこと。** `navigator.share` と
  クリップボードは**ヘッドレスでは通っていない**。iOSで弾かれる可能性を6.に書いた
- **「本人が v40〜v42 を見た」と仮定しないこと。** 実機の報告はまだ無い
- **「説明書に共有のことが書いてある」と仮定しないこと。** 書いていない
- **「`git status` が clean ＝公開版が最新」と仮定しないこと。** 4.の curl で実物を見る
- **テストの結果をパイプ越しに見ないこと。** 終了コードがパイプ最後のものになる

---


## 2026-08-21（20:18時点）— イニングの既定を「数えない」に。14-1だけ既定で数える（v40公開）

本人の指示2件（16:3x「既定を数えない、にします」／20:0x「14-1だけ既定で数えるにします」）。
**実装して公開まで通した。** コミット `eeeac4a`、公開は **v40**。

### 変更点

- 一般種目の既定を **「数えない」** に（9/10/8ボール＋ダブルス・ローテーション）
- **14-1 だけ既定で「数える」**。`data/games_data.js` に **`inningsDefault: true`** を書いた
  種目だけ既定ONになる仕組みにした（イニングが実力の指標のため）
- JPA・JCLは従来どおり常に数える（選択欄を出さない）
- **種目を切り替えるたび、その種目の既定に戻る**
  （`js/ui_setup.js`: `countInnings = g.inningsOption ? !!g.inningsDefault : true;`）
- 既存テスト3本（`land` `tune8` `detail`）はイニングの表示・平均イニング数を
  確かめるのが目的なので、始める前に **`helpers.set_innings(pg, True)`** で
  明示的に「数える」を選ぶようにした（新設のヘルパー）

### 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| `innings_opt_test` 44/44 | 終了コード0 | 20:1x |
| ローカル**44本すべて成功**（node 4＋実ブラウザ40） | 1本ずつ終了コードを直接確認 | 19:5x〜20:15 |
| push が通り `origin/main` = `eeeac4a` | `git log` | 20:16:46 |
| Pagesのビルドが success | `gh run list` | 20:17 |
| **公開版の `CACHE_VERSION` が `v40`** | `curl`（キャッシュ回避つき） | 20:17:54 |
| 公開URLの実ブラウザ検証19項目が成功 | `python _test/live_test.py` 終了コード0 | 20:18 |
| **公開URLで実物確認**: 9/10/8ボール・ローテーションの既定が「数えない(選択中)」／14-1だけ「数える(選択中)」／14-1を触らず始めると `countInnings: true` で試合中にイニングが出る／9ボールを触らず始めると `false` で出ない／JSエラー無し | 公開URL相手の実ブラウザ確認 10項目 | 20:18:5x |

**確認していないこと**: **本人の実機では見ていない**（v40 は未確認）。

### 触ってはいけない領域（このぶん）

- **`data/games_data.js` の `inningsDefault`** → 14-1 の既定ONはこれ1行だけで決まる。
  消すと 14-1 も「数えない」になる
- **`_test/helpers.py` の `set_innings()`** → イニングの表示を確かめる3本のテストが
  これを呼んでいる。外すと既定OFFのせいでテストが落ちる

---

## 2026-08-21（16:26時点）— 一般種目でイニングを数えるかを選べるようにした（v39公開）

### 目標と結果

本人の指示「一般種目ルール選択時にイニングをカウントするか選択できるようにして」。
**実装して公開まで通した。** コミット `2e49d3d`、公開は **v39**。

### 変更点

- `data/games_data.js` に **`inningsOption: true`** を追加した種目でだけ、
  設定に「イニング：数える／数えない」が出る。
  対象は **9/10/8ボール＋ダブルス・ローテーション・14-1**（＝一般グループ）
- **既定は「数える」**。指示は「選べるように」だったので、既定までは変えていない。
  種目を選び直すと既定に戻る
- **JPA・JCLには出さない**（公式スコアシートの土台なので常に数える）。
  ボウラードは1人でやる種目でイニングの概念が無い
- 「数えない」を選ぶと出さない場所: 試合中の帯（`#inningInfo`）／
  スコア修正のイニング調整（`#reviseInning`）／試合終了の確認／履歴
- **成績**: 「1ラックあたりの平均イニング数」の分母を `g.inningRacks`
  （数えた試合のラック数だけ）に変えた。数えた試合が0なら行ごと出さない。
  「あがりまでの最小／最大イニング数」も数えた試合だけ
- **engine は今までどおり全種目で数えている**（`st.innings`）。
  変わるのは出すかどうかだけなので、あとから数え直せる
- `sw.js` の `CACHE_VERSION` は v38 → **v39**

触ったファイル: `data/games_data.js` `js/ui_setup.js` `js/ui_match.js`
`js/store.js` `js/ui_players.js` `sw.js`／新規 `_test/innings_opt_test.py`（40項目）／
`_test/run_all.bat`（44本に）

### 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| 新設 `innings_opt_test` 40/40（横向きの確認込み） | 終了コード0 | 16:2x |
| ローカル**44本すべて成功**（node 4＋実ブラウザ40） | 1本ずつ実行し終了コードを直接確認 | 16:0x〜16:23 |
| push が通り `origin/main` = `2e49d3d` | `git log` | 16:24:30 |
| Pagesのビルドが success | `gh run list` | 16:25 |
| **公開版の `CACHE_VERSION` が `v39`** | `curl`（キャッシュ回避つき） | 16:25:38 |
| 公開URLの実ブラウザ検証19項目が成功 | `python _test/live_test.py` 終了コード0 | 16:26 |
| **公開URLで実物確認**: 9ボールに「数える(選択中)／数えない」が出る／JPAには出ない／開始前の確認に「イニング: 数えない」／記録に `countInnings: false` が残る／試合中の帯にイニングが出ない／ラック数は出る／JSエラー無し | 公開URL相手の実ブラウザ確認 7項目 | 16:26:4x |

**確認していないこと**

- **本人の実機では見ていない**（v39 は未確認）
- **「数えない」試合を含む成績ページの見た目**は目視していない（数値の筋は通してある）
- **既定を「数える」にしたのは私の判断**。本人が「既定は数えないでいい」と言えば
  `data/games_data.js` に `inningsDefault` を足すだけで変えられる

### 触ってはいけない領域（このぶん）

- **`js/ui_match.js` の `inningsShown()`** → 古い記録には `countInnings` が無いので
  「無ければ数える」にしてある。`=== true` で判定すると過去の試合からイニングが消える
- **`js/store.js` の `indexEntry` の `innings`** → 「数えない」試合は `null`。
  履歴とCSVはこの null を見て出し分けている
- **`g.inningRacks`** → 平均イニングの分母。`g.racks` に戻すと
  「数えない」試合のラックまで分母に入って平均が薄まる

---

## 2026-08-21（14:05時点）— 段階3・4・5まで完了。26件の指示は一巡した

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開URL: https://hiroshit164820-create.github.io/pool-score/

本人が実機で確認して出した**26件の指示**を、前セッションで5段階に分けた。
このセッションで**段階3（選手・成績）・段階4（見た目）・段階5（アドレス）を片付けた**。
段階1・2（試合画面）は前セッションで公開済み。

**次のセッションの目標**: 決まった作業は無い。
**本人が v38 を実機で見た報告がまだ無い**ので、そこで出た指摘から始めるのが自然。

⚠ **26件の原文（本人が送った画像2枚）は、このセッションでは見ていない。**
実装したのは前セッションの引き継ぎに文章で書き起こされた項目（段階3が7件、
段階4が4件、段階5が1件）。**「26件すべてを原文と突き合わせた」わけではない。**

---

### 2. 変更点（このセッションで実際に変更したもの）

コミット3件。push済み（`origin/main` = `a80e198`、14:03:41時点の `git log` で確認）。

| コミット | 内容 | 公開 |
|---|---|---|
| `30262c6` | 段階3: 選手・成績（種目ごとの成績の削除・自分の扱い） | v37 |
| `e93933a` | 段階4: 見た目（左右の色・ホームのカード・アイコン） | v38 |
| `a80e198` | 狭い画面（360/320px）の検証を追加（アプリ本体は変えていない） | v38のまま |

#### 段階3（選手・成績）

- **「種目ごとの成績」を画面ごと削除した。** `js/ui_gamestats.js` を消し、
  index.html の `#screenGameStats` と script タグ、sw.js のキャッシュ一覧からも外した。
  **前日 v35 で「成績ページから開けるように」した導線も一緒に消えている**
- **他選手の成績の一覧に自分を出さない。** 自分は左の「自分の成績」で見る。
  自分しか登録が無いときは「自分のほかに登録された選手がいません。」と出す
- **選手一覧の登録ボタン2つを上の帯へ移し、「戻る」を削除した**（下部タブで移動できるため）
- **自分の印を名前の右へ移し、「自分」→「★」に短くした**
  - ここが要点: 印を**名前の欄（`.pc-name`）の中**に入れた。
    以前は行（`.mc-main`）の直下に置いていて、`.mc-main` が
    `1fr auto 1fr` の3列 grid なので**自分だけ欄が1つ増えて列がずれ、
    勝敗の札が他の人と違う位置に出ていた**。これが
    「自分の勝敗も他と同じくカード右上に表示」の正体
- パートナー別の成績は消えていない（個人の成績ページに残っている）

#### 段階4（見た目）

- **プレーヤーの名前欄を A=青／B=赤 の枠線に。** 見出しの字も同じ色
- **ハンデあり・点数制の目標・JPAのスキルレベル・ボールハンデ**の左右別の欄にも同じ色。
  選んだボタンは**金色の地のまま枠だけ色**にした（地ごと変えると「押してある」ことが
  分かりにくくなるため）
- **ホームのカードを作り直した。** 見出しをカード幅いっぱいの金ブロックの帯
  （四隅にリベット・ドット文字）、数字の欄をコインブロック、
  直近の試合の行の左にレンガの縁。カードの枠を太く（4px）
- **アプリのアイコンをアラミス ブラックの9番に**（黒地に黄の帯、白丸の中に9）。
  図案は `_test/make_icons.py` の `PIXELS`（16x16）。作り直しは `python _test/make_icons.py`

#### 段階5（サイトのアドレス）

- **本人の判断は「いまのまま変えない」**（14:00頃・選択肢を提示して回答をもらった）。
  **作業はしていない。URLは https://hiroshit164820-create.github.io/pool-score/ のまま**
- 調べた事実（GitHub Docs、13:45頃取得）:
  - **リポジトリ名を変えると Pages のURLはリダイレクトされない**（旧URLは切れる）
  - **ユーザー名変更**はリポジトリのリダイレクトはあるがプロフィールURLは404。
    **Pages への影響は Docs に明記が無い**（不確実なまま）
  - **独自ドメインは GitHub 側は無料**。ドメイン代のみ（.com は原価ベースで年 $8〜11）

#### 触ったファイル

`index.html`（選手一覧の上の帯・種目別成績の画面を削除）／`css/v2.css`／`sw.js`(v36→v38)
`js/ui_players.js`（自分の印・他選手一覧から自分を除外・種目別成績のボタン削除）
`js/ui_setup.js`（左右の色のクラス付与）／**`js/ui_gamestats.js` は削除**
`_test/make_icons.py` と アイコン4枚（`icon-192` `icon-512` `icon-512-maskable` `apple-touch-icon`）
新規テスト2本 `stage3`(34) `stage4`(23)／既存4本の期待値更新（`stats2` `stats4` `tune5` `fix2`は無変更）
`_test/run_all.bat`（43本に）

#### 変更した外部状態

- **GitHub の `main` を3回更新した**（`fd563dd` → `a80e198`）
- **GitHub Pages が3回再ビルドされ、公開URLの中身が入れ替わった**（v36 → v38）
- ローカルのデータ（localStorage）には触っていない

---

### 3. 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| 段階3の時点でローカル検証**42本すべて成功**（node 4＋実ブラウザ38） | 1本ずつ実行し**終了コードを直接**確認 | 13:0x〜13:29 |
| 段階4の時点でローカル検証**43本中42本成功、1本失敗**（`fix2_test`） | 同上 | 13:5x |
| 失敗の中身: ホームの×ボタンを38pxに縮めてしまい**44pxのタップ領域を割った** | `fix2_test` のNG行 | 13:58 |
| 直した後、`fix2`(17) `stage4`(23) が成功 | 各終了コード0 | 13:59 |
| ホームに触る9本（self_player/session/stage3/stats2/stats4/tabbar/tune4/tune5/layout_fit）が成功 | 各終了コード0 | 14:00 |
| 新設 `stage3_test` 34/34、`stage4_test` 23/23 | 各終了コード0 | 14:0x |
| push が通り `origin/main` = `a80e198` | `git log` | 14:03:41 |
| Pagesのビルドが success | `gh run list` | 14:01 |
| **公開版の `CACHE_VERSION` が `v38`** | `curl` でキャッシュ回避パラメータ付き取得 | 14:01:44 |
| **公開URLでの実ブラウザ検証19項目がすべて成功** | `python _test/live_test.py` 終了コード0 | 14:02 |
| **公開URL（390x844）で段階3・4が動く** — 種目別成績もGAMESTATSも無い／選手一覧に戻るが無く上の帯に登録2つ／自分の印★が名前の右・勝敗の右端が他と一致(359px)／他選手の成績に自分が出ない／名前欄がA青(11,99,214)・B赤(212,59,18)／ホームの見出しが金(251,208,0)／JSエラー無し | 公開URL相手の実ブラウザ確認 13項目 | 14:02:5x |
| **360px・320px でも上の帯がはみ出さず、見出しに重ならず、ボタンは44px** | 実ブラウザ計測（`stage3_test` に恒久化） | 14:03 |
| アイコンPNGの実ピクセル: 上=(20,20,20)黒 帯=(240,193,27)黄 | PIL で直接読取（`stage4_test`） | 14:0x |

**確認していないこと（完了扱いにしていない）**

- **本人のスマホ実機・iOS Safari では見ていない**（v38 は未確認）
- **段階3・4の見た目を本人が確認していない**（★の印・金ブロックの帯・アイコン）
- **26件の原文（画像2枚）との突き合わせをしていない**（上の1.参照）
- **ホーム画面に「中断中の試合」がある状態の見た目**は `fix2_test` の数値では通ったが、
  目視していない（帯を52pxに高くしたので×との重なりは無いはず）
- **横向き（844x390）での段階4の見た目**は測っていない

---

### 4. 一次情報の取り直しコマンド（疑わしいときはまずこれ）

```
cd D:\Claudecode\pool-score

# いま公開されているものが新しいか（v38以上ならこのセッションぶんは反映済み）
curl -s -A "Mozilla/5.0" "https://hiroshit164820-create.github.io/pool-score/sw.js?cb=%RANDOM%" | findstr CACHE_VERSION

# 公開版が壊れていないか（実ブラウザで19項目）
python _test\live_test.py

# 今回ぶんの検証
python _test\stage3_test.py   ... 選手・成績（34項目・狭い画面の確認込み）
python _test\stage4_test.py   ... 見た目とアイコン（23項目）

# ローカルの全検証（43本・50分ほど）
_test\run_all.bat
```

**`git status` が clean でも「公開版が最新」とは限らない。** 必ず上の curl で実物を見る。
**テストをパイプ（`| tail`）に通すと終了コードがパイプ最後のものになり、失敗を見落とす。**
**`_test\run_all.bat` は Git Bash から `cmd /c` で呼ぶと文字化けして落ちる。**
1本ずつ `python _test\xxx_test.py` で回して終了コードを見るほうが確実。
43本を1本ずつ回すシェルの雛形はこのセッションで使ったものが手早い:
`for f in $(grep -o 'python _test.[a-z0-9_]*\.py' _test/run_all.bat | sed 's/^python _test.//'); do python "_test/$f" >/tmp/$f.log 2>&1; echo "rc=$? $f"; done`

---

### 5. 未着手の範囲

**26件の指示から出た作業は、段階1〜5まで一巡した。決まった残件は無い。**

やるとすれば次のあたり（**どれも本人の指示ではない。勝手に始めないこと**）:

- **横向き（844x390）での段階4の見た目の点検**（金ブロックの帯が高さを食っていないか）
- **`css/v2.css` が2600行を超えた。** 末尾に上書き用のブロックが積み上がっている。
  整理するなら本人に時間の見積もりを出してから
- **未実装の種目はカイルンとJCLのみ**（前からの持ち越し）

---

### 6. 不確実な点・リスク

- **前セッションからの持ち越し**（このセッションでは触っていない）:
  - ブレイクエースを押すとマスワリも1増える（JPA等の球単位の種目）。本人の想定は未確認
  - 一般種目ではエースを押してもマスワリは増えない。**JPAと数え方が違う**
  - 無効球は盤面の先頭の球（9番以外）を1つ外す。どの球かは選ばせていない
  - イニングの自動カウント（`ensureTurnBy`）は実データでの確認をしていない
- **自分の印を「★」1文字にした。** 「自分」という語が消えたので、
  初見で意味が取れるかは本人の目で見てもらう必要がある（title と aria-label には「自分」を入れてある）
- **ホームの見出しをドット文字（DotGothic16）にした。** 15pxで、
  本文16pxの制約は本文向けなので外したが、読みにくいと言われたら戻す
- **アイコンを変えたので、すでにホーム画面に追加してある端末では
  古いアイコンが残ることがある**（PWAのアイコンは貼り直すまで更新されないことがある）
- **`.field.side-a` / `.side-b` は設定画面の左右別の欄すべてに効く。**
  今後 `.field` を足すときは、左右に関係ない欄に side クラスを付けないこと

---

### 7. 触ってはいけない領域

**このセッションで増えたもの**

- **`js/ui_players.js` の自分バッジを `nameBox.appendChild()` している所**
  → 行（`.mc-main`）の直下に戻すと3列 grid がずれ、
  **自分だけ勝敗の位置が変わる不具合が再発する**
- **`css/v2.css` の `.add-player-head.in-bar`**
  → 本文いっぱいに広がる指定を打ち消している。外すと上の帯が壊れる
- **`.home-card.resume .hc-close` の大きさを指定しないこと**
  → 44pxを割ると `fix2_test` が落ちる。**一度やって落とした**
- **`css/v2.css` の `.home-card .hc-title` の `margin: 0 -14px 12px`**
  → カード幅いっぱいの帯にするための負のマージン。`.home-card` の
  `padding: 0 14px 12px` と対になっている
- **`_test/make_icons.py` の `PIXELS` は16行×16列**
  → 1行でも長さが違うと図がずれる。変えたら必ず行数と長さを数える

**前からのもの（そのまま）**

- **`sw.js` の `CACHE_VERSION`**（現在 `v38`）→ アプリのファイルを変更したら**必ず数字を上げる**
- **`js/engine.js` の `ensureTurnBy()`**（交代を押さなくてもイニングを数える要）
- **`js/engine.js` の `applyDeadBalls()` の `deadPoints`**（ラックの区切り判定・個数ではなく点）
- **`js/ui_match.js` の `recordInstantByBalls()`**（球単位の種目のマスワリ・エース）
- **`js/ui_match.js` の `maybeAdvanceRack()`**（満点を種目データから計算。10を直書きしない）
- **`js/ui_sheet.js` の `jpa-sheet-open` クラス**（開いている間の高さ配分）
- **`js/ui_match.js` の `syncMetaPlace()` / `hasVisibleChild()`**（`getClientRects()` で判定しない）
- **`css/v2.css` の `#screenMatch .topbar h1` の `white-space: nowrap`**
- **`#screenMatch .turn-row .break-toggle .bt-name` の `font-size: 14px`**
- **`js/ui_match.js` の `isAutoEvent()` / `voidTrailingAuto()`**
- **grid の欄に `justify-self: start / end` を付けない**
- **`data/handicap_data.js` の `JPA_DOUBLES_9BALL`**（対称・合計15まで）
- **`js/ui_layout.js` の `RAIL_PX`（14）と `css/v2.css` の `--rail`**（同じ値に）
- **`_test/run_all.bat` はUTF-8。** パスの区切りは `chr(92)` かraw文字列で書く
- **テストのスクショの保存先は `_test/shots`**（`.gitignore` 済み）。
  `_test/_shots` に書くと成果物でないPNGがコミットに混ざる
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

**このセッションで下した判断**

1. **段階3を先に公開し、段階4を別コミットにした** — 段階4は見た目が大きく変わるので、
   もし戻すことになっても段階3が巻き添えにならないようにした
2. **自分の印を名前の欄の中に入れた** — 「名前の右に置く」だけなら行の中で順番を
   変えればよいが、それでは3列 grid のずれ（＝勝敗の位置の違い）が直らない。
   指示の2件は同じ原因だったので、まとめて直した
3. **印の文字を「★」にした** — 「短く」の指示に対し、
   アプリ内で「自分を登録する」に使っている記号に合わせた
4. **選んだボタンの地の色は変えず、枠だけ左右の色にした** — 金色の地は
   「押してある」ことの合図なので、そこを潰さないため
5. **×ボタンを縮めるのではなく帯を高くした** — 44pxのタップ領域は
   本人が非IT利用者向けに置いている決まり。見た目のために割らない
6. **アドレスは選択肢を出して本人に決めてもらった** — 有料の案があるため。
   結果は「いまのまま変えない」

**本人確認待ちの事項**

- **v38 の実機（スマホ・iOS Safari）での見え方**
- **自分の印「★」で意味が取れるか**
- **ホームの金ブロックの帯・アイコンの黒い9番**

---

### 次の安全なアクション（1つだけ）

**本人に v38 を実機で見てもらい、出た指摘を聞く。**
決まった作業は残っていないので、勝手に次を始めない。

### 再開者が仮定してはいけないこと

- **「26件を原文と突き合わせた」と仮定しないこと。** 見たのは前セッションの書き起こし
- **「本人が v38 を見た」と仮定しないこと。** 実機の確認報告はまだ無い
- **「アドレスを変えた」と仮定しないこと。** 本人の判断で**変えていない**
- **「種目ごとの成績がどこかに残っている」と仮定しないこと。** ファイルごと消してある
- **「イニングの数え方が過去の記録に影響しない」と仮定しないこと。** 実データで見ていない
- **「`git status` が clean ＝公開版が最新」と仮定しないこと。** 4.の curl で実物を見る
- **テストの結果をパイプ越しに見ないこと。** 終了コードがパイプ最後のものになる

---

## 2026-08-21（12:50時点）— 26件の指示のうち段階1・2（試合画面）を公開

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開URL: https://hiroshit164820-create.github.io/pool-score/

本人が実機で確認し、**26件の指示**（画像1＝試合画面12件／画像2＝選手一覧5件／その他6件＋
「種目ごとの成績は削除」「他選手の成績に自分を出さない」）を出した。
着手前に5段階に分けて進めることを示し、**段階1と段階2を公開まで通した。**

**次のセッションの目標**: **段階3（選手・成績）→ 段階4（見た目）→ 段階5（アドレス）**。
段階5は本人の判断待ち。

---

### 2. 変更点（このセッションで実際に変更したもの）

コミット2件。push済み（`origin/main` = `045504b`、12:48:42時点の `git log` で確認）。

| コミット | 内容 | 公開 |
|---|---|---|
| `05737a9` | 種目ごとの成績を成績ページから開けるようにした（本人の指示） | v35 |
| `045504b` | 段階1・2: 試合画面の作り直しと、球単位の種目のマスワリ・エースの不具合 | v36 |

#### 段階1（不具合）

- **JPAのように球1個ずつ入力する種目で、マスワリ・ブレイクエースの札を押しても
  回数が増えず、スコアだけ1点増えていた**（本人の指摘）。
  原因は `recordRackWin` が球単位の種目に渡すときに instant（押した札）を捨てていたこと。
  - ブレイクエース → 「ブレイクで9番が入った」形（`POCKET` の `onBreak: true`）で記録
  - マスワリ → 「残り球を撞き切った」形（残りの球を9番を最後にして一度に記録）
  - どちらも engine が回数を数えられる形にした。
    **ブレイクエースを押すとマスワリも1増える**（ブレイクして撞き切った形になるため）
- 横向きでスコアシートを開いても、CSSで高さ50pxに潰されて**中身が見えなかった**
- 横向きで**スコアボードが潰れていた**（パネルがスコア欄から19pxはみ出していた）

#### 段階2（試合画面の作り直し）

- **下の帯を「スコア修正・スコアシート・無効球・次のラック」の1行**にした
  （次のラックはラック情報の帯から移動。ラック情報の帯は中身が無くなり畳まれる）
- **「訂正」→「スコア修正」**に改名し、アイコン（🛠）を付けた
- **スコアシートは下の帯のボタンで開閉**。開いている間はシートに残りの高さを渡し、
  記録のボタン列（マスワリ等）は隠す。閉じている間は場所を取らない
- **スコアシートにラックの区切り線とラック番号（R1・R2…）**を入れた
- **無効球ボタン**を足した（点は増えず、回数だけ数える。`DEAD_BALLS` イベント）
- **両者のスコアと無効球ぶんの点の合計が1ラックぶんになったら次のラックへ**進む。
  満点（JPA9ボールは10点）は種目のデータから計算している
- **交代を押さなくても、A→B→A の入力でイニングを1増やす**（engine の `ensureTurnBy`）
- **スコア修正の一覧を「○○が N点 追加」**に変え、無効球も一覧に出す
- **スコア修正からイニングを増減**できるようにした（`INNING_ADJ` イベント）

**副産物**: シートが閉じている間に場所を取らなくなったので、
横向きのスコアボードが **115px → 240px** に広がり、記録のボタンのはみ出しが0になった。

#### 触ったファイル

`index.html`（下の帯・スコア修正モーダル）／`css/v2.css`／`sw.js`(v35→v36)
`js/engine.js`（無効球の点数計算・イニングの自動カウント）
`js/ui_match.js`（札の記録・無効球・ラック送り・スコア修正）
`js/ui_sheet.js`（開閉の外出し・区切り線）／`js/ui_players.js`・`js/ui_gamestats.js`（v35ぶん）
新規テスト2本 `flag_jpa`(15) `match2`(39)／既存4本の期待値更新（`slice1` `tune7` `stats2` `detail`）
`_test/run_all.bat`（41本に）

#### 変更した外部状態

- **GitHub の `main` を2回更新した**（`13c3e0e` → `045504b`）
- **GitHub Pages が2回再ビルドされ、公開URLの中身が入れ替わった**（v34 → v36）
- ローカルのデータ（localStorage）には触っていない

---

### 3. 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| ローカルの検証**45本すべて成功**（node 4本＋実ブラウザ41本） | 1本ずつ実行し**終了コードを直接**確認（パイプに通していない） | 12:0x〜12:45 |
| 新設 `flag_jpa_test` 15/15、`match2_test` 39/39 | 各終了コード0 | 12:4x |
| 期待値を直した既存テスト: `slice1` 36項目、`tune7` 35/35、`stats2` 53項目、`detail` 60/60 | 各終了コード0 | 12:4x |
| push が通り `origin/main` = `045504b` | `git log` | 12:48:42 |
| Pagesのビルドが success | `gh run list` | 12:48 |
| **公開版の `CACHE_VERSION` が `v36`** | `curl` でキャッシュ回避パラメータ付き取得 | 12:48:42 |
| **公開URLでの実ブラウザ検証19項目がすべて成功** | `python _test/live_test.py` 終了コード0 | 12:48 |
| **公開URL（横向き844x390・JPA9ボール）で段階1・2が動く** — 下の帯が4つ／スコアボード240px・はみ出し0・下の帯も画面内／ブレイクエースが1回でスコア+2／無効球1個で点は増えずラック2へ／シートが118pxで開き区切り R1 が出る／スコア修正の見出しが「🛠 スコア修正」・一覧が「たいら が 2点 追加」「たいら の無効球 1個」・イニング調整あり／JSエラー無し | 公開URL相手の実ブラウザ確認 | 12:49:37 |

**確認していないこと（完了扱いにしていない）**

- **本人のスマホ実機・iOS Safari では見ていない**（v36 は未確認）
- **段階2の見た目を本人が確認していない**（アイコンの見え方・区切り線の位置など）
- **無効球ボタンの実運用**（ファウルと同時に押す流れ）は本人未確認

---

### 4. 一次情報の取り直しコマンド（疑わしいときはまずこれ）

```
cd D:\Claudecode\pool-score

# いま公開されているものが新しいか（v36以上ならこのセッションぶんは反映済み）
curl -s -A "Mozilla/5.0" "https://hiroshit164820-create.github.io/pool-score/sw.js?cb=%RANDOM%" | findstr CACHE_VERSION

# 公開版が壊れていないか（実ブラウザで19項目）
python _test\live_test.py

# 今回ぶんの検証
python _test\flag_jpa_test.py   ... マスワリ・エースの記録（15項目）
python _test\match2_test.py     ... 試合画面の作り直し（39項目）

# ローカルの全検証（41本・50分ほど）
_test\run_all.bat
```

**`git status` が clean でも「公開版が最新」とは限らない。** 必ず上の curl で実物を見る。
**テストをパイプ（`| tail`）に通すと終了コードがパイプ最後のものになり、失敗を見落とす。**
**`_test\run_all.bat` は Git Bash から `cmd /c` で呼ぶと文字化けして落ちる。**
1本ずつ `python _test\xxx_test.py` で回して終了コードを見るほうが確実。

---

### 5. 未着手の範囲（**ここが次の作業。すべて未着手**）

#### 段階3: 選手・成績（画像2＋その他）

- **「種目ごとの成績」は削除**する（本人の指示 12:0x）。
  → **12:50時点では削除していない。** 成績ページの冒頭にボタンがあり、
  画面（`#screenGameStats`）も `js/ui_gamestats.js` も残っている。
  **今日いちど「成績ページから開けるように」して公開した（v35）ものを、消すことになる**
- **他選手の成績ページに自分を表示しない**（いまは自分も一覧に出る）
- **登録ボタンを上の帯に置く**（選手一覧。いまは本文の上に2つ並んでいる）
- **戻るボタンを削除**（選手一覧の上の帯）
- **自分のバッジは名前の右側に置く**（いまは名前の左）
- **自分バッジを短く**（いまは「自分」）
- **自分の勝敗も他と同じくカード右上に表示**（いまは自分だけ出ていない可能性。要確認）

#### 段階4: 見た目（その他）

- **プレーヤー選択時の名前の枠線に色**（Aは青、Bは赤）
- **ハンデあり・勝利条件選択（JPA含む）でも、A側の選択は青、B側は赤の枠線**
- **ホーム画面のカードと中の色を、もう少しマリオのギミックらしく**
- **アイコンのボールをアラミスブラックにする**
  （`data/balls_data.js` の `aramith_black`。地が黒で帯が色。
  アイコンは `icon-192.png` `icon-512.png` `icon-512-maskable.png` `apple-touch-icon.png`。
  作り直しは `_test/make_icons.py` にある）

#### 段階5: サイトのアドレス（**本人の判断待ち。着手していない**）

いまのアドレス: `https://hiroshit164820-create.github.io/pool-score/`
（GitHubリポジトリ `hiroshit164820-create/pool-score` の GitHub Pages）

考えられる方法（**どれも一次情報で確認していない。着手前に裏を取ること**）:

| 方法 | 変わるアドレス | 費用 | 影響 |
|---|---|---|---|
| リポジトリ名を変える | `.../新しい名前/` | 無料 | **いまのURLは切れる。** スマホのホーム画面のアイコンも貼り直し |
| GitHubのユーザー名を変える | `https://新しい名前.github.io/pool-score/` | 無料 | **他のリポジトリのURLも全部変わる**（koban-roadmap 等） |
| 独自ドメイン | 好きな名前 | **年額1,000〜2,000円ほど（有料）** | DNS設定が要る |
| 別のホスティング（Netlify等） | `https://好きな名前.netlify.app/` | 無料 | 置き場が増える。デプロイの流れを作り直す |

**本人に、どの形にしたいか（費用をかけるか）を聞くこと。**

---

### 6. 不確実な点・リスク

- **ブレイクエースを押すと、マスワリも1回増える。** engine が
  「ブレイクした人が相手に渡さず取った」と判定するため。
  実際の競技でもブレイクエースはマスワリの一種なので妥当と判断したが、
  **本人がどちらを想定しているかは確認していない**
- **一般種目（9ボール等）では、エースを押してもマスワリは増えない。**
  ラック単位の種目は押した札しか立てない作りのため。**JPAと数え方が違う**
- **無効球は「盤面の先頭の球（9番以外）」を1つ外す。**
  どの球が落ちたかは選ばせていない（目視できない前提の指示のため）。
  9番が最後の1個になったときは9番が外れる
- **イニングの自動カウントは engine を変えた（`ensureTurnBy`）。**
  進行中の試合の数え方が変わる。**保存済みの過去の記録は result に固定されているので
  変わらないはずだが、実データでの確認はしていない**
- **無効球の点は「その球が本来持っていた点」で数える**（9番なら2点）。
  ラックの区切り判定に使っている
- **`css/v2.css` が2500行を超えた。** 末尾に上書き用のブロックが積み上がっている
- **JPA の 360x640 縦で、記録のボタンがスコア欄から17px出る**（前セッションからの残件。
  シートを閉じている間は場所が空いたので、いまは改善している可能性があるが未測定）

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v36`）
  → ファイルを変更したら**必ず数字を上げる**
- **`js/engine.js` の `ensureTurnBy()`** → 交代を押さなくてもイニングを数える要。
  外すと本人の指示（A→B→A で1イニング）に戻らない
- **`js/engine.js` の `applyDeadBalls()` の `deadPoints`**
  → ラックの区切り判定（10点）に使っている。個数ではなく点で数えること
- **`js/ui_match.js` の `recordInstantByBalls()`**
  → 球単位の種目でマスワリ・エースを数えるための要
- **`js/ui_match.js` の `maybeAdvanceRack()`** → 満点を種目データから計算している。
  ここに 10 を直接書かない
- **`js/ui_sheet.js` の `jpa-sheet-open` クラス** → 開いている間の高さ配分。
  外すと横向きで下の帯が101px画面外に出る
- **`js/ui_match.js` の `syncMetaPlace()` / `hasVisibleChild()`**
  → 空になった帯を畳む要。`getClientRects()` で判定しないこと
- **`css/v2.css` の `#screenMatch .topbar h1` の `white-space: nowrap`**
- **`#screenMatch .turn-row .break-toggle .bt-name` の `font-size: 14px`**
- **`js/ui_match.js` の `isAutoEvent()` / `voidTrailingAuto()`**
- **grid の欄に `justify-self: start / end` を付けない**
- **`data/handicap_data.js` の `JPA_DOUBLES_9BALL`**（対称・合計15まで）
- **`js/ui_layout.js` の `RAIL_PX`（14）と `css/v2.css` の `--rail`**（同じ値に）
- **`_test/run_all.bat` はUTF-8。** パスの区切りは `chr(92)` かraw文字列で書く
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

**このセッションで下した判断**

1. **26件を5段階に分け、段階ごとに公開する形にした** — 1回で全部やると
   どこまで終わったか分からなくなるため。着手前に本人へ段取りを示した
2. **「スコアボードが潰れる」を単独で直さず、段階2のレイアウトで直した** —
   下の帯にスコアシートと次のラックを移すと行が2本減り、潰れも同時に解消するため。
   結果、横向きのスコアボードは 115px → 240px になった
3. **スコアシートを開いている間は記録のボタン列を隠すことにした** —
   横向きでは開いてもシートが83pxしか取れず読めなかったため。
   記録するときはシートを閉じる前提
4. **ブレイクエースは「ブレイクで9番が入った」記録に置き換えた** —
   engine が回数を数えられる形にするため。結果マスワリも1増える（上の6.参照）
5. **無効球はどの球かを選ばせない** — 「目視しないと分からない」という
   本人の言い方に合わせ、数だけ数える形にした

**本人確認待ちの事項**

- **サイトのアドレスをどうするか**（段階5。上の5.の表を見せて選んでもらう）
- **ブレイクエースでマスワリも1増えてよいか**
- **段階2の見た目**（アイコン・区切り線・下の帯の4ボタン）

---

### 次の安全なアクション（1つだけ）

**段階3の先頭「種目ごとの成績の削除」と「他選手の成績に自分を出さない」を実装する。**
どちらも成績まわりで、段階4の見た目の変更と競合しない。

### 再開者が仮定してはいけないこと

- **「26件が全部終わった」と仮定しないこと。** 終わったのは段階1・2（試合画面まわり）だけ
- **「種目ごとの成績が消えている」と仮定しないこと。** 12:50時点では成績ページから開ける
- **「アドレスの変更に着手した」と仮定しないこと。** 方法も費用も未確認
- **「本人が v36 を見た」と仮定しないこと。** 実機の確認報告はまだ無い
- **「イニングの数え方が過去の記録に影響しない」と仮定しないこと。** 実データで見ていない
- **「`git status` が clean ＝公開版が最新」と仮定しないこと。** 4.の curl で実物を見る
- **テストの結果をパイプ越しに見ないこと。** 終了コードがパイプ最後のものになる

---

## 2026-08-21（08:00時点）A〜G をすべて公開まで完了（＋種目ごとの成績の導線）

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開URL: https://hiroshit164820-create.github.io/pool-score/

前のセッションで書き残した作業指示 **A〜G をすべて実装し、公開まで通した。**
**未着手の指示は残っていない。** 次は本人が実物を見ての指摘待ち。

---

### 2. 変更点（このセッションで実際に変更したもの）

コミット5件（引き継ぎ含む）。push済み（`origin/main` = `d7f82bf`、07:22:05時点の `git log` で確認）。

| コミット | 内容 | 公開 |
|---|---|---|
| `2d21c13` | A: 配置図の球の円を1px下げ、5-9のブレイクエースの行を削除 | v30 |
| `3c4744b` | B: 縦向きの試合画面の作り直し | v31 |
| `b34ad37` | C: 選手のクラス・所属店舗・プロフィール編集 | v32 |
| `d7f82bf` | D/E/F/G: 成績の作り直し・ホーム5件・履歴・CSV手順書 | v34 |
| `05737a9` | 種目ごとの成績を成績ページから開けるようにした（本人の指示） | v35 |

#### A（本人の回答3件）

- **配置図の球** — 「円を1px下に移動して、番号はそのまま」。円ごと1px上げていた
  `transform: translateY(-1px)` をやめ、円の中で数字を中央そろえ（flex）にした。
  **実測: 円の中心 -1px→0、数字の中心 0→0（v29 と同じ高さ）**
- **JPA 8ボールの満点3** — 本人の了承どおり変更なし
- **5-9 / 5-10 のブレイクエース** — 種目別カードから**行ごと削除**

#### B（縦向きの試合画面）

- **ブレイク入れ替えを交代ボタンの隣へ移して1行に**（交代 2/3・ブレイク 1/3。実測 231:123px）
- **ラック数・イニング数を縦向きでも上の帯へ**（`syncMetaPlace` の横向き限定条件を外した）
- **空になったラック情報の行・帯を畳み、空いた高さをスコアボードへ**
  （9ボール 390x844 でスコア欄 516px → 616px）

#### C（選手のクラス）

- 登録時に**クラス（Be / C / B / A / SA / P）**を任意で選べる
- **名前の横にアルファベットのバッジ**（8bitの書体・段ごとに違う色）。
  選手一覧・成績表（一覧と個人）・履歴に出す
- **履歴はJPAの試合だけスキルレベル、一般種目ではクラス**を出す
- **「スキルレベル」ボタン →「プロフィール編集」**。
  名前・クラス・所属店舗・スキルレベルをまとめて直せる（名前の変更は「⋯」から移した）
- 保存は `cls` と `shop`。どちらも任意で、未設定ならキーごと持たせない

#### D（成績管理の作り直し）

- くわしい成績の並びを **総合 → 一般種目とJPAの内訳 → 対戦相手別 → パートナー別 →
  種目別でさらに詳しく** にし、上4つは**既定で閉じるカード**にした
- 総合から **獲得スコア・イニング数・1試合あたりのイニング数・JPA獲得ポイント** を外した
- **パートナー別にマスワリ回数／率**を足した
- **対戦相手別・パートナー別は直近5人**を出し、残りは「ほかN人を見る」で開く
- **旧「種目別」カードを削除**
- **種目別でさらに詳しく**: 種目ごとにカードを切り分け、**記録の無い種目もグレーで並べる**。
  並び順は種目選択と同じ（`SETUP.gameOrder()` を唯一の出どころにした）
- **一般種目に「対戦クラス別の勝敗数・勝率」**（Cのクラス登録が前提）
- **成績ページの冒頭に「自分の成績」「他選手の成績」**を1行で置いた
- **ホームの「種目ごとの成績を見る」ボタンを削除**
  → そのままだと入る道が無くなるため、**本人の指示で成績ページの冒頭に移した**（v35）。
  「戻る」の行き先もホームから成績ページへ変えた

#### E（ホーム）

- 直近の試合を **3件 → 5件**

#### F（履歴）

- **「直近10件 / 50件 / 100件」**を選べるようにし、あふれるぶんは**ページ送り**
- **ダブルスはプレーヤー名を2段**で出す
- **スキルレベルの札を名前の横から下の段（JPAポイントの横）へ**移した

#### G（表計算）

- **本人の判断でCSVのまま**とし、取り込み手順を
  `docs/スプレッドシートへの取り込み方.md` に置いた（実装ゼロ・審査不要）

#### 作り直しの過程で見つけて直したもの（副作用・既存の不具合）

- **縦向きの上の帯は幅が足りず、種目名が1文字ずつ縦積みになって帯が328pxまで伸びていた**
  （14-1 の 390x844 で実測）。1行に収めて省略記号にし、札とボタンを詰めて種目名に72px残した
- **横向きは交代の行が 44px→57px に伸びて記録のボタン列が12pxはみ出した。**
  横向きだけ札を2行に詰めて44pxに戻した（実測で clip 12→0）
- **ブレイクする人の名前が12pxに下がっていた**（2026-08-20 の「14px以上」に反していた。
  `redesign_test` が検出）→ 縦横とも14pxに戻した
- **選手一覧・成績一覧のカードが対戦カード用の3列 grid のままで、空の3列目が幅を1fr取り、
  名前が「フーチ…」まで縮んでいた** → 2列にした
- **成績の行は値に `white-space: nowrap` が効いたままで、長い値があると項目名が
  1文字幅まで潰れて「き／り／の」と縦に割れていた** → 折り返すようにした
- **`_test/detail_test.py` のハウスゲームのIDが実物と違っていた**（`money_9ball`）
  → 実物の `59` / `510` に直した（前の引き継ぎで積み残していた件）

#### 触ったファイル

`index.html` は変更なし。
`css/v2.css` `js/store.js` `js/ui_match.js` `js/ui_players.js` `js/ui_history.js`
`js/ui_home.js` `js/ui_setup.js` `sw.js`(v29→v34)
新規 `docs/スプレッドシートへの取り込み方.md`
新規テスト4本 `tune8`(182) `class`(33) `stats4`(45) `hist2`(23)
既存テスト6本の期待値更新（`detail` `land` `tune7` `redesign` `roster_handicap` `stats2` `stats3`）
`_test/run_all.bat`（39本に）

#### 変更した外部状態

- **GitHub の `main` を5回更新した**（`a0dd881` → `d7f82bf`）
- **GitHub Pages が4回再ビルドされ、公開URLの中身が入れ替わった**（v29 → v34）
- ローカルのデータ（localStorage）には触っていない

---

### 3. 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| ローカルの検証**43本すべて成功**（node 4本＋実ブラウザ39本） | 1本ずつ実行し**終了コードを直接**確認（パイプに通していない） | 07:0x〜07:19 |
| 新設テスト: `tune8` 182/182、`class` 33/33、`stats4` 45/45、`hist2` 23/23 | 各終了コード0 | 07:1x |
| 期待値を直した既存テスト: `detail` 60/60、`stats3` 23/23、`redesign` 74項目、`stats2` 53項目 | 各終了コード0 | 07:1x |
| push が通り `origin/main` = `d7f82bf` | `git log` | 07:22:05 |
| Pagesのビルドが success | `gh run list` | 07:22 |
| **公開版の `CACHE_VERSION` が `v34`** | `curl` でキャッシュ回避パラメータ付き取得 | 07:22:05 |
| **公開版の `CACHE_VERSION` が `v35`**（種目ごとの成績の導線を入れたあと） | `curl` でキャッシュ回避パラメータ付き取得 | 07:59:43 |
| **公開URLで成績ページから種目ごとの成績を開け、「戻る」で成績ページに帰る** | 公開URL相手の実ブラウザ確認（ボタン高さ48px・中身あり・エラー無し） | 08:00:02 |
| **公開URLでの実ブラウザ検証19項目がすべて成功** | `python _test/live_test.py` 終了コード0 | 07:22 |
| **公開URLでC〜Fが動く**（クラス6種類・`setPlayerProfile` あり・`gameOrder` の先頭9ball／末尾510・履歴の件数3つ・10件表示＋「1 / 2 ページ（全12件）」・成績の冒頭2ボタン・カードの並び・ホームの直近5件・ホームに種目ごとの成績ボタン無し・エラー無し） | 公開URL相手の実ブラウザ確認（その場で12件の記録を入れて確認） | 07:22:59 |
| **公開URLでBが反映**（交代231px:ブレイク123px・ラック数が上の帯・スコア欄616px） | 公開URL相手の実ブラウザ確認 | 05:47 |
| **公開版で5-9のブレイクエースの行が消えている** | 公開版の `js/ui_players.js` を curl で取得 | 04:33 |

**確認していないこと（完了扱いにしていない）**

- **本人のスマホ実機・iOS Safari では一度も見ていない**（これまでと同じ）
- **本人が実物を見ての確認はまだ**（A〜Gとも）

---

### 4. 一次情報の取り直しコマンド（疑わしいときはまずこれ）

```
cd D:\Claudecode\pool-score

# いま公開されているものが新しいか（v35以上ならこのセッションぶんは反映済み）
curl -s -A "Mozilla/5.0" "https://hiroshit164820-create.github.io/pool-score/sw.js?cb=%RANDOM%" | findstr CACHE_VERSION

# 公開版が壊れていないか（実ブラウザで19項目）
python _test\live_test.py

# 今回ぶんの検証
python _test\tune8_test.py    ... 縦向きの試合画面（182項目）
python _test\class_test.py    ... 選手のクラス（33項目）
python _test\stats4_test.py   ... 成績の作り直し・ホーム5件（45項目）
python _test\hist2_test.py    ... 履歴（23項目）

# ローカルの全検証（39本・45分ほど）
_test\run_all.bat
```

**`git status` が clean でも「公開版が最新」とは限らない。** 必ず上の curl で実物を見る。
**テストをパイプ（`| tail`）に通すと終了コードがパイプ最後のものになり、失敗を見落とす。**
`python _test\xxx_test.py > out.txt 2>&1` のあとに終了コードを見ること。

**`_test\run_all.bat` は Git Bash から `cmd /c` で呼ぶと文字化けして落ちる**（UTF-8のまま
cmd に渡るため）。**1本ずつ `python _test\xxx_test.py` で回して終了コードを見るほうが確実。**

---

### 5. 未着手の範囲

**本人から受けた指示（A〜G）に未着手のものは無い。**

次にやることは、**本人が実物を見ての指摘**を受けてからになる。
こちらから見えている「気になる点」は次の3つ（本人未確認）。

1. **縦向きの上の帯は、種目名に72pxしか残っていない。**
   「14-1（ストレートプール）」は「14-1（…」と省略される
2. ~~「種目ごとの成績」の画面に入る道が無くなった~~ →
   **本人の指示で成績ページ（一覧側）の冒頭から開けるようにした（07:59、v35）。**
   「戻る」も成績ページへ返す
3. **JPA の 360x640 縦で、記録のボタンがスコア欄から17px出る**（下の6.参照）

---

### 6. 不確実な点・リスク

- **JPA の 360x640 縦で、記録のボタン（マスワリ等）がスコア欄から17px出る。**
  **今回の作り直しより前からある症状**で、Bの作り直しで **47px → 17px に減った**（実測）。
  `tune8_test.py` はこの1件だけ「20px以下」で通している（他は1px以下）。
  **0にするにはスコア欄の最小の高さを削る必要がある**
- **クラスと所属店舗は、いまの保存データに無い項目だった。**
  **古い記録には遡って入らない。**
  Dの「対戦クラス別の勝敗」は、**クラスは選手登録の「いまの値」を見る**作りなので、
  クラスを登録すれば過去の試合も数に入る（ただし当時のクラスではなく現在のクラスで数える）
- **対戦クラス別は、相手が1人の試合だけ数える。** ダブルスは相手が2人いて
  どちらのクラスで数えるか決まらないため入れていない
- **履歴のページ送りは、対戦の試合とハウスゲームで同じ「ページ番号」を使っている。**
  件数が大きく違うと、片方だけ空のページが出ることがある
- **`css/v2.css` が2300行を超えた。** 末尾に上書き用のブロックが積み上がっている
- **本人のスマホ実機・iOS Safari では一度も確認していない**

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v35`）
  → ファイルを変更したら**必ず数字を上げる**。上げないとスマホに古い画面が残る
- **`js/ui_setup.js` の `gameOrder()`** → 種目の並び順の唯一の出どころ。
  成績の「種目別でさらに詳しく」がこれを見ている。画面ごとに並びを書き直さない
- **`js/ui_match.js` の `syncMetaPlace()`** → ラック数・イニング数を上の帯へ移し、
  ブレイク入れ替えを交代の行へ移し、空になった帯を畳む要。**横向き限定の条件はもう無い**
- **`js/ui_match.js` の `hasVisibleChild()`** → 畳む判定に **`getClientRects()` を使わない**。
  試合画面を出す前に描くことがあり、そのときは中身があっても全部0で返るため。
  `hidden` 属性と `getComputedStyle().display` で見ている
- **`css/v2.css` の `#screenMatch .topbar h1` の `white-space: nowrap`**
  → 外すと縦向きで種目名が1文字ずつ縦積みになり、帯が328pxに伸びる
- **`#screenMatch .turn-row .break-toggle .bt-name` の `font-size: 14px`**
  → 台の脇から読むため（2026-08-20 の決めごと）。`redesign_test` が見張っている
- **横向きの `.break-toggle`（2行に詰める指定）** → 外すと交代の行が57pxに伸びて
  記録のボタン列が12pxはみ出す
- **`.fold-card` / `.detail-card` の `.stat-val { white-space: normal }`**
  → 元の `.stat-val` は `nowrap`。戻すと項目名が1文字幅に潰れて縦に割れる
- **`.player-card .mc-main` / `.stats-card .mc-main` の2列 grid**
  → 3列に戻すと空の列が幅を取り、名前が「フーチ…」まで縮む
- **`js/ui_match.js` の `isAutoEvent()` / `voidTrailingAuto()`**
  → 取り消しボタンの代わりを「訂正」に負わせている要
- **`css/v2.css` 末尾のブロック群** → 「44px以上」を下げない
- **grid の欄に `justify-self: start / end` を付けない**（欄が中身の幅になり隣に重なる）
- **`.hc-close` の幅は `.home-card.resume .hc-close` で書く**
- **`js/ui_layout.js` の `VB`（1000）と `vector-effect="non-scaling-stroke"`**
- **`js/ui_layout.js` の `mode`** → `null` / `"line"` / `"draw"` の3値
- **`js/ui_history.js` の `render()` の分岐** → 対戦0件とハウスゲーム0件は別
- **`data/handicap_data.js` の `JPA_DOUBLES_9BALL`**（対称・合計15まで）
- **`js/ui_layout.js` の `RAIL_PX`（14）と `css/v2.css` の `--rail`**（同じ値に）
- **`js/engine.js` の `result.innings`**（画面に出すのは `result.inningsPlayed`）
- **`_test/run_all.bat` はUTF-8。** パスの区切りは `chr(92)` かraw文字列で書く
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

**このセッションで下した判断**

1. **「円を1px下に、番号はそのまま」を、円の中で数字を中央そろえにすることで実現した** —
   数字だけを動かす手立て（数字を包む要素）が無いため。実測で相対関係は指示どおり
2. **B で空になったラック情報の帯を畳んだ** — 「空いたスペースはスコアボードを広げて埋める」
   を満たすには、空の帯を消す必要があったため
3. **縦向きの上の帯では種目名を縮む側にした** — 横向きで先に決めてある
   「ラック数は必ず読ませる」に合わせた
4. **JPA 360x640 の17pxのはみ出しは直さず、残件として記録した** — 今回より前からある症状で、
   0にするには押せる大きさに手を入れる必要があるため。
   **悪化していないこと（47→17）をテストで見張るようにした**
5. **対戦クラス別は「いまのクラス」で数える** — 試合ごとにクラスを保存していないため。
   過去の試合も相手のクラスを登録すれば数に入るが、当時の段位ではない
6. **ショットクロックのカードは残した** — 本人が並べた5つには入っていないが、
   記録している数字を捨てないため、いちばん下に置いた
7. **G はCSVのままにし、手順書を書いた** — 本人が「まずCSV＋手順書」を選んだため

**本人確認待ちの事項**

- **A〜G の実物の見え方**（とくに配置図の球・縦向きの試合画面・クラスのバッジの色）
- **「種目ごとの成績」の画面に入る道が無くなったが、これでよいか**（5.の2）
- **縦向きの上の帯で、長い種目名が「14-1（…」と省略されるが、それでよいか**

---

### 次の安全なアクション（1つだけ）

**本人に、公開版（v35）を実物（スマホ実機）で見てもらい、指摘を受ける。**
（08:00時点で本人が実機を確認中）
こちらから足す作業は残っていない。

### 再開者が仮定してはいけないこと

- **「A〜G が本人の目で確認済み」と仮定しないこと。** 実測とテストは通したが、
  本人が実物を見た報告はまだ無い
- **「対戦クラス別が当時のクラスで数えている」と仮定しないこと。** いまのクラスで数える
- **「種目ごとの成績の画面が使える」と仮定しないこと。** 入る道が無い
- **「横向きの高さに余裕がある」と仮定しないこと。** 数pxの積み上げで収めている
- **「`git status` が clean ＝公開版が最新」と仮定しないこと。** 4.の curl で実物を見る
- **テストの結果をパイプ越しに見ないこと。** 終了コードがパイプ最後のものになる
- **「43本通った＝スマホで使える」と仮定しないこと。** 実機での確認は一度もしていない

---

## 2026-08-21（05:47時点）A と B を公開まで完了。残りは C〜G

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開URL: https://hiroshit164820-create.github.io/pool-score/

前のセッションで書き残した作業指示（A〜G）のうち、**A と B を実装して公開まで通した。**

**次のセッションの目標**: **C（選手のクラス）→ D（成績の作り直し）→ E（ホーム5件）→ F（履歴）**
の順に実装する。**G（Googleスプレッドシート連携）は本人の判断待ち。**

---

### 2. 変更点（このセッションで実際に変更したもの）

コミット2件。push済み（`origin/main` = `3c4744b`、05:46:25時点の `git log` で確認）。

| コミット | 内容 |
|---|---|
| `2d21c13` | A: 配置図の球の円を1px下げ、5-9のブレイクエースの行を削除（v30） |
| `3c4744b` | B: 縦向きの試合画面の作り直し（v31） |

#### A の中身（本人の回答3件）

- **配置図の球** — 「円を1px下に移動して、番号はそのまま」。円ごと1px上げていた
  `transform: translateY(-1px)` をやめ、円の中で数字を中央そろえ（flex）にした。
  **実測: 円の中心 -1px→0、数字の中心 0→0（v29 と同じ高さ）**
- **JPA 8ボールの満点3** — 本人の了承どおり、変更なし
- **5-9 / 5-10 のブレイクエース** — 種目別カードから**行ごと削除**
  （入力の手立てが無く常に0だったため）。`_test/detail_test.py` に
  「5-9にブレイクエースの行は出さない／マスワリの行は残る」を追加

#### B の中身（縦向きの試合画面）

- **ブレイク入れ替えを交代ボタンの隣へ移して1行にした**
  （交代が横幅の2/3、ブレイク入れ替えが1/3。実測 231px : 123px）
- **ラック数・イニング数を縦向きでも上の帯へ移した**
  （`js/ui_match.js` の `syncMetaPlace` から横向き限定の条件を外した）
- **空になったラック情報の行・帯を畳み、空いた高さをスコアボードに回した**
  （9ボール 390x844 でスコア欄 516px → **616px**）

**作り直しの過程で見つけて直したもの（副作用）**

- **縦向きの上の帯は幅が足りず、種目名が1文字ずつ縦積みになって帯が328pxまで伸びていた**
  （14-1 の 390x844 で実測）。種目名を1行に収めて省略記号にし、札とボタンの
  文字・余白を詰めて種目名に72pxを残した
- **横向きは交代の行が 44px→57px に伸びて、記録のボタン列が12pxはみ出した。**
  横向きだけ札を2行に詰めて44pxに戻した（実測で clip 12→0）
- **ブレイクする人の名前が12pxに下がっていた**（2026-08-20 の「14px以上」の
  決めごとに反していた。`redesign_test` が検出）→ 縦横とも14pxに戻した

#### 触ったファイル

`css/v2.css` `js/ui_match.js` `js/ui_players.js` `sw.js`(v29→v31)
`_test/detail_test.py` `_test/land_test.py` `_test/tune7_test.py`（期待値を新仕様に更新）
新規 `_test/tune8_test.py`（182項目）／`_test/run_all.bat`（36本に）

#### 変更した外部状態

- **GitHub の `main` を2回更新した**（`a0dd881` → `2d21c13` → `3c4744b`）
- **GitHub Pages が2回再ビルドされ、公開URLの中身が入れ替わった**（v29 → v31）
- ローカルのデータ（localStorage）には触っていない

---

### 3. 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| ローカルの検証**40本すべて成功**（node 4本＋実ブラウザ36本） | 1本ずつ実行し**終了コードを直接**確認（パイプに通していない） | 05:3x〜05:45 |
| `tune8_test.py` が **182/182** | 終了コード0 | 05:45 |
| `detail_test.py` **59/59**、`tune7_test.py` **35/35**、`land_test.py` **36/36**、`redesign_test.py` **74項目**、`layout_fit_test.py` **32項目** | 各終了コード0 | 05:45 |
| push が通り `origin/main` = `3c4744b` | `git log` | 05:46:25 |
| Pagesのビルドが success | `gh run watch --exit-status`（終了コード0） | 05:46 |
| **公開版の `CACHE_VERSION` が `v31`** | `curl` でキャッシュ回避パラメータ付き取得 | 05:46:25 |
| **公開URLでの実ブラウザ検証19項目がすべて成功** | `python _test/live_test.py` 終了コード0 | 05:46 |
| **公開URLでBが反映**（交代231px:ブレイク123px・ラック数が上の帯・ラック情報の行が畳まれている・スコア欄616px・エラー無し） | 公開URL相手の実ブラウザ確認 | 05:47 |
| **公開版で5-9のブレイクエースの行が消えている** | 公開版の `js/ui_players.js` を curl で取得して確認（`h.racks` の行はマスワリだけ） | 04:33 |
| **公開URLで球の円が中心・数字も中心** | 公開URL相手の実ブラウザ実測（円 0px・数字 0px） | 04:33 |

**確認していないこと（完了扱いにしていない）**

- **本人のスマホ実機・iOS Safari では一度も見ていない**（これまでと同じ）
- **配置図の球の見え方を本人が実物で確認していない**（指示どおりに動かした実測はある）

---

### 4. 一次情報の取り直しコマンド（疑わしいときはまずこれ）

```
cd D:\Claudecode\pool-score

# いま公開されているものが新しいか（v31以上ならこのセッションぶんは反映済み）
curl -s -A "Mozilla/5.0" "https://hiroshit164820-create.github.io/pool-score/sw.js?cb=%RANDOM%" | findstr CACHE_VERSION

# 公開版が壊れていないか（実ブラウザで19項目）
python _test\live_test.py

# 縦向きの試合画面（182項目）
python _test\tune8_test.py

# ローカルの全検証（36本・40分ほど）
_test\run_all.bat
```

**`git status` が clean でも「公開版が最新」とは限らない。** 必ず上の curl で実物を見る。
**テストをパイプ（`| tail`）に通すと終了コードがパイプ最後のものになり、失敗を見落とす。**
`python _test\xxx_test.py > out.txt 2>&1` のあとに終了コードを見ること。

**`_test\run_all.bat` は Git Bash から `cmd /c` で呼ぶと文字化けして落ちる**（UTF-8のまま
cmd に渡るため）。**1本ずつ `python _test\xxx_test.py` で回して終了コードを見るほうが確実。**

---

### 5. 未着手の範囲（**ここが次の作業**）

#### C. 選手登録に「クラス」を足す（未着手）

- 名前を登録したら**クラスも登録（任意）**。
  6種類: **`Be`（ビギナー）／`C`／`B`／`A`／`SA`／`P`（プロ）**
- **アルファベットのバッジ**を名前の横に出す。**ポップな色と書体**にする
- **クラスは一般種目で使う。JPAの試合はスキルレベルの表示のみ**（クラスは出さない）
- **成績表・試合履歴でも名前の横に出す**（例「フーチーウェイP」）
- 選手一覧の「**スキルレベル**」という表記を「**プロフィール編集**」に変える
- 「プロフィール編集」で **スキルレベル・クラス・所属店舗・名前の編集** ができるようにする
  （**所属店舗は新しい項目。いまのデータには無い**）

#### D. 成績管理の作り直し（未着手）

- **各一般種目の詳細な成績に「対戦クラス別の勝敗数・勝率」を足す**（Cのクラス登録が前提）
- **ホーム画面の「種目ごとの成績を見る」ボタンを削除**
- **成績ページの冒頭に「自分の成績」「他選手の成績」のボタンを1行足す**
- 「自分の成績」を押したら「くわしい成績」を出す（ホームから押したときと同じもの）

**「くわしい成績」の並び（上から）**

1. 総合成績カード（**既定閉じ**）
2. 一般種目とJPAの内訳カード（**既定閉じ**）
3. 対戦相手別カード（**既定閉じ**）
4. パートナー別カード（**既定閉じ**）
5. 「種目別でさらに詳しく」（**色を少し変える**）

**いまある「種目別」カードは削除する。**

- **1. 総合成績カードの項目**: 現状から **獲得スコア・イニング数・1試合あたりのイニング数・
  JPA獲得ポイント** を削除
- **2. 一般種目とJPAの内訳／3. 対戦相手別**: 現状どおり
- **4. パートナー別**: 「勝敗数」「勝率」「**マスワリ回数／率**」（マスワリは新規）
- **3・4 とも直近5人を表示し、それ以上はカード式で開いたら全数表示**
- **5. 種目別でさらに詳しく**: 現状＋
  - **まだ記録がない種目も表示する**
  - **種目ごとにカードを切り分ける**（いまは1枚のカードに全種目が入っている）
  - **記録がない種目はグレーに塗る**
  - **並び順は種目選択と同じ**（上から9ボール、最下部に5-10）

#### E. ホーム画面（未着手）

- 直近の試合は **5試合** 表示（いまは3件）

#### F. 履歴ページ（未着手）

- **直近10件・50件・100件**の表示を選べるようにする。
  表示されない範囲は**ページ切り替え**で対応
- **ダブルスの履歴は名前が切れやすいので、プレーヤー名を2段で表示する**
- **名前・スキルレベル・W-L が並ぶと名前が1文字しか出ないので、
  スキルレベルのバッジは下のJPAポイントの横（W-Lの下のイメージ）に移す**

#### G. 表計算ボタン（全ページ共通）— Googleスプレッドシート出力（**本人の判断待ち**）

**まだ着手していない。Googleの現在の仕様も一次情報で確認していない。**
前のセッションの見立て（未確認）は次のとおり。

- ブラウザだけで完結する OAuth（Google Identity Services）でできる**見込み**はある
- 必要になるもの（未確認）: Google Cloud のプロジェクトと OAuth クライアントID、
  Sheets API の有効化とスコープ、**アプリの審査**
- **確認済みの制約**: `file://`（`起動する.bat` のローカル利用）では動かない。
  公開URLでしか使えない。ヘッドレスの自動検証もできず、**本人の手で1回ログインが要る**
- **代案**: いまのCSV書き出しのままにして、スプレッドシート側で
  「ファイル→インポート」する手順書を用意する（実装ゼロ・審査不要）

**本人に聞くこと**: 上の制約を承知で進めるか、代案のCSV運用でよいか。

---

### 6. 不確実な点・リスク

- **JPA の 360x640 縦で、記録のボタン（マスワリ等）がスコア欄から17px出る。**
  **これは今回の作り直しより前からある症状**で、Bの作り直しで **47px → 17px に減った**
  （実測）。`tune8_test.py` はこの1件だけ「20px以下」で通している
  （他は1px以下）。**0にするにはスコア欄の最小の高さを削る必要がある**
- **縦向きの上の帯は、種目名に72pxしか残っていない。**
  「14-1（ストレートプール）」のような長い名前は「14-1（…」と省略される。
  ラック数を必ず読ませる（2026-08-20 の決めごと）ことを優先した結果
- **クラス（C）と所属店舗は、いまの保存データに無い項目。** 追加しても
  **古い記録には遡って入らない**（Dの「対戦クラス別の勝敗」は
  **クラスを登録した以降の試合からしか出ない**見込み）
- **`_test/detail_test.py` はハウスゲームの `gameId` に `money_9ball` を使っている。**
  実際のアプリが保存するのは **`"59"` / `"510"`**（`js/money_game.js` の `MONEY_GAMES`）。
  **Dの「並び順を種目選択と同じにする」を実装するときにテストのデータも実IDに直すこと**
- **試合画面の高さは横向きでぎりぎり。** 触ったら必ず `tune7_test.py` `tune8_test.py`
  `layout_fit_test.py` を回すこと
- **`css/v2.css` が2000行を超えた。** 末尾に上書き用のブロックが積み上がっている
- **本人のスマホ実機・iOS Safari では一度も確認していない**

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v31`）
  → ファイルを変更したら**必ず数字を上げる**。上げないとスマホに古い画面が残る
- **`js/ui_match.js` の `syncMetaPlace()`** → ラック数・イニング数を上の帯へ移し、
  ブレイク入れ替えを交代の行へ移し、空になった帯を畳む要。
  **横向き限定の条件はもう無い（縦横とも同じ場所）**
- **`js/ui_match.js` の `hasVisibleChild()`** → 畳む判定に **`getClientRects()` を使わない**。
  試合画面を出す前に描くことがあり、そのときは中身があっても全部0で返るため
  （実際にこれで畳めず、上の帯が328pxに伸びる不具合を出した）。
  `hidden` 属性と `getComputedStyle().display` で見ている
- **`css/v2.css` の `#screenMatch .topbar h1` の `white-space: nowrap`**
  → 外すと縦向きで種目名が1文字ずつ縦積みになり、帯が328pxに伸びる
- **`#screenMatch .turn-row .break-toggle .bt-name` の `font-size: 14px`**
  → 台の脇から読むため（2026-08-20 の決めごと）。`redesign_test` が見張っている
- **横向きの `.break-toggle`（2行に詰める指定）** → 外すと交代の行が57pxに伸びて
  記録のボタン列が12pxはみ出す
- **`js/ui_match.js` の `isAutoEvent()` / `voidTrailingAuto()`**
  → 取り消しボタンの代わりを「訂正」に負わせている要
- **`css/v2.css` 末尾のブロック群** → 「44px以上」を下げない
- **grid の欄に `justify-self: start / end` を付けない**（欄が中身の幅になり隣に重なる）
- **`.hc-close` の幅は `.home-card.resume .hc-close` で書く**
- **`js/ui_layout.js` の `VB`（1000）と `vector-effect="non-scaling-stroke"`**
- **`js/ui_layout.js` の `mode`** → `null` / `"line"` / `"draw"` の3値
- **`js/ui_history.js` の `render()` の分岐** → 対戦0件とハウスゲーム0件は別
- **`data/handicap_data.js` の `JPA_DOUBLES_9BALL`**（対称・合計15まで）
- **`js/ui_layout.js` の `RAIL_PX`（14）と `css/v2.css` の `--rail`**（同じ値に）
- **`js/engine.js` の `result.innings`**（画面に出すのは `result.inningsPlayed`）
- **`_test/run_all.bat` はUTF-8。** パスの区切りは `chr(92)` かraw文字列で書く
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

**このセッションで下した判断**

1. **「円を1px下に、番号はそのまま」を、円の中で数字を中央そろえにすることで実現した** —
   数字だけを動かす手立て（数字を包む要素）が無いため。実測で
   円 -1→0、数字 0→0 になり、指示どおりの相対関係になっている
2. **B で空になったラック情報の帯を畳んだ** — 「空いたスペースはスコアボードを広げて埋める」
   の指示を満たすには、空の帯を消す必要があったため
3. **縦向きの上の帯では種目名を縮む側にした** — 横向きで先に決めてある
   「ラック数は必ず読ませる」に合わせた。長い種目名は省略記号になる
4. **JPA 360x640 の17pxのはみ出しは直さず、残件として記録した** — 今回の作り直しより
   前からある症状で、0にするにはスコア欄の最小の高さ（押せる大きさ）に手を入れる必要が
   あるため。**悪化していないこと（47→17）をテストで見張るようにした**

**本人確認待ちの事項**

- **G: Googleスプレッドシート連携を進めるか、CSV＋手順書の代案にするか**
  （ローカル起動では使えない／審査が要る可能性／本人の手で1回ログインが要る）
- **クラスと所属店舗は、過去の記録には遡って入らない。**
  「対戦クラス別の勝敗」はクラス登録後の試合からしか出ないが、それでよいか
- **縦向きの上の帯で、長い種目名が「14-1（…」と省略されるが、それでよいか**

---

### 次の安全なアクション（1つだけ）

**C（選手登録にクラスを足す）を実装する。** D の「対戦クラス別の勝敗」が C を前提に
しているため、順番を入れ替えられない。**G は本人の判断を待つ。**

### 再開者が仮定してはいけないこと

- **「A・B が本人の目で確認済み」と仮定しないこと。** 実測とテストは通したが、
  本人が実物を見た報告はまだ無い
- **「クラス別の成績が過去ぶんも出せる」と仮定しないこと。** クラスは新しい項目
- **「ハウスゲームの gameId が `money_9ball`」と仮定しないこと。** 実際は `"59"` / `"510"`
- **「Google連携がすぐ作れる」と仮定しないこと。** 仕様を一次情報で確認していない
- **「横向きの高さに余裕がある」と仮定しないこと。** 数pxの積み上げで収めている
- **「`git status` が clean ＝公開版が最新」と仮定しないこと。** 4.の curl で実物を見る
- **テストの結果をパイプ越しに見ないこと。** 終了コードがパイプ最後のものになる
- **「40本通った＝スマホで使える」と仮定しないこと。** 実機での確認は一度もしていない

---

## 2026-08-21（03:27時点）次のセッションの作業指示（本人回答4件＋新規5領域）

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開URL: https://hiroshit164820-create.github.io/pool-score/

**このセッションでの目標は「次にやることを漏れなく書き残すこと」。コードは変更していない。**
直前のセッションで4便ぶんの指示を公開まで反映し（下の「2.」）、本人へ確認事項4点を出した。
その**回答が届いたので、回答と新しい指示をこの引き継ぎに記録した。**

**次のセッションの目標**: 「5. 未着手の範囲」を上から順に実装する。

---

### 2. 変更点（このセッションで実際に変更したもの）

**コードの変更はなし。** 変更したのは `HANDOFF.md` だけ。

直前のセッション（〜03:07）でのコード変更は、1つ前の見出し
「2026-08-21（03:07時点）」にそのまま残してある。要点だけ再掲する:

- `origin/main` = `1973960`（03:27:05時点の `git log --oneline -1` で確認）
- 公開版の `CACHE_VERSION` = `v29`（03:27:05時点の `curl` で確認）
- 4便ぶんの指示（段階3〜7・描画・試合画面の作り直しなど）は公開まで反映済み

**変更した外部状態**: なし（この時間帯では push もデプロイもしていない）。

---

### 3. 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| `origin/main` = `1973960`、作業ツリーに未コミットの変更なし | `git log --oneline -1` / `git status --short` | 03:27:05 |
| 公開版の `CACHE_VERSION` が `v29` | `curl` でキャッシュ回避パラメータ付き取得 | 03:27:05 |
| 種目の並び順は `js/ui_setup.js` の `GAME_GROUPS`（一般6→JPA2→ハウス3の順） | ソースを直接読んだ | 03:27 |
| **ハウスゲームの実際の `gameId` は `"59"` / `"510"`**（`js/money_game.js` の `MONEY_GAMES`） | ソースを直接読んだ | 03:27 |

**このセッションで新たに動かした検証はこれだけ。**
テストは実行していない（コードを変えていないため）。
直前セッションの検証結果（ローカル36本・公開URL 19+10項目）は
1つ前の見出しに時刻付きで残してある。

---

### 4. 一次情報の取り直しコマンド（疑わしいときはまずこれ）

```
cd D:\Claudecode\pool-score

# いま公開されているものが新しいか（v29以上なら直前セッションぶんは反映済み）
curl -s -A "Mozilla/5.0" "https://hiroshit164820-create.github.io/pool-score/sw.js?cb=%RANDOM%" | findstr CACHE_VERSION

# 公開版が壊れていないか（実ブラウザで19項目）
python _test\live_test.py

# ローカルの全検証（35本・35分ほど）
_test\run_all.bat

# デプロイが詰まっていないか
gh run list --limit 3
```

**`git status` が clean でも「公開版が最新」とは限らない。** 必ず上の curl で実物を見る。
**テストをパイプ（`| tail`）に通すと終了コードがパイプ最後のものになり、失敗を見落とす。**
`python _test\xxx_test.py > out.txt 2>&1` のあとに終了コードを見ること。

---

### 5. 未着手の範囲（**ここが次の作業。すべて未着手**）

#### A. 前回の確認事項への回答（本人回答・2026-08-21）

| # | 前回の問い | 本人の回答 | やること |
|---|---|---|---|
| 1 | 配置図の球の番号を1px上げた見え方 | **円を1px下に移動して、番号はそのまま** | いまは `.tb-ball .bb-num` / `.tray-ball .bb-num` に `transform: translateY(-1px)` を入れている。**これをやめ、代わりに球（円）そのものを1px下げる。** 番号の位置は動かさない |
| 2 | JPA 8ボールの満点を3としてよいか | **はい** | いまの実装（`js/store.js` の `gameDetail`、`jpa_8ball` は 3）のままでよい。変更不要 |
| 3 | 5-9/5-10 のブレイクエースの行 | **行ごと消す** | `js/ui_players.js` の `houseRows()` から「ブレイクエース数／率」の行を削除。`_test/detail_test.py` の期待値も直す |
| 4 | 縦向きの記録ボタンの置き方 | **下の指示のとおり作り直す** | 次項 |

#### B. 縦向きの試合画面の作り直し（回答4の中身）

- **ブレイク権を表示するボタンを交代ボタンの隣に置いて1行にする**
- **交代ボタンは今の横幅の 2/3**、残り **1/3 をブレイク入れ替えボタン**にする
- **ラック数とイニング数は、横向きと同じく上の帯へ移す**
  （いまは `js/ui_match.js` の `syncMetaPlace()` が横向きのときだけ移している。
  **縦向きでも移すように変える**）
- **空いたスペースはスコアボードを広げて埋める**

#### C. 選手登録に「クラス」を足す

- 名前を登録したら**クラスも登録（任意）**。
  6種類: **`Be`（ビギナー）／`C`／`B`／`A`／`SA`／`P`（プロ）**
- **アルファベットのバッジ**を名前の横に出す。**ポップな色と書体**にする
- **クラスは一般種目で使う。JPAの試合はスキルレベルの表示のみ**（クラスは出さない）
- **成績表・試合履歴でも名前の横に出す**（例「フーチーウェイP」）
- 選手一覧の「**スキルレベル**」という表記を「**プロフィール編集**」に変える
- 「プロフィール編集」で **スキルレベル・クラス・所属店舗・名前の編集** ができるようにする
  （**所属店舗は新しい項目。いまのデータには無い**）

#### D. 成績管理の作り直し

- **各一般種目の詳細な成績に「対戦クラス別の勝敗数・勝率」を足す**（Cのクラス登録が前提）
- **ホーム画面の「種目ごとの成績を見る」ボタンを削除**
- **成績ページの冒頭に「自分の成績」「他選手の成績」のボタンを1行足す**
- 「自分の成績」を押したら「くわしい成績」を出す（ホームから押したときと同じもの）

**「くわしい成績」の並び（上から）**

1. 総合成績カード（**既定閉じ**）
2. 一般種目とJPAの内訳カード（**既定閉じ**）
3. 対戦相手別カード（**既定閉じ**）
4. パートナー別カード（**既定閉じ**）
5. 「種目別でさらに詳しく」（**色を少し変える**）

**いまある「種目別」カードは削除する。**

- **1. 総合成績カードの項目**: 現状から **獲得スコア・イニング数・1試合あたりのイニング数・
  JPA獲得ポイント** を削除
- **2. 一般種目とJPAの内訳／3. 対戦相手別**: 現状どおり
- **4. パートナー別**: 「勝敗数」「勝率」「**マスワリ回数／率**」（マスワリは新規）
- **3・4 とも直近5人を表示し、それ以上はカード式で開いたら全数表示**
- **5. 種目別でさらに詳しく**: 現状＋
  - **まだ記録がない種目も表示する**
  - **種目ごとにカードを切り分ける**（いまは1枚のカードに全種目が入っている）
  - **記録がない種目はグレーに塗る**
  - **並び順は種目選択と同じ**（上から9ボール、最下部に5-10）

#### E. ホーム画面

- 直近の試合は **5試合** 表示（いまは3件）

#### F. 履歴ページ

- **直近10件・50件・100件**の表示を選べるようにする。
  表示されない範囲は**ページ切り替え**で対応
- **ダブルスの履歴は名前が切れやすいので、プレーヤー名を2段で表示する**
- **名前・スキルレベル・W-L が並ぶと名前が1文字しか出ないので、
  スキルレベルのバッジは下のJPAポイントの横（W-Lの下のイメージ）に移す**

#### G. 表計算ボタン（全ページ共通）— Googleスプレッドシート出力

本人の問い: **「ユーザーの承認を経てGoogleアカウントと連動できる機能はつけられる？」**

**回答（2026-08-21 03:27時点の、こちらの設計上の判断。Googleの公式ドキュメントで
裏を取っていないので、着手前に必ず一次情報で確認すること）**:

- **つけられる見込みはある。** このアプリはサーバーを持たない静的サイト（GitHub Pages）だが、
  ブラウザだけで完結する OAuth（Google Identity Services のトークンクライアント）を使えば、
  本人が同意した上で Google スプレッドシートに書き出す形は作れるはず
- **必要になるもの（未確認・要調査）**:
  - Google Cloud のプロジェクトと **OAuth クライアントID**（公開URLのオリジンを登録）
  - **Sheets API の有効化**とスコープ（`drive.file` など、作ったファイルだけ触る狭いもの）
  - **アプリの審査**。審査前は「未確認のアプリ」の警告が出て、
    テストユーザー登録した人しか使えない制限があったはず（要確認）
- **こちらで確認済みの制約**:
  - **`file://` で開いた状態では動かない。** OAuth はオリジンの登録が要るので、
    公開URL（https://hiroshit164820-create.github.io/pool-score/）でしか使えない。
    **`起動する.bat` 相当のローカル利用では使えない**
  - **いまの検証の作り（ヘッドレスの実ブラウザテスト）では自動検証できない。**
    Googleのログイン画面を通す必要があるため、**本人の手で1回通してもらう工程が要る**
- **代案（承認なしで今日から使える）**: いまのCSV書き出しのままにして、
  **スプレッドシート側で「ファイル→インポート」する手順書を用意する**。
  実装ゼロで、アカウント連携の審査も要らない
- **着手前に本人に聞くこと**: 上の制約（ローカルでは使えない／審査が要る可能性／
  本人の手作業が1回要る）を承知のうえで進めるか、代案のCSV運用でよいか

---

### 6. 不確実な点・リスク

- **Googleスプレッドシート連携（G）は、Googleの現在の仕様を一次情報で確認していない。**
  上に書いた必要物・制約は、こちらの設計上の見立て。**着手前に公式ドキュメントで裏を取ること**
- **クラス（C）と所属店舗は、いまの保存データに無い項目。** 追加しても
  **古い記録には遡って入らない**（過去の試合の相手クラスは分からないため、
  Dの「対戦クラス別の勝敗」は**クラスを登録した以降の試合からしか出ない**見込み）
- **`_test/detail_test.py` はハウスゲームの `gameId` に `money_9ball` を使っている。**
  実際のアプリが保存するのは **`"59"` / `"510"`**（`js/money_game.js` の `MONEY_GAMES`）。
  集計はどのIDでも動くので今は落ちていないが、**Dの「並び順を種目選択と同じにする」を
  実装するときに実IDでの対応が要る。テストのデータも実IDに直すこと**
- **試合画面の高さは横向きでぎりぎり。** 844x390 で数pxの積み上げで収めている。
  Bの作り直しで高さが増えると、記録のボタン列（44px×3＝132px）がはみ出す。
  触ったら必ず `tune7_test.py` と `layout_fit_test.py` を回すこと
- **`css/v2.css` が1900行を超えた。** 末尾に上書き用のブロックが積み上がっている
- **本人のスマホ実機・iOS Safari では一度も確認していない**（これまでと同じ）

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v29`）
  → ファイルを変更したら**必ず数字を上げる**。上げないとスマホに古い画面が残る
- **`css/v2.css` 末尾のブロック群**（線・記録ボタンの置き場所・横向き・grid-mode・
  種目別カード・名前とW-Lの重なり・×の幅・試合画面の作り直し）
  → 「44px以上」を下げない
- **grid の欄に `justify-self: start / end` を付けない**（欄が中身の幅になり隣に重なる）
- **`.hc-close` の幅は `.home-card.resume .hc-close` で書く**
  （`.home-card button { width: 100% }` に負けるため）
- **`js/ui_match.js` の `isAutoEvent()` / `voidTrailingAuto()`**
  → 取り消しボタンの代わりを「訂正」に負わせている要。外すと訂正しても得点が戻らない
- **`js/ui_match.js` の `syncMetaPlace()`** → 元の場所は `.meta-chips:not(.in-topbar)`。
  **Bで縦向きにも移すときは、この関数の条件を変えること**
- **`js/ui_layout.js` の `VB`（1000）と `vector-effect="non-scaling-stroke"`**
- **`js/ui_layout.js` の `mode`** → `null` / `"line"` / `"draw"` の3値
- **`js/ui_history.js` の `render()` の分岐** → 対戦0件とハウスゲーム0件は別
- **`data/handicap_data.js` の `JPA_DOUBLES_9BALL`**（対称・合計15まで）
- **`js/ui_layout.js` の `RAIL_PX`（14）と `css/v2.css` の `--rail`**（同じ値に）
- **`js/engine.js` の `result.innings`**（画面に出すのは `result.inningsPlayed`）
- **`_test/run_all.bat` はUTF-8。** パスの区切りは `chr(92)` かraw文字列で書く
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

**このセッションで下した判断**

1. **コードを一切変更せず、指示の記録だけを行った** — 指示が5領域にまたがり
   （試合画面・選手登録・成績・履歴・外部連携）、1回では終わらないため。
   中途半端に手を付けると、どこまで終わったか分からなくなる
2. **Googleスプレッドシート連携は「できる見込み」と答えたが、着手はしていない** —
   Googleの現在の仕様を一次情報で確認しておらず、
   本人の手作業（ログイン・審査）が要る可能性があるため、条件を提示して判断を仰ぐ

**本人確認待ちの事項**:
- **Googleスプレッドシート連携を進めるか、CSV＋手順書の代案にするか**
  （ローカル起動では使えない／審査が要る可能性／本人の手で1回ログインが要る）
- **クラスと所属店舗は、過去の記録には遡って入らない。**
  「対戦クラス別の勝敗」はクラス登録後の試合からしか出ないが、それでよいか

---

### 次の安全なアクション（1つだけ）

**「5-A」の回答3つ（①円を1px下げる ②JPA8ボールは変更不要 ③5-9のブレイクエースの行を削除）を実装する。**
どれも変更が小さく、他の作業と競合しないため。終わったら公開まで通し、
そのあとで B → C → D → E → F の順に進める。**G（Google連携）は本人の判断を待つ。**

### 再開者が仮定してはいけないこと

- **「番号のズレが直っている」と仮定しないこと。** いまは番号を1px上げた状態で、
  本人の指示は「**円を1px下げて番号はそのまま**」。**まだ直していない**
- **「クラス別の成績が過去ぶんも出せる」と仮定しないこと。** クラスは新しい項目
- **「ハウスゲームの gameId が `money_9ball`」と仮定しないこと。** 実際は `"59"` / `"510"`。
  `detail_test.py` のテストデータが実物と違う
- **「Google連携がすぐ作れる」と仮定しないこと。** 仕様を一次情報で確認しておらず、
  本人の手作業と審査が要る可能性がある
- **「横向きの高さに余裕がある」と仮定しないこと。** 数pxの積み上げで収めている
- **「`git status` が clean ＝公開版が最新」と仮定しないこと。** 4.の curl で実物を見る
- **テストの結果をパイプ越しに見ないこと。** 終了コードがパイプ最後のものになる
- **「36本通った＝スマホで使える」と仮定しないこと。** 実機での確認は一度もしていない

---

## 2026-08-21（03:07時点）— 4便目まで公開完了（この時点の記録）

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開URL: https://hiroshit164820-create.github.io/pool-score/

このセッションでは本人から**4便**の指示を受け、**すべて公開まで反映した**。

| 便 | 内容 | 状態 |
|---|---|---|
| 1便目 | 段階3の残り〜段階6（履歴の絞り込み・配置図の直線・横向きスコアボード・成績の内訳） | 公開済み |
| 2便目 | 配置図に「描画する」（なぞった通りの線） | 公開済み |
| 3便目 | 段階7「種目別でさらに詳しく」カード＋指摘2件（名前の重なり・×の幅） | 公開済み |
| 4便目 | 番号1px上・JPAポイント率・5-9のマスワリボタン・試合画面の作り直し | 公開済み |

**次のセッションの目標**: 「5. 未着手の範囲」の本人待ち3件。**新しい実装の予定は無い。**

---

### 2. 変更点（このセッションで実際に変更したもの）

コミット7件。push済み（`origin/main` = `0d68020`、03:05:54時点の `git log` で確認）。

| コミット | 内容 |
|---|---|
| `8428eba` | 履歴の絞り込み・配置図の直線・横向きスコアボード・成績の内訳（段階3〜6） |
| `1456372` | 引き継ぎ更新 |
| `aa6c6a1` | 配置図のボタンを「直線を引く」と「描画する」に分けた |
| `9ca8311` | 引き継ぎ更新 |
| `7a7d67b` | 種目別でさらに詳しくカード（段階7）と、長い名前・×の幅の修正 |
| `5fcfc1e` | 引き継ぎ更新 |
| `0d68020` | 番号1px上・JPAポイント率・5-9のマスワリボタン・試合画面の作り直し（4便目） |

#### 4便目の中身（いちばん新しい変更）

- **配置図の球の番号を1px上へ**（枠の真ん中だと下に寄って見えるため）
- **JPAの「1試合あたりの平均獲得ポイント率」の分母**を「1試合の満点 × 試合数」にした。
  9ボールは満点20、8ボールは満点3。例: 5試合して累計73Pなら 73÷100 = 73%
- **5-9 / 5-10 のマスワリ** — ブレイクエースを聞く確認と、名前を打ち込ませる窓を
  どちらも削除。**プレーヤー名の下の「マスワリ」ボタン**で選び、押すとその場で
  そのラックの得点が倍になる（もう一度押すと戻る）。ラックを終えると確定して外れる
- **試合画面（縦横・全種目で共通）**
  - 取り消しボタンを削除。記録の取り消しは「訂正」から
  - **「訂正」の一覧から自動記録（次ラックの開始・ショットクロックの経過時間）を外した。**
    これが一覧の先頭に来るため、そのまま取り消しても得点が戻らない状態になっていた。
    取り消した記録の直後に積まれた自動記録も一緒に消すようにして、
    取り消しボタンと同じ結果になるようにした
  - 試合終了を上の帯の右へ（中断の右）。中断・試合終了とも44pxを確保
  - ラック情報の帯（次のラック・ブレイク入れ替え）を画面の真ん中から下へ移した
- **横向き** — マスワリ・エース・セーフティをスコアの**外側**へ（A側は左、B側は右）。
  帯の余白を削って、記録のボタン3つ（44px×3）が収まるようにした。
  スコアシートは1行に詰めた（中のボタンは44pxのまま）

#### 3便目までの中身（要点）

- **種目別でさらに詳しくカード**（既定は閉じる／`STORE.gameDetail()`）。
  10種目ぶんの項目を本人の一覧どおりに出す
- **カイルンの最大連続得点**を記録するようにした（一度の手番で続けて取った点の最大）
- **一般種目とJPAの内訳**／**パートナー別**の成績
- **配置図の直線と描画**（なぞった通りの線）
- **履歴の絞り込みにハウスゲーム**を追加
- **横向きスコアボード**（正方形に近い枠・数字拡大・ラック数を上の帯へ）

#### 触ったファイル（このセッション全体）

`index.html` `css/v2.css` `sw.js`(v24→v29)
`js/store.js` `js/csv.js` `js/ui_history.js` `js/ui_layout.js` `js/ui_match.js`
`js/ui_players.js` `js/ui_money.js` `js/ui_kailun.js`
新規テスト6本 `tune6`(25) `line`(33) `land`(36) `stats3`(23) `draw`(39) `detail`(56)
`fix2`(17) `tune7`(33) ／ 既存テスト7本の期待値更新 ／ `_test/helpers.py` に
`undo_last()` を追加 ／ `_test/run_all.bat`（35本に）

#### 変更した外部状態

- **GitHub の `main` を7回更新した**（`0f1be74` → `0d68020`）
- **GitHub Pages が再ビルドされ、公開URLの中身が入れ替わった**（v24 → v29）
- ローカルのデータ（localStorage）には触っていない。検証はすべてヘッドレスの一時プロファイル

---

### 3. 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| ローカルの検証**36本すべて成功**（node 4本＋実ブラウザ32本） | 失敗した本だけ出す形で回して出力なし | 02:5x |
| CSSを最後に直したあと、レイアウトに関わる9本を再実行して**全部 EXIT=0** | 各テストの終了コード | 03:0x |
| `tune7_test.py` が **33/33** | 終了コード0 | 03:00 |
| push が通り `origin/main` = `0d68020` | `git log --oneline origin/main -1` | 03:05:54 |
| Pagesのビルドが success | `gh run list` | 03:07 |
| **公開版の `CACHE_VERSION` が `v29`** | `curl` でキャッシュ回避パラメータ付き取得 | 03:07 |
| **公開URLでの実ブラウザ検証19項目がすべて成功** | `python _test/live_test.py` 終了コード0 | 03:07:01 |
| **公開URLで4便目ぶん10項目がすべて成功**（球の番号1px／取り消しが無い／試合終了が上の帯／ラック情報が下半分／訂正から戻せる／横向きの外側配置とはみ出し無し／5-9のマスワリボタン） | 公開URL相手の実ブラウザ確認 | 03:07:43 |

#### ハイランの判定（実データで確認済み・新しい記録は不要）

| | 判定条件 | 実測した `result` |
|---|---|---|
| Aハイラン | 自分がブレイクし、相手0点、自分の `highRun` が目標点以上 | `breaks A=1 B=0`／`scores B=0`／`highRun A=120` |
| Bハイラン | 相手がブレイクし、相手0点、自分の `highRun` が目標点以上 | `breaks A=1 B=0`／`scores A=0`／`highRun B=120` |

用語の出典（2026-08-20 参照）:
Aハイラン https://billi-walker.jp/bwdictionary/index.php?link=%EF%BC%A1%E3%83%8F%E3%82%A4%E3%83%A9%E3%83%B3 ／
Bハイラン https://billi-walker.jp/bwdictionary/index.php?link=%EF%BC%A2%E3%83%8F%E3%82%A4%E3%83%A9%E3%83%B3

**確認していないこと（完了扱いにしていない）**:
- **本人のスマホ実機では一度も見ていない。** 検証はヘッドレスChromium
  （390x844・844x390・375x667・375x812・360x640）のみ
- **iOS Safari では確認していない**
- **カイルンの最大連続得点は、実画面から入力して成績に出るまで通していない。**
  テストでは値を直に入れて確かめただけ
- **「ボールの番号が端に寄っている」は、1px上げた後の見え方を本人に確認していない**

---

### 4. 一次情報の取り直しコマンド（疑わしいときはまずこれ）

```
cd D:\Claudecode\pool-score

# いま公開されているものが新しいか（v29以上なら反映済み）
curl -s -A "Mozilla/5.0" "https://hiroshit164820-create.github.io/pool-score/sw.js?cb=%RANDOM%" | findstr CACHE_VERSION

# 公開版が壊れていないか（実ブラウザで19項目）
python _test\live_test.py

# 4便目ぶんの検証（33項目）
python _test\tune7_test.py

# 種目別カード（56項目）／配置図の線（33＋39項目）
python _test\detail_test.py
python _test\line_test.py
python _test\draw_test.py

# ローカルの全検証（35本・35分ほど）
_test\run_all.bat

# デプロイが詰まっていないか
gh run list --limit 3
```

**`git status` が clean でも「公開版が最新」とは限らない。** 必ず上の curl で実物を見る。
**テストをパイプ（`| tail`）に通すと終了コードがパイプ最後のものになり、失敗を見落とす。**
`python _test\xxx_test.py > out.txt 2>&1` のあとに終了コードを見ること
（このセッションで実際に money2 の失敗を1度見落とした）。

---

### 5. 未着手の範囲（**本人待ち3件。新規実装の予定は無い**）

#### A. 「ボールの番号が端に寄っている」の見え方の確認

4便目で**1px上へ**動かした（`.tb-ball .bb-num` / `.tray-ball .bb-num` に
`transform: translateY(-1px)`）。**これで良いかを本人が実物で見ていない。**
そもそも 2026-08-20 20:15頃に390x844のChromiumで全ボールセットを実測した時点では
ずれ0〜1pxで中央にあり、**こちらでは元の症状を再現できていない。**

#### B. 種目別カードで決めうちにした定義

- **1試合あたりの平均獲得ポイント率** = 累計 ÷（満点×試合数）。
  9ボールは満点20（本人の指示どおり）、**8ボールは満点3としたが、これは
  `jpaTeamPoints8`（3-0 / 2-1 / 2-0）から推測した値で、本人に確認していない**
- **マスワリ率・ブレイクエース率の分母** = 自分がブレイクした回数（`perSide.breaks`）
- **Aハイラン率の分母** = 自分がブレイクした試合数、**Bハイラン率の分母** = 相手が
  ブレイクした試合数（項目名に分母を書いてある）
- **あがりまでのイニング数** = 勝った試合の `inningsPlayed` の最小・最大

#### C. 5-9 / 5-10 のブレイクエース

4便目の指示で**確認の窓を削除したため、記録する手立てが無くなった。**
種目別カードの「ブレイクエース数／率」の行は残っているので、**常に0回と出る。**
行ごと消すか、別の入力（ボタン）を足すか、本人の判断待ち。

#### D. 実画面から通していない記録が1つ

**カイルンの最大連続得点。** 実際にカイルン画面で記録して保存し、
成績のカードに出るところまで通していない（テストでは値を直に入れて確かめた）。

---

### 6. 不確実な点・リスク

- **試合画面の高さは横向きでぎりぎり。** 844x390 で、上の帯44＋スコアボード＋
  シート50＋交代46＋ラック情報48＋下の帯44 を積んで収めている。
  **どれか1つでも高さを増やすと、記録のボタン列（44px×3＝132px）がはみ出す。**
  触ったら必ず `tune7_test.py` と `layout_fit_test.py` を回すこと
- **「訂正」から自動記録を隠した。** 自動記録そのものを取り消したい場面が
  もしあれば、いまの画面からは選べない
- **5-9のマスワリは「押した時点で倍」。** 保存前にやめた場合でも、
  選んだままなら倍のまま保存される（`liveRacks()`）
- **描画の線は点の列で持つ。** 長くなぞると点が増える（3px間引きあり）。
  保存容量の上限は実測していない
- **段階5以降、縦向きの見た目も変わっている**（記録ボタンが各サイドへ）。本人未確認
- **`css/v2.css` が1900行を超えた。** 末尾に上書き用のブロックが積み上がっている

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v29`）
  → ファイルを変更したら**必ず数字を上げる**。上げないとスマホに古い画面が残る
- **`css/v2.css` 末尾のブロック群**（線・記録ボタンの置き場所・横向き・grid-mode・
  種目別カード・名前とW-Lの重なり・×の幅・試合画面の作り直し）
  → 「44px以上」を下げない。横向きの高さは上の6.のとおりぎりぎり
- **grid の欄に `justify-self: start / end` を付けない**（欄が中身の幅になり隣に重なる）
- **`.hc-close` の幅は `.home-card.resume .hc-close` で書く**
  （`.home-card button { width: 100% }` に負けるため）
- **`js/ui_match.js` の `isAutoEvent()` / `voidTrailingAuto()`**
  → 取り消しボタンの代わりを「訂正」に負わせている要。ここを外すと
  訂正しても得点が戻らない状態に戻る
- **`js/ui_match.js` の `syncMetaPlace()`** → 元の場所は `.meta-chips:not(.in-topbar)`
- **`js/ui_layout.js` の `VB`（1000）と `vector-effect="non-scaling-stroke"`**
  → 直線となぞった線が同じ座標系である前提
- **`js/ui_layout.js` の `mode`** → `null` / `"line"` / `"draw"` の3値
- **`js/ui_history.js` の `render()` の分岐** → 対戦0件とハウスゲーム0件は別
- **`data/handicap_data.js` の `JPA_DOUBLES_9BALL`**（対称・合計15まで）
- **`js/ui_layout.js` の `RAIL_PX`（14）と `css/v2.css` の `--rail`**（同じ値に）
- **`js/engine.js` の `result.innings`**（画面に出すのは `result.inningsPlayed`）
- **`_test/run_all.bat` はUTF-8。** パスの区切りは `chr(92)` かraw文字列で書く
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

**このセッションで下した主な判断**

1. **「訂正」から自動記録を隠し、取り消しに連鎖させた** — 取り消しボタンを消した結果、
   一覧の先頭が「ラック2 開始」になり、訂正しても得点が戻らない状態になっていた。
   これは指示の副作用として実際に壊れていたので直した（テストが検出）
2. **横向きの記録ボタンを外側に置き、帯の余白を削って収めた** — 押せる大きさ（44px）は
   下げないという制約を優先し、削るのは余白と文字の大きさだけにした
3. **5-9のブレイクエースは記録しない形になった** — 確認の窓を消す指示だったため。
   成績の行は残してあるので常に0になる（本人判断待ち）
4. **JPA 8ボールの満点を3とした** — 本人の「1試合最大20ポイント」は9ボールの話。
   8ボールは `jpaTeamPoints8` から推測した
5. **種目別カードは `details/summary` にした** — 既定で閉じる指示を、JSを増やさずに満たせる

**本人確認待ちの事項**:
- **配置図の球の番号を1px上げた見え方**（元の症状はこちらで再現できていない）
- **JPA 8ボールの満点を3としてよいか**
- **5-9 / 5-10 のブレイクエースを、行ごと消すか・別の入力を足すか**
- 縦向きでも記録ボタンが各サイドに移ったが、これでよいか

---

### 次の安全なアクション（1つだけ）

**本人に上の「8.」の確認待ち4点をまとめて聞く。**
とくに5-9のブレイクエース（いまは常に0と出る）は、行を消すか入力を足すかで
作業量が変わるので、着手前に決めてもらう。

### 再開者が仮定してはいけないこと

- **「36本通った＝スマホで使える」と仮定しないこと。** 実機での確認は一度もしていない
- **「番号のズレが直った」と仮定しないこと。** 元の症状を再現できておらず、
  1px上げたのは本人の指示に沿った対応で、見え方の確認はまだ
- **「5-9のブレイクエースが記録されている」と仮定しないこと。** 手立てが無く常に0
- **「カイルンの最大連続得点が実際に記録されている」と仮定しないこと。**
  画面から入力する流れは通していない
- **「古い記録にも5-9のマスワリ・カイルンの連続得点が入っている」と仮定しないこと。**
  保存に載せたのは2026-08-21から
- **「横向きの高さに余裕がある」と仮定しないこと。** 数pxの積み上げで収めている
- **「`git status` が clean ＝公開版が最新」と仮定しないこと。** 4.の curl で実物を見る
- **テストの結果をパイプ越しに見ないこと。** 終了コードがパイプ最後のものになる

---

## 2026-08-21（01:45時点）— 段階7まで完了（この時点の記録）

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開URL: https://hiroshit164820-create.github.io/pool-score/

本人の指示22件は**すべて公開まで反映済み**（段階1〜7）。
**残っているのは「ボールの番号が円の中心に来ず端に寄っている」だけで、
これは再現できていないため本人の画像待ち。**

---

### 2. 変更点（このセッション）

コミット3件。push済み（`origin/main` = `7a7d67b`、01:43:29時点の `git log` で確認）。

| コミット | 内容 |
|---|---|
| `aa6c6a1` | 配置図のボタンを「直線を引く」と「描画する」に分けた |
| `9ca8311` | 引き継ぎ更新 |
| `7a7d67b` | 種目別でさらに詳しくカード（段階7）と、長い名前・×の幅の修正 |

#### 種目別でさらに詳しく（既定は閉じる）

`STORE.gameDetail(playerId)` を新設。種目ごとの数字を1回でまとめる。
画面は選手の詳細に `details/summary` のカードを足した（押したときだけ開く）。

| 種目 | 出している項目 |
|---|---|
| 9/10/8ボール（ダブルス含む） | 勝敗数・勝率・マスワリ数/率・ブレイクエース(9のみ)・セーフティ平均(ラック/試合)・1ラック平均イニング・ショットクロック平均タイム/エクステンション |
| ローテーション | 点数別の勝敗数と勝率・Aハイラン/Bハイランの数と率・セーフティ平均・イニング平均・ショットクロック |
| 14-1 | 勝敗数・勝率・ハイラン・ショットクロック |
| ボウラード | 平均スコア(過去10/30/50回)・最高スコア・累計ストライク/スペア/ミス |
| JPA 9ボール・8ボール | 勝敗数・勝率・累計ポイント・1試合平均ポイントと率・マスワリ/エースの数と率・セーフティ平均・イニング平均・あがりまでの最小/最大イニング・相手のSL平均・SL別の勝敗と勝率 |
| JPA 9ボールダブルス | 上からポイントとSLの項目を除いた形 |
| ハウスゲーム | 獲得得点の履歴（新しい順・直近20回）／カイルンは最大連続得点／5-9・5-10 はマスワリとブレイクエースの数と率 |

**決めた定義（本人確認待ち）**:
- **1試合あたりの平均獲得ポイント率** = 自分のポイント ÷ その試合で動いた全ポイント
  （自分+相手）。別の意味なら直す
- **Aハイラン率の分母** = 自分がブレイクした試合数、**Bハイラン率の分母** = 相手が
  ブレイクした試合数。項目名に分母を書いてある
- **マスワリ率・ブレイクエース率の分母** = 自分がブレイクした回数（`perSide.breaks`）
- **あがりまでのイニング数** = 勝った試合の `inningsPlayed` の最小・最大

#### 記録が無かった2つを記録するようにした

- **カイルンの最大連続得点** — 一度の手番で続けて取った点の最大。交代・反則で切れる
- **5-9 / 5-10 のマスワリ数** — ラックの `runoutBy` から数える（**記録自体は前からあった**）
- **5-9 / 5-10 のブレイクエース数** — ラックを終えるとき、撞き切った人がいる場合だけ
  `window.confirm` で聞く（**新しく足した入力**）

**どちらも古い記録には遡って入らない。**

#### ハイランの判定（実データで確認済み・新しい記録は不要）

| | 判定条件 | 実測した `result` |
|---|---|---|
| Aハイラン | 自分がブレイクし、相手0点、自分の `highRun` が目標点以上 | `breaks A=1 B=0`／`scores B=0`／`highRun A=120` |
| Bハイラン | 相手がブレイクし、相手0点、自分の `highRun` が目標点以上 | `breaks A=1 B=0`／`scores A=0`／`highRun B=120` |

用語の出典（2026-08-20 参照）:
- Aハイラン https://billi-walker.jp/bwdictionary/index.php?link=%EF%BC%A1%E3%83%8F%E3%82%A4%E3%83%A9%E3%83%B3
- Bハイラン https://billi-walker.jp/bwdictionary/index.php?link=%EF%BC%A2%E3%83%8F%E3%82%A4%E3%83%A9%E3%83%B3

#### 本人の指摘2件（画像）

- **履歴で長い名前が「W-L」の札に重なる** — grid で `justify-self: start / end` を
  付けると、その欄は「中身の幅」になって割り当てられた列（90px）をはみ出す。
  欄いっぱいに伸ばして寄せ方だけ変え、名前は `…` で切るようにした
- **中断中カードの「×」が横いっぱいに伸びる** — `style.css` の
  `.home-card button { width: 100% }` に負けていた。同じだけ細かい書き方にして
  44px角に戻した。名前が長いと2行目が×に潜り込む件も、名前の行にも余白を取って解消

#### 触ったファイル

`js/store.js` `js/ui_players.js` `js/ui_kailun.js` `js/ui_money.js`
`js/ui_layout.js` `css/v2.css` `sw.js`(v25→v28)
新規テスト3本 `_test/draw_test.py`(39) `_test/detail_test.py`(56) `_test/fix2_test.py`(17)
`_test/line_test.py`（ボタンIDと座標系）／`_test/run_all.bat`（34本に）

---

### 3. 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| ローカルの検証**35本すべて成功**（node 4本＋実ブラウザ31本） | 失敗した本だけ出す形で回して出力なし | 01:41 |
| `detail_test.py` **56/56**、`fix2_test.py` **17/17**、`draw_test.py` **39/39** | 各テストの終了コード0 | 01:30〜01:41 |
| push が通り `origin/main` = `7a7d67b` | `git log --oneline origin/main -1` | 01:43:29 |
| Pagesのビルドが success | `gh run list` | 01:44:29 |
| **公開版の `CACHE_VERSION` が `v28`** | `curl` でキャッシュ回避パラメータ付き取得 | 01:44:29 |
| **公開URLでの実ブラウザ検証19項目がすべて成功** | `python _test/live_test.py` 終了コード0 | 01:45 |
| **公開URLで今回ぶん12項目がすべて成功**（名前とW-Lの重なり／カードの開閉と項目／項目名の潰れ／×の幅／文字の食い込み） | 公開URL相手の実ブラウザ確認 | 01:45:54 |

**確認していないこと**:
- **本人のスマホ実機・iOS Safari では未確認**（これまでと同じ）
- **カイルンの最大連続得点と5-9のブレイクエースは、テストでは値を直に入れて確かめた。**
  実際の画面から入力して保存する流れは通していない

---

### 4. 一次情報の取り直しコマンド

```
cd D:\Claudecode\pool-score

# いま公開されているものが新しいか（v28以上なら反映済み）
curl -s -A "Mozilla/5.0" "https://hiroshit164820-create.github.io/pool-score/sw.js?cb=%RANDOM%" | findstr CACHE_VERSION

# 公開版が壊れていないか（実ブラウザで19項目）
python _test\live_test.py

# 今回ぶん
python _test\detail_test.py
python _test\fix2_test.py
python _test\draw_test.py

# ローカルの全検証（34本・30分ほど）
_test\run_all.bat
```

**テストをパイプに通すと終了コードがパイプ最後のものになり、失敗を見落とす。**
`python _test\xxx_test.py > out.txt 2>&1` のあとに終了コードを見ること。

---

### 5. 未着手・本人待ち

#### A. 「ボールの番号が円の中心に来ず端に寄っている」（配置図）

390x844のChromiumで標準・プラチナム・アラミスの全ボールセット、台の上・置ける球の
一覧の両方を実測したが、**ずれ0〜1px**で中心にあった（2026-08-20 20:15頃）。
**本人の画像をもう一度もらってから直すこと。** いまの三角枠3pxは推測での対応。

#### B. 定義の確認（上の2.参照）

「1試合あたりの平均獲得ポイント率」「ハイラン率の分母」「マスワリ率の分母」など、
決めうちにした定義がいくつかある。意図と違えば直す。

#### C. 実画面からの入力を通していない2つ

カイルンの最大連続得点／5-9のブレイクエースは、**実際に画面から記録して保存し、
成績に出るところまで通していない**。次に触るときは実機か実ブラウザで一度通すこと。

---

### 6. 不確実な点・リスク

- **5-9 のブレイクエースは `window.confirm` で聞く。** ラックを終えるたびに
  1回増える操作なので、うるさければ別の形（ボタン）に変えられる
- **描画の線は点の列で持つ。** 長くなぞると点が増える（3px間引きあり）。
  保存容量の上限は実測していない
- **SVGの座標系は 0〜1000。** 直線となぞった線を同じ座標系に揃えるため
- **段階5で縦向きの見た目も変わっている**（記録ボタンが各サイドへ）。本人未確認
- `css/v2.css` が1600行を超えた

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v28`）→ 変更したら必ず上げる
- **`css/v2.css` 末尾のブロック群**（線／記録ボタンの置き場所／横向き／grid-mode／
  種目別カード／名前とW-Lの重なり／×の幅）→ 「44px以上」を下げない
- **grid の欄に `justify-self: start / end` を付けない**（欄が中身の幅になり隣に重なる）
- **`.hc-close` の幅は `.home-card.resume .hc-close` で書く**
  （`.home-card button { width: 100% }` に負けるため）
- **`js/ui_layout.js` の `VB`（1000）と `vector-effect="non-scaling-stroke"`**
- **`js/ui_layout.js` の `mode`** → `null` / `"line"` / `"draw"` の3値
- **`js/ui_match.js` の `syncMetaPlace()`** → 元の場所は `.meta-chips:not(.in-topbar)`
- **`js/ui_history.js` の `render()` の分岐** → 対戦0件とハウスゲーム0件は別
- **`data/handicap_data.js` の `JPA_DOUBLES_9BALL`**（対称・合計15まで）
- **`js/ui_layout.js` の `RAIL_PX`（14）と `css/v2.css` の `--rail`**（同じ値に）
- **`js/engine.js` の `result.innings`**（画面に出すのは `result.inningsPlayed`）
- **`_test/run_all.bat` はUTF-8。** パスの区切りは `chr(92)` かraw文字列で書く
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

1. **種目別カードは `details/summary` にした** — 既定で閉じる指示を、JSを増やさず
   satisfies できるため。見出しは44px以上を確保
2. **率の分母を項目名の側に書いた** — 値を長くすると項目名の欄が潰れて
   「A ハ イ ラ ン」と縦に割れたため
3. **5-9のブレイクエースは confirm で聞く形にした** — 撞き切った人がいるときだけ
   出るので、ふだんの操作は増えない
4. **ハイランは新しい記録を足さずに既存データから出した** — 実データで確認済み

**本人確認待ちの事項**:
- **「ボールの番号が端に寄っている」の画像**
- 種目別カードで決めうちにした定義（ポイント率・各種の率の分母）
- 縦向きでも記録ボタンが各サイドに移ったが、これでよいか

---

### 次の安全なアクション（1つだけ）

**本人に「番号のズレ」の画像をもらう。** それまでは、
カイルンの最大連続得点と5-9のブレイクエースを**実画面から入力して成績に出るまで**
通す（テストでは値を直に入れて確かめただけ）。

### 再開者が仮定してはいけないこと

- **「35本通った＝スマホで使える」と仮定しないこと。** 実機での確認は一度もしていない
- **「番号のズレを直した」と仮定しないこと。** 再現できておらず、対応は推測
- **「カイルンの最大連続得点が実際に記録されている」と仮定しないこと。**
  画面から入力する流れは通していない
- **「古い記録にも5-9のマスワリが入っている」と仮定しないこと。**
  保存に載せたのは2026-08-21から
- **「`git status` が clean ＝公開版が最新」と仮定しないこと。** 4.の curl で実物を見る
- **テストの結果をパイプ越しに見ないこと。** 終了コードがパイプ最後のものになる

---

## 2026-08-21（00:22時点）— 配置図に「描画する」を追加（この時点の記録）

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開URL: https://hiroshit164820-create.github.io/pool-score/

**このセッションの追加指示**: 「配置図で直線を引くボタンを分割して、もう一方に
『描画する』ボタンにして、指でなぞったら線を引けるようにして」→ **実装して公開済み**。

**次のセッションの目標**: 段階7「種目別でさらに詳しく」。
**ただし約60項目の中身が、このリポジトリのどこにも残っていない。**
本人に項目一覧をもらってから着手すること（下の「5.」参照）。

---

### 2. 変更点

コミット1件。push済み（`origin/main` = `aa6c6a1`、00:20:57時点の `git log` で確認）。

| コミット | 内容 |
|---|---|
| `aa6c6a1` | 配置図のボタンを「直線を引く」と「描画する」に分けた |

- **ボタンを2つに分けた**。`#layoutLineBtn`「直線を引く」（押した点と離した点を結ぶ1本）と
  `#layoutDrawBtn`「描画する」（なぞった通りのフリーハンド）
- **どちらか一方だけが入る**。もう一度押すと切れて球を動かせる状態に戻る
- 描いた線も直線と同じく、押すと消える／戻る・進む／全部どける／保存・呼び出しに入る
  （描画を入れる前に保存した配置は空として読む。テスト済み）
- 指のぶれ対策で **3px以上動いたときだけ点を足す**。なぞった**道のり**が10px未満なら
  「押しただけ」として線を消す操作にする（ぐるっと回って戻ると始点と終点の距離では
  区別できないため）
- **SVGの座標系を 0〜1000 に変えた**。なぞった線（polyline）は「%」を受け付けないため、
  直線と座標系を揃える必要がある。台が縦長なので `preserveAspectRatio="none"` で伸ばし、
  線の太さが縦横で変わらないよう `vector-effect="non-scaling-stroke"` を付けた

触ったファイル: `js/ui_layout.js` `js/store.js` `sw.js`(v25→v26)
`_test/draw_test.py`（新設・39項目）／`_test/line_test.py`（ボタンIDと座標系の期待値を更新）
／`_test/run_all.bat`（32本に）

---

### 3. 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| ローカルの検証**33本すべて成功**（node 4本＋実ブラウザ29本） | 各テストの終了コード（`grep -c EXIT=0` で29件、非ゼロ0件） | 00:19 |
| `draw_test.py` が **39/39**、`line_test.py` が **33/33** | 各テストの終了コード0 | 00:15 |
| push が通り `origin/main` = `aa6c6a1` | `git log --oneline origin/main -1` | 00:20:57 |
| **公開版の `CACHE_VERSION` が `v26`** | `curl` でキャッシュ回避パラメータ付き取得 | 00:21:56 |
| **公開URLでの実ブラウザ検証19項目がすべて成功** | `python _test/live_test.py` 終了コード0 | 00:22 |
| **公開URLで描画7項目がすべて成功**（2つのボタン／なぞった通りに描ける／曲げた跡が残る／切り替え／エラー無し） | 公開URL相手の実ブラウザ確認 | 00:22:55 |
| **Aハイラン／Bハイランが既存の記録だけで判定できる** | ローテーション120点の試合を2通り実際に記録し `result` を読み出し（下記） | 00:1x |

#### ハイランの判定（実データで確認済み。**新しい記録は不要**）

| | 実測した `result` | 判定条件 |
|---|---|---|
| Aハイラン | `breaks A=1 B=0`／`scores B=0`／`highRun A=120`（＝目標点）／`innings=0` | 勝者がブレイクし、相手が0点、勝者の `highRun` が目標点以上 |
| Bハイラン | `breaks A=1 B=0`／`scores A=0`／`highRun B=120` | 敗者がブレイクし、敗者が0点、勝者の `highRun` が目標点以上 |

目標点は `match.goal.targets[side]` にある。

**確認していないこと**: 本人のスマホ実機・iOS Safari では未確認（これまでと同じ）。

---

### 4. 一次情報の取り直しコマンド

```
cd D:\Claudecode\pool-score

# いま公開されているものが新しいか（v26以上なら反映済み）
curl -s -A "Mozilla/5.0" "https://hiroshit164820-create.github.io/pool-score/sw.js?cb=%RANDOM%" | findstr CACHE_VERSION

# 公開版が壊れていないか（実ブラウザで19項目）
python _test\live_test.py

# 配置図の線まわり（直線33項目＋描画39項目）
python _test\line_test.py
python _test\draw_test.py

# ローカルの全検証（32本・25分ほど）
_test\run_all.bat
```

**テストをパイプ（| tail）に通すと終了コードが tail のものになり、失敗を見落とす。**
`python _test\xxx_test.py > out.txt 2>&1` のあとに終了コードを見ること。

---

### 5. 未着手の範囲（**ここが次の作業**）

#### A. 本人待ち: 段階7の項目一覧

**「種目別でさらに詳しく」の約60項目の中身が、リポジトリのどこにも残っていない。**
`HANDOFF.md` の過去版・`07_ロードマップ.md` も探したが、項目名は「10種目ぶん・約60項目」
としか書かれていない（00:10 に `grep` で確認）。
**推測で60項目を作ると作り直しになるので、本人から一覧をもらってから着手すること。**

分かっている前提:
- **ローテーションのAハイラン／Bハイランは既存データで出せる**（上の3.参照）
- **新しく記録を足さないと出せないもの**:
  - カイルンの最大連続得点（いまは最終スコアしか保存していない）
  - 5-9 / 5-10 のマスワリ・ブレイクエース（money 画面で記録していない）
  - ボウラードの平均スコア（`result.bowlard.total` は 2026-08-21 以降の記録のみ）

#### B. 本人待ち: 「ボールの番号が円の中心に来ず端に寄っている」

390x844のChromiumで標準・プラチナム・アラミスの全ボールセット、台の上・置ける球の
一覧の両方を実測したが、**ずれ0〜1px**で中心にあった（2026-08-20 20:15頃）。
**本人の画像をもう一度もらってから直すこと。** いまの三角枠3pxは推測での対応。

---

### 6. 不確実な点・リスク

- **描画の線は点の列で持つ。** 長くなぞると点が増える（3px間引きあり）。
  localStorage の容量は `STORE.usageKB()` で見られる。極端に長い線を何本も保存すると
  容量を食う可能性があるが、実測での上限確認はしていない
- **SVGの座標系を変えた（% から 0〜1000 へ）。** 直線の見た目は変わらないが、
  `line_test.py` の座標の見方を10で割る形に直してある。ここを触るときは両方を回すこと
- 段階5で縦向きの見た目も変わっている（記録ボタンが各サイドへ）。本人未確認
- `css/v2.css` が1400行を超えた

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v26`）→ 変更したら必ず上げる
- **`js/ui_layout.js` の `VB`（1000）と `u()`** → 直線となぞった線が同じ座標系である前提。
  `vector-effect="non-scaling-stroke"` を外すと線の太さが縦横で変わる
- **`js/ui_layout.js` の `mode`** → `null` / `"line"` / `"draw"` の3値。
  同時に2つ入らないこと（球を掴む操作と競合する）
- **`css/v2.css` 末尾の4ブロック**（線／記録ボタンの置き場所／横向き／grid-mode）
  → 「44px以上」を下げない
- **`js/ui_match.js` の `syncMetaPlace()`** → 元の場所は `.meta-chips:not(.in-topbar)`
- **`js/ui_history.js` の `render()` の分岐** → 対戦0件とハウスゲーム0件は別
- **`data/handicap_data.js` の `JPA_DOUBLES_9BALL`**（対称・合計15まで）
- **`js/ui_layout.js` の `RAIL_PX`（14）と `css/v2.css` の `--rail`**（同じ値に）
- **`js/engine.js` の `result.innings`**（画面に出すのは `result.inningsPlayed`）
- **`_test/run_all.bat` はUTF-8。** パスの区切りをPythonの通常文字列経由で書くと
  タブ・改行に化ける（`chr(92)` かraw文字列を使うこと）
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

1. **描画は直線とは別のボタンにした** — 本人の指示どおり。同時には入らない
2. **「押しただけ」の判定を、始点と終点の距離ではなく道のりにした** — 描画は
   ぐるっと回って元の位置に戻れるため、距離では消す操作と区別できない
3. **SVGの座標系を 0〜1000 に統一した** — polyline が「%」を受け付けないため
4. **段階7には入らなかった** — 約60項目の中身がリポジトリに残っておらず、
   推測で作ると作り直しになるため

**本人確認待ちの事項**:
- **段階7「種目別でさらに詳しく」の項目一覧**（これが無いと着手できない）
- **「ボールの番号が端に寄っている」の画像**
- 縦向きでも記録ボタンが各サイドに移ったが、これでよいか

---

### 次の安全なアクション（1つだけ）

**本人に段階7の項目一覧を聞く。** もらえたら、まず
「既存データで出せるもの」と「記録を足す必要があるもの」に仕分けてから着手する。

### 再開者が仮定してはいけないこと

- **「33本通った＝スマホで使える」と仮定しないこと。** 実機での確認は一度もしていない
- **「段階7の項目が分かっている」と仮定しないこと。** リポジトリには残っていない
- **「番号のズレを直した」と仮定しないこと。** 再現できておらず、対応は推測
- **「`git status` が clean ＝公開版が最新」と仮定しないこと。** 4.の curl で実物を見る
- **テストの結果をパイプ越しに見ないこと。** 終了コードが tail のものになる

---

## 2026-08-20（20:52時点）— 指示22件のうち15件を公開まで完了（この時点の記録）

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開URL: https://hiroshit164820-create.github.io/pool-score/

本人からの**22件の指示**を7段階に分けて進めている。前回までに段階1〜3の途中（9件）。
**このセッションで段階3の残り〜段階6（6件）を実装し、公開まで反映した。**

**次のセッションの目標**: 段階7「種目別でさらに詳しく」カード（約60項目）。
着手前に本人へ規模と分割案を出すこと（12h以上かかる）。

---

### 2. 変更点（このセッションで実際に変更したもの）

コミット1件。push済み（`origin/main` = `8428eba`、20:50:19時点の `git log` で確認）。

| コミット | 内容 |
|---|---|
| `8428eba` | 履歴の絞り込み・配置図の直線・横向きスコアボード・成績の内訳（段階3〜6） |

#### 完了した6件

| 段階 | 指示 | やったこと |
|---|---|---|
| 3の残り | 履歴の絞り込みにハウスゲームを追加 | `renderFilters()` が `listMoneyResults()` からも種目と名前を集めるようにした |
| 4 | 配置図に直線を引く | 台をなぞって引き、線を押すと消える。戻る/進む・全部どける・保存/呼び出しにも入れた |
| 4 | ボールの番号が端に寄る | **再現できなかった**。三角枠だけ試合画面と同じ下げ幅に揃えた（暫定） |
| 5 | 横向きのスコアボード（中身6つ） | 下の「段階5の中身」参照 |
| 6 | 「詳しい成績」を一般種目とJPAに分ける | `playerStats` に `general` / `jpa` を足し、「一般種目とJPAの内訳」の表を出す |
| 6 | パートナー別の勝敗と勝率 | `playerStats.partners` を足し、「パートナー別」の表を出す |

#### 段階5の中身（本人の指示6つ、すべて実装）

- スコアボードを大きく（横向きで枠 208x164px・数字96px。前は高さ60px・30px）
- セーフティのボタンを各プレーヤーサイドへ移動 → `panelFlags{A,B}` へ
- ラック数とイニング数を上の帯へ移動 → `syncMetaPlace()` が向きを見て移す
- ブレイク権の表示もプレーヤーサイドへ → 帯（`.break-banner`）は2026-08-20の指示で
  既に縦横とも非表示。パネル内の `BREAK` の札に枠を付けて目立たせた
- マスワリとエースをスコアボード横に、セーフティと同じカウント式で各サイドへ
  → 3つとも「名前＋回数」のボタンにそろえた。**ブレイクしていない側は押せなくして
  出す**（回数は左右それぞれで読める）

#### 指示外だが直したもの（不具合）

- **ハウスゲームで絞ると履歴が空になっていた** — 対戦の記録が0件になった時点で
  早期returnしており、ハウスゲームの記録まで消えていた
- **ローテーション・14-1で11〜15番のボタンが画面外に出た** — 段階5で記録ボタンが
  各サイドに増えたぶん盤面が押し下げられた。盤面のある種目では記録ボタンを
  スコアの**横**に置いて解消（`layout_fit_test` の32通りで確認）
- カイルンの記録に「0ラック」と出ていたのをやめた
- 見出し「5-9 / 5-10 の記録」→「ハウスゲームの記録」（画面もCSVも）

#### 触ったファイル

`css/v2.css` / `sw.js`(v24→v25) /
`js/store.js` `js/csv.js` `js/ui_history.js` `js/ui_layout.js` `js/ui_match.js`
`js/ui_players.js` /
新規テスト4本 `_test/tune6_test.py`(25項目) `_test/line_test.py`(33項目)
`_test/land_test.py`(36項目) `_test/stats3_test.py`(23項目) と
既存テスト3本の期待値更新（`browser_test.py` `slice2_test.py` `money2_test.py`）

#### 変更した外部状態

- **GitHub の `main` を更新した**（`0f1be74` → `8428eba`）
- **GitHub Pages が再ビルドされ、公開URLの中身が入れ替わった**（v24 → v25）
- ローカルのデータ（localStorage）には触っていない。検証はすべてヘッドレスの一時プロファイル

---

### 3. 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| ローカルの検証**32本すべて成功**（node 4本＋実ブラウザ28本） | 各テストの終了コード0 | 20:26〜20:49 |
| 新規4本が全項目成功（tune6 25／line 33／land 36／stats3 23） | 各テストの終了コード0 | 20:49 |
| push が通り `origin/main` = `8428eba` | `git log --oneline origin/main -1` | 20:50:19 |
| Pagesのビルドが success | `gh run list` | 20:51:23 |
| **公開版の `CACHE_VERSION` が `v25`**（push前は `v24`） | `curl` でキャッシュ回避パラメータ付き取得 | 20:51:23 |
| **公開URLでの実ブラウザ検証19項目がすべて成功** | `python _test/live_test.py` 終了コード0 | 20:52 |
| **公開URLで今回ぶん10項目がすべて成功**（直線／記録ボタン3つ×左右／横向きの枠と上の帯／成績の内訳／履歴のカイルン絞り込み） | 公開URL相手の実ブラウザ確認 | 20:52:28 |

**確認していないこと（完了扱いにしていない）**:
- **本人のスマホ実機では一度も見ていない。** 検証はヘッドレスChromium（390x844・
  844x390・375x667・375x812・360x640）のみ
- **iOS Safari では確認していない**
- **「ボールの番号が端に寄る」は再現できていない。** 直したつもりの3pxは推測
- **JPAダブルスの表は本人提供の画像を読み取った値**（前回から変わらず）

---

### 4. 一次情報の取り直しコマンド（疑わしいときはまずこれ）

```
cd D:\Claudecode\pool-score

# いま公開されているものが新しいか（v25以上なら反映済み）
curl -s -A "Mozilla/5.0" "https://hiroshit164820-create.github.io/pool-score/sw.js?cb=%RANDOM%" | findstr CACHE_VERSION

# 公開版が壊れていないか（実ブラウザで19項目）
python _test\live_test.py

# 今回ぶんの検証（4本・合計117項目）
python _test\tune6_test.py
python _test\line_test.py
python _test\land_test.py
python _test\stats3_test.py

# ローカルの全検証（31本・25分ほどかかる）
_test\run_all.bat

# デプロイが詰まっていないか
gh run list --limit 3
```

**`git status` が clean でも「公開版が最新」とは限らない。** 必ず上の curl で実物を見る。
**テストを `| tail` に通すと終了コードが tail のものになり、失敗を見落とす。**
`python _test\xxx.py > out.txt 2>&1` のあとに終了コードを見ること
（このセッションで実際に money2 の失敗を1度見落とした）。

---

### 5. 未着手の範囲（**7件。ここが次の作業**）

#### 段階4の残り（1件）
- **「ボールの番号が円の中心に来ず端に寄っている」** — 390x844のChromiumで
  標準・プラチナム・アラミスの全ボールセット、台の上・置ける球の一覧の両方を
  実測したが、番号の枠も枠の中の数字も**ずれ0〜1px**で中心にあった（20:15頃）。
  **本人の画像をもう一度もらってから直すこと。** いまの三角枠3pxは推測での対応

#### 段階7 「種目別でさらに詳しく」カード（6件相当・目安12h以上）
既定は閉じたカード。10種目ぶん・約60項目。**すべて未着手**。

engine が既に記録している値（そのまま使える）:
`masuwari` `breakAce` `safety` `highRun` `shotClockShots/TotalSec`
`shotClockExtensions` `innings` `jpa.teamPoints` `skillLevel` `playerIds`

**新しく記録を足さないと出せないもの**:
- **カイルンの最大連続得点** — 新しいカイルン画面は最終スコアしか保存していない
- **5-9 / 5-10 のマスワリ・ブレイクエース** — money 画面で記録していない
- **ボウラードの平均スコア（過去10/30/50回）** — `result.bowlard.total` は
  2026-08-21から保存しているので、**それ以降の記録からしか出せない**

**ローテーションの「Aハイラン／Bハイラン」の意味は確認済み**（本人の指示どおりWebで裏取り。
2026-08-20 20:45 参照）:
- **Aハイラン** = 自分がブレイクして、そのまま上がり点まで撞き切る（相手はバンキングのみ）
  出典: https://billi-walker.jp/bwdictionary/index.php?link=%EF%BC%A1%E3%83%8F%E3%82%A4%E3%83%A9%E3%83%B3
- **Bハイラン** = 相手のブレイク後、自分の1キュー目で上がり点まで撞き切る（相手は得点なし）
  出典: https://billi-walker.jp/bwdictionary/index.php?link=%EF%BC%A2%E3%83%8F%E3%82%A4%E3%83%A9%E3%83%B3

→ **どちらも新しい記録を足さずに出せる見込み**。「相手のスコアが0」「イニング数」
「どちらがブレイクしたか」は `result` に既にある。**ただし実データで確かめていないので、
実装前に既存の記録で計算できるかを確認すること**。

---

### 6. 不確実な点・リスク

- **段階5で縦向きの見た目も変わった。** マスワリ・エース・セーフティを各サイドの
  パネルの下（盤面のある種目は横）に置いたため。本人の指示は横向きの文脈だったが、
  同じ情報を2か所に持つのを避けて縦横で同じ作りにした。**本人の意図と違えば戻せる**
- **マスワリ・エースを両側に出すようにした。** ブレイクしていない側は `disabled` で
  薄く出る。以前は「両方に出すと押し間違える」として片側だけだった（コメントも更新済み）
- **記録ボタンの置き場所は3か所で分岐している**（縦向き＝パネルの下、
  盤面のある種目＝パネルの横、横向き＝パネルの横）。`css/v2.css` の末尾3ブロック。
  ここを触ったら `land_test.py` と `layout_fit_test.py` を必ず回すこと
- **`css/v2.css` が1400行を超えた。**
- **配置図の線は「押すと消える」。** 確認は挟まないが「一つ前に戻る」で戻せる
- **線は台の割合で持つ。** 古い配置（`lines` が無い）は線なしとして読む（テスト済み）

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v25`）
  → ファイルを変更したら**必ず数字を上げる**。上げないとスマホに古い画面が残る
- **`css/v2.css` 末尾の3ブロック**（記録ボタンの置き場所・横向き・grid-mode）
  → 「44px以上」を下げないこと。`.panel-flags button` の高さを削ると `land_test` が落ちる
- **`js/ui_match.js` の `syncMetaPlace()`**
  → 上の帯に作る置き場も `.meta-chips` なので、元の場所は
  `.meta-chips:not(.in-topbar)` で指すこと（取り違えて1度壊した）
- **`js/ui_history.js` の `render()` の分岐**
  → 「対戦が0件」と「ハウスゲームが0件」は別。両方が空のときだけ
  「合う試合はありません」を出す
- **`data/handicap_data.js` の `JPA_DOUBLES_9BALL`**（対称・合計15まで）
- **`js/ui_layout.js` の `RAIL_PX`（14）と `css/v2.css` の `--rail`**（同じ値に）
- **`js/engine.js` の `result.innings`**（画面に出すのは `result.inningsPlayed`）
- **セット制の判定は `reduceMatch` のループの中だけ**
- **`style.css` の可読性の制約**（タップ44px以上、本文16px以上、コントラスト4.5:1以上）
- **`_test/run_all.bat` はUTF-8。** パスの `\t` `\n` をPythonの通常文字列経由で
  書くとタブ・改行に化ける（`chr(92)` かraw文字列を使うこと）
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

**このセッションで下した主な判断**

1. **記録の3つ（マスワリ・エース・セーフティ）を縦横とも各サイドに置いた** —
   横向きだけ別の作りにすると、JSで向きごとにDOMを組み替えることになり壊れやすい
2. **マスワリ・エースは両側に出し、ブレイクしていない側は押せなくした** —
   「各プレーヤーサイドへ」という指示と、誤記録の防止を両立させるため
3. **盤面のある種目だけ記録ボタンを横に置いた** — 下に置くと11〜15番が
   画面外に出た（推測ではなく `layout_fit_test` が検出した）
4. **「番号が端に寄る」は直したと言わないことにした** — 再現できていないため。
   三角枠の3pxは暫定
5. **段階7には入らず、ここで止めた** — 約60項目で12h以上かかり、
   記録そのものが無い項目も混ざっているため、分割案を出してから着手すべき

**本人確認待ちの事項**:
- **「ボールの番号が端に寄っている」の画像**（これが無いと直せない）
- 縦向きでも記録ボタンが各サイドに移ったが、これでよいか
- 横向きのスコアボードが意図どおりか（`_test/shots/land_scoreboard.png`）
- 段階7の進め方（10種目・約60項目をどう分けるか）

---

### 次の安全なアクション（1つだけ）

**段階7に入る前に、本人へ分割案を出す。**
そのうえで、まず**ローテーションのAハイラン／Bハイランが既存の記録で計算できるか**を
実データで確かめる（新しい記録を足さずに済むなら、段階7の見積もりが下がる）。

### 再開者が仮定してはいけないこと

- **「32本通った＝スマホで使える」と仮定しないこと。** 実機での確認は一度もしていない
- **「22件の指示が終わった」と仮定しないこと。** 完了は15件、未着手は7件
- **「番号のズレを直した」と仮定しないこと。** 再現できておらず、対応は推測
- **「Aハイランが記録できる」と仮定しないこと。** 意味は分かったが、
  既存の記録で計算できるかは未確認
- **「`git status` が clean ＝公開版が最新」と仮定しないこと。** 4.の curl で実物を見る
- **テストの結果を `| tail` 越しに見ないこと。** 終了コードが tail のものになる
- **`layout_fit_test.py` を2分で切らないこと。** 3分ほどかかる
- **落ちたテストを「本物の不具合」とも「flaky」とも決めつけないこと。** まず再実行する

---

## 2026-08-21（19:37時点）— 指示22件のうち9件を公開まで完了（この時点の記録）

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開URL: https://hiroshit164820-create.github.io/pool-score/

本人から**22件の指示**を受けた（不具合3件＋UI改修＋「種目別でさらに詳しく」の
新規成績ページ）。全体を7段階に分け、**段階1〜3（9件）を実装して公開まで反映**。
**本人の指示で段階3の途中（履歴）まででいったん停止**した。

**次のセッションの目標**: 「5. 未着手の範囲」の段階4〜7を上から順に進める。

---

### 2. 変更点（このセッションで実際に変更したもの）

コミット2件。push 済み（`origin/main` = `a0d5aa1`、19:36:59時点の `git log` で確認）。

| コミット | 内容 |
|---|---|
| `2c64349` | 段階1〜3（カイルンの人選び・JPAダブルス表・ホーム/種目/選手・履歴の作り直し） |
| `a0d5aa1` | 公開版の確認テストを新しい種目名に合わせる |

#### 完了した9件

| 指示 | やったこと |
|---|---|
| カイルンで選んだ人が反映されない | **値は入っていたが、プルダウンが縮まない指定のせいで名前の入力欄が30pxまで潰れて見えなかった**。入力欄を主役にして279pxに |
| 参加する人を選ぶページの横スクロール | 上と同じ原因。doc幅414→390で解消 |
| JPA 9ボールダブルスのスコア表 | 本人提供の画像を2次元表（`JPA_DOUBLES_9BALL`）として `data/handicap_data.js` に入れ、設定画面を「2人ぶんのSLを1人ずつ選ぶ」形に変更。**以前は「2人のSLの合計」で1次元表を引いており値が違っていた**。合計15超は組めないことも画面に出す |
| ホーム「新しい試合を始める」削除 | 削除（下のタブの「種目」と役目が重なる） |
| 中断中カードに×（ホーム） | ×で保存せずに閉じる |
| 中断中カードに×（種目ページ） | 同上 |
| 選手ページの文言 | 「対戦相手を登録する」→「選手を登録する」 |
| 14-1の種目名 | 「14-1（ストレートプール）」 |
| 履歴ページの修正（7件） | 開始・終了の時刻＋かかった時間／種目名を太字16px／W-Lをスコアの左右（例: たいら W 5 - 1 L たかのぶ）／JPAポイントは「P」なしで上のスコアと列をそろえる／マスワリ・セーフティは出した人の名前の下／ボウラードは獲得スコア・ストライク・スペア・ミス・経過時間（イニングなし）／上の「書き出し」「戻る」と下の3ボタンを削除 |

#### 触ったファイル

`index.html` / `css/v2.css` / `sw.js`(v23→v24) /
`data/handicap_data.js` / `data/games_data.js` /
`js/app.js` `js/store.js` `js/engine.js` `js/ui_setup.js` `js/ui_home.js`
`js/ui_history.js` `js/ui_kailun.js` /
`_test/tune5_test.py`（新設・38項目）と既存テスト9本の期待値更新

#### 変更した外部状態

- **GitHub の `main` を更新した**（`659a63d` → `a0d5aa1`）
- **GitHub Pages が再ビルドされ、公開URLの中身が入れ替わった**
- ローカルのデータ（localStorage）には触っていない。検証はすべてヘッドレスの一時プロファイル

---

### 3. 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| ローカルの検証**27本すべて成功**（node 4本＋実ブラウザ23本） | 各テストの終了コード0 | 19:05〜19:32 |
| 新規 `tune5_test.py` が **38/38** | 終了コード0 | 19:32 |
| JPAダブルス表が対称・合計15まで埋まり・例（SL4+6=24）が一致 | node で全81通りを突き合わせ | 19:10 |
| push が通り `origin/main` = `a0d5aa1` | `git log --oneline origin/main -1` | 19:36:59 |
| **公開版の `CACHE_VERSION` が `v24`**（push前は `v23`） | `curl` でキャッシュ回避パラメータ付き取得 | 19:36:59 |
| **公開URLでの実ブラウザ検証19項目がすべて成功** | `python _test/live_test.py` 終了コード0 | 19:35 |
| **公開URLで今回ぶん7項目がすべて成功**（カイルンの横スクロール／名前の欄の幅／JPAダブルス表／種目名／選手を登録する／新しい試合ボタンが無い／履歴の下部ボタンが無い） | 公開URL相手の実ブラウザ確認 | 19:35 |

**確認していないこと（完了扱いにしていない）**:
- **本人のスマホ実機では一度も見ていない。** 検証はヘッドレスChromium（390x844）のみ
- **iOS Safari では確認していない**
- **履歴の見た目（新しい3列レイアウト）を目視で確認したのは390x844の1サイズだけ。**
  縦の短い端末・横向きでは見ていない
- **JPAダブルスの表は本人提供の画像を読み取った値。** JPA公式PDFでの裏取りはしていない

---

### 4. 一次情報の取り直しコマンド（疑わしいときはまずこれ）

```
cd D:\Claudecode\pool-score

# いま公開されているものが新しいか（v24以上なら反映済み）
curl -s -A "Mozilla/5.0" "https://hiroshit164820-create.github.io/pool-score/sw.js?cb=%RANDOM%" | findstr CACHE_VERSION

# 公開版が壊れていないか（実ブラウザで19項目）
python _test\live_test.py

# 今回ぶんの検証だけ（38項目・2分ほど）
python _test\tune5_test.py

# ローカルの全検証（27本・25分ほどかかる）
_test\run_all.bat

# デプロイが詰まっていないか
gh run list --limit 3
```

**`git status` が clean でも「公開版が最新」とは限らない。** 必ず上の curl で実物を見る。

---

### 5. 未着手の範囲（**13件。ここが次の作業**）

段階4〜7。上から順に進めるのが安全（後ろほど大きい）。

#### 段階3の残り（1件・目安0.5h）
- **履歴の種目の絞り込みにハウスゲーム（5-9 / 5-10 / カイルン）を追加**
  → いまの `renderFilters()` は `STORE.listMatches()` からしか種目を集めていない。
  `STORE.listMoneyResults()` の `gameId` / `gameLabel` も足す必要がある

#### 段階4 配置図（2件・目安2.5h）
- **直線を引ける機能**（ボールの軌道を示すため）。**未着手**
- **ボールの番号が円の中心に来ず端に寄っている**（本人の画像2）。**未着手**

#### 段階5 横向きのスコアボード（1件だが中身は6つ・目安3h）
本人の指示そのまま:
- スコアボードを大きく（正方形に近い形）、フォントも拡大
- セーフティのボタンを各プレーヤーサイドへ移動
- ラック数とイニング数を上の帯へ移動
- ブレイク権の表示もプレーヤーサイドへ移動
- マスワリとエースのボタンをスコアボード横に、セーフティと同じカウント式にして
  各プレーヤーサイドへ配置

**いまの横向きは「AとBを左右に並べる」までしかやっていない**（2026-08-21 17:45の変更）。

#### 段階6 成績ページ（2件・目安2.5h）
- **「詳しい成績を見る」を「一般種目」と「JPA」に切り離す。**
  総合の勝敗は出しつつ、一般種目で〇勝●敗／JPAで〇勝●敗を別に出す
- **パートナー別の勝敗数と勝率を追加**

#### 段階7 「種目別でさらに詳しく」カード（目安12h以上）
既定は閉じたカード。10種目ぶん・約60項目。**すべて未着手**。

engine が既に記録している値（そのまま使える）:
`masuwari` `breakAce` `safety` `highRun` `shotClockShots/TotalSec`
`shotClockExtensions` `innings` `jpa.teamPoints` `skillLevel` `playerIds`

**新しく記録を足さないと出せないもの（過去の記録には遡って入らない）**:
- **ローテーションの「Aハイラン／Bハイラン」** — 用語の意味をまだ確認していない。
  **本人が「用語の意味は自分で確認して」と書いているので、実装前にWebで裏を取ること**
- **カイルンの最大連続得点** — 新しいカイルン画面は最終スコアしか保存していない
- **5-9 / 5-10 のマスワリ・ブレイクエース** — money 画面で記録していない
- **ボウラードの平均スコア（過去10/30/50回）** — `result.bowlard.total` は
  2026-08-21から保存しているので、**それ以降の記録からしか出せない**

---

### 6. 不確実な点・リスク

- **JPAダブルスの表は画像から目で読み取った値。**
  対称性と合計15の境目、本人の例（SL4+6=24）は突き合わせて確認したが、
  **個々の数字を公式資料と照合してはいない**
- **`js/ui_history.js` を大きく書き換えた。**
  カード下部のボタンを `appendFoot()` に切り出し、描画を
  「ボウラード用」と「対戦用」に分岐させた。ここを触るときは
  `tune5_test.py` と `stats2_test.py` `money2_test.py` の3本を必ず回すこと
- **中断中の試合の×は、確認を挟まずにその場で消す。**
  本人の指示（記録の保存はしない）どおりだが、**押し間違えても戻せない**
- **カイルンの通知は「場所を空けて」出す作りなので、出ている1.3秒の間だけ
  スコアのボタンが下にずれる。** 長押しの最中にずれると長押しが外れる
  （`kailun_test.py` で実際に起きたため、テスト側で通知が消えるのを待っている）。
  気になるようなら、カイルン画面だけ通知を重ねる方式に戻せる
- **`css/v2.css` が1200行を超えた。** `style.css` へ畳んでよいが、そのとき
  `js/app.js` の `loadExtraCss()` と `sw.js` の ASSETS からも消すこと
- **`redesign_test.py` の可読性検査は `style.css` しか見ていない。**
  `css/v2.css` に書いた規則は自動検査の対象外

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v24`）
  → ファイルを変更したら**必ず数字を上げる**。上げないとスマホに古い画面が残る
- **`data/handicap_data.js` の `JPA_DOUBLES_9BALL`**
  → 本人提供の表。**対称かつ合計15まで**という性質を壊さないこと
  （`tune5_test.py` が全81通りを検査している）
- **`js/ui_layout.js` の `RAIL_PX`（14）と `css/v2.css` の `--rail`**
  → 同じ値にしておくこと。ずらすとポイント（ダイヤ）がレールから外れる
- **`js/engine.js` の `result.innings`**
  → 「後攻→先攻に手番が戻った回数」。`engine.test.js` の122項目がこの意味で使っている。
  画面に出すのは `result.inningsPlayed`（= `innings + 1`）のほう
- **セット制の判定は `reduceMatch` のループの中だけ**
  → ループの外の `checkWin` は `setsToWin <= 1` のときしか通さない
- **`style.css` の可読性の制約**（タップ56px以上、本文16px以上、コントラスト4.5:1以上）
- **配置図の球は40px、番号は16px が下限**
- **`_test/run_all.bat` はUTF-8。** Shift-JISで書き戻すと見出しが化ける。
  **パスの `\t` `\n` をPythonの通常文字列経由で書くとタブ・改行に化ける**
  （`chr(92)` かraw文字列を使うこと）
- **書き出し／読み込みのボタンを消したが、処理そのもの（`exportJSON` / `onImportFile`）は
  `js/ui_history.js` に残してある。** バックアップが要るときはボタンを戻すだけで使える
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

**このセッションで下した主な判断**

1. **22件を7段階に分け、上から順に進めることにした** — 「種目別でさらに詳しく」
   だけで10種目・約60項目あり、1回では終わらないため
2. **カイルンの「反映されない」は表示の問題だった** — 値は正しく入っていた。
   同じ原因で横スクロールも出ていたので、2件を1つの修正で直した
3. **JPAダブルスは「合計SLで1次元表を引く」作りを捨てた** — 本人提供の表は
   2人のSLを縦横で見る2次元表で、値も合わなかった。UIも1人ずつ選ぶ形に変えた
4. **中断中の×は確認を挟まずに消す作りにした** — 本人の指示（記録の保存はしない）
   のとおり。押し間違えたときの復旧手段は無い
5. **履歴のJPAポイントは、W/Lの札と同じ幅の空きを左右に置いて列をそろえた** —
   「上のスコアと列を合わせて」という指示に対し、実測でズレ2px未満まで詰めた

**本人確認待ちの事項**:
- 履歴の新しい並び（W-Lの位置・JPAポイントの列・ボウラードの書き方）が意図どおりか
- 中断中の×が「確認なしで即消す」で問題ないか
- JPAダブルスの表の数字が実際の運用と合っているか
- **ローテーションの「Aハイラン／Bハイラン」の意味**（段階7で必要。
  Webで裏が取れなければ本人に聞く）

---

### 次の安全なアクション（1つだけ）

**「5. 未着手の範囲」の段階3の残り＝履歴の種目の絞り込みにハウスゲーム
（5-9 / 5-10 / カイルン）を追加する。**

`js/ui_history.js` の `renderFilters()` が `STORE.listMatches()` からしか
種目を集めていないので、`STORE.listMoneyResults()` の `gameId` / `gameLabel` も
足す。`renderMoneyResults()` 側の絞り込みは既に `filter.gameId` を見ているので、
選択肢を足すだけで効くはず（**「はず」なので実物で確認すること**）。

### 再開者が仮定してはいけないこと

- **「27本通った＝スマホで使える」と仮定しないこと。** 実機での確認は一度もしていない
- **「22件の指示が終わった」と仮定しないこと。** 完了は9件、未着手は13件
- **「JPAダブルスの表が公式どおり」と仮定しないこと。** 出所は本人提供の画像のみ
- **「種目別成績はデータが揃っている」と仮定しないこと。**
  Aハイラン・カイルンの最大連続得点・5-9のマスワリは**記録そのものが無い**
- **「ボウラードの平均スコアが過去ぶんも出せる」と仮定しないこと。**
  `result.bowlard.total` は2026-08-21以降の記録にしか入っていない
- **「`git status` が clean ＝公開版が最新」と仮定しないこと。** 4.の curl で実物を見る
- **`layout_fit_test.py` を2分で切らないこと。** 種目×画面の全数を回すため3分ほどかかる
- **落ちたテストを「本物の不具合」とも「flaky」とも決めつけないこと。** まず再実行する

---

## 2026-08-21（17:45時点）— 指示20件を実装し、**公開まで完了**

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開URL: https://hiroshit164820-create.github.io/pool-score/

本人が実機で触った結果の**指示20件をすべて実装し、ローカル26本・公開版19項目＋
今回ぶん7項目の検証を通し、push して公開まで反映した**。

**次のセッションの目標**: 本人が実機で再確認してからの指摘待ち。
新しい指示が無ければ「5. 未着手の範囲」から選ぶ。

---

### 2. 変更点（このセッションで実際に変更したもの）

コミット2件。push 済み（`origin/main` = `7c122f1`、17:43:21時点の `git log` で確認）。

| コミット | 内容 |
|---|---|
| `2b961ad` | 段階A〜C（台の潰れ・ローテのスコア・番号・説明文） |
| `7c122f1` | 段階D〜G（カイルン複数人・セット数・ボウラードの成績・横向き） |

#### 本人の指摘と、それぞれ何をしたか

| 指摘 | やったこと |
|---|---|
| 配置図がまだ潰れる | **CSSの「高さ→幅」任せをやめ、JSで実測して寸法を入れる方式にした**（`fitTable()`）。手元のChromium/Firefox/WebKit・14サイズでは再現しなかったので、環境に依存しない計算に置き換えた。ラシャが1:2、外にレール14px |
| プルダウンから30点以下を削除 | 40点以上だけにした。少ない点数は種目ごとのボタン（カイルン5点など）から選ぶ |
| ボウラードはイニングでなくストライク／スペア／ミス | 結果画面・履歴・CSVを差し替えた（`bowlardTally()`） |
| 説明文を1行に | **全数で洗い直して16か所を短くし、2行以上を0件にした**（9種目×設定・6種目×試合・5画面） |
| ローテのスコアがスマホで出ない | **手元でも再現**。`.panel-wrap` が `flex-basis:0` で「高さ0」と数えられ、スコアボードが22pxに潰れていた。**前回の変更とは無関係の既存不具合** |
| ポイントをレールの上に | 目印の箱をレールまで広げ、ダイヤ18個をすべて茶色のレール上に置いた |
| ラシャをグレーに | `rgb(125,125,125)` |
| カイルンのルール選択2つを削除 | 「1回の手番で」「ミスしたとき」を削除。「反則のとき」は残した |
| カイルンを複数人に | **`js/ui_kailun.js` を新設**（2〜6人）。engine はA/B2サイド固定で人数を増やせないため、5-9/5-10と同じ専用画面にした |
| 交代ボタンを1つに・成功を大きく | 成功（96px）／交代／反則の3つ。以前は交代が2か所にあった |
| 成功ボタンの3段階表示 | 1段目「赤→黄 or 黄→赤」／2段目「赤→赤」／3段目「全部（1点）」。色の丸も出す。1点取るか交代で1段目に戻る |
| カイルンのスコアをタッチで加減算 | タップで+1、長押しで-1 |
| セット数を追加 | 勝利条件のすぐ下に1〜5を1行で。既定1（＝今までどおり）。engine がセットごとに点とラックを戻し、規定セット先取で終了 |
| 番号を白地・黒枠・黒文字の丸に | どのセットでも統一。三角・菱形の枠はやめた |
| アラミスブラックの9番以降 | 「真ん中が色・上下が黒」に変えた |
| 横向きでA左・B右 | 横向きのときだけ左右に並べる。縦向きは変えていない |

#### 新しく増えたファイル

| ファイル | 役割 |
|---|---|
| `js/ui_kailun.js` | カイルン（複数人）の専用画面 |
| `_test/tune4_test.py` | 今回ぶんの検証（40項目） |

`_test/kailun_test.py` は新画面向けに全面的に書き直した（39項目）。

#### 変更した外部状態

- **GitHub の `main` を更新した**（`dc20eb9` → `7c122f1`）
- **GitHub Pages が再ビルドされ、公開URLの中身が入れ替わった**
- ローカルのデータ（localStorage）には触っていない。検証はすべてヘッドレスの一時プロファイル

---

### 3. 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| ローカルの検証**26本すべて成功**（node 4本＋実ブラウザ22本） | 各テストの終了コード0 | 17:20〜17:40 |
| 新規 `tune4_test.py` が **40/40**、書き直した `kailun_test.py` が **39/39** | 終了コード0 | 17:35 / 17:30 |
| push が通り `origin/main` = `7c122f1` | `git log --oneline origin/main -1` | 17:43:21 |
| **公開版の `CACHE_VERSION` が `v23`**（push前は `v22`） | `curl` でキャッシュ回避パラメータ付き取得 | 17:44:52 |
| 公開版で `js/ui_kailun.js` が HTTP 200、`index.html` に `screenKailunMatch`、`engine.js` に `bowlardTally` | `curl` + `grep -c` | 17:45:04 |
| **公開URLでの実ブラウザ検証19項目がすべて成功** | `python _test/live_test.py` 終了コード0 | 17:45 |
| **公開URLで今回ぶん7項目がすべて成功**（台1:2・グレー・ローテのスコア・番号・セット数・カイルンの画面） | 公開URL相手の実ブラウザ確認 | 17:46 |

**確認していないこと（完了扱いにしていない）**:
- **本人のスマホ実機では一度も見ていない。**
  検証はヘッドレスChromium（280〜980幅、縦横）と、比率だけFirefox/WebKitでも確認
- **iOS Safari の実機では確認していない**
- **台が潰れた現象そのものは再現できていない。**
  作りを「実測して入れる」方式に変えたので直っているはずだが、**原因の断定はできていない**

---

### 4. 一次情報の取り直しコマンド（疑わしいときはまずこれ）

```
cd D:\Claudecode\pool-score

# いま公開されているものが新しいか（v23以上なら反映済み）
curl -s -A "Mozilla/5.0" "https://hiroshit164820-create.github.io/pool-score/sw.js?cb=%RANDOM%" | findstr CACHE_VERSION

# 公開版が壊れていないか（実ブラウザで19項目）
python _test\live_test.py

# 今回ぶんの検証だけ（40項目・2分ほど）
python _test\tune4_test.py

# カイルンだけ（39項目）
python _test\kailun_test.py

# ローカルの全検証（26本・20分ほどかかる）
_test\run_all.bat

# デプロイが詰まっていないか
gh run list --limit 3
```

**`git status` が clean でも「公開版が最新」とは限らない。** 必ず上の curl で実物を見る。

---

### 5. 未着手の範囲

- **カイルンの記録は「成績」に出ない。** 5-9 / 5-10 と同じ入れ物（`pool_money_results`）に
  保存しており、履歴と「表計算へ」には出るが、種目別成績の集計には入っていない
- **5-9/5-10 の記録も同じく成績に出ない**
- **8ボールの「1ボールハンデ」は得点計算に効いていない**（記録と表示のみ）
- **JPA 8ボールのポイントは本人の説明と実運用の確認のみが根拠。** 公式資料での確認は未取得
- **JCL 9ボール/8ボールは `games_data.js` に定義だけあって種目選択に出していない**
  （目標点が手入力のため）
- **セット制はラック先取・点数先取の一般種目のみ。** JPAとボウラードには付けていない
- 実機での確認全般

---

### 6. 不確実な点・リスク

- **配置図の潰れは原因を断定できていない。** 手元の3エンジン×14サイズで再現せず、
  作りを変えることで対処した。**再発したら、まず `fitTable()` が測っている
  `.lay-stage` の `clientWidth` を実機で確かめる**
- **カイルンは engine を通らなくなった。** `engine.js` の stepMachine の実装と
  `engine.test.js` の該当テストは残っているが、画面からは使われていない。
  消してよいが、消すときは engine.test.js の該当分も一緒に外すこと
- **セット制は engine の途中で点とラックを0に戻している。**
  セーフティ・マスワリなどの記録はセットをまたいで通算のまま。
  セットごとに分けたいなら別途作りが要る
- **横向きの詰めは `@media (orientation: landscape) and (max-height: 560px)` の中だけ。**
  タブレットの横向き（高さ560px超）は縦向きと同じ並びのまま
- **`[hidden] { display: none !important; }` は前回入れたものが効いたまま**
- **`css/v2.css` が 1000行を超えた。** `style.css` へ畳んでよいが、そのとき
  `js/app.js` の `loadExtraCss()` と `sw.js` の ASSETS からも消すこと
- **`redesign_test.py` の可読性検査は `style.css` しか見ていない。**
  `css/v2.css` に書いた規則は自動検査の対象外

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v23`）
  → ファイルを変更したら**必ず数字を上げる**。上げないとスマホに古い画面が残る
- **`js/ui_layout.js` の `RAIL_PX`（14）と `css/v2.css` の `--rail`**
  → 同じ値にしておくこと。ずらすとポイント（ダイヤ）がレールから外れる
- **`data/` 配下の数値**（`handicap_data.js` のJPA表など）
  → 一次情報で確認した値。`JPA_TEAM_POINT_BANDS[9]` は本人が確定済み
- **`js/engine.js` の `result.innings`**
  → 「後攻→先攻に手番が戻った回数」。`engine.test.js` の122項目がこの意味で使っている。
  画面に出すのは `result.inningsPlayed`（= `innings + 1`）のほう
- **セット制の判定は `reduceMatch` のループの中だけ**
  → ループの外の `checkWin` は `setsToWin <= 1` のときしか通さない。
  外すと、点を0に戻した直後に「0点で勝ち」と判定されてしまう
- **`style.css` の可読性の制約**（タップ56px以上、本文16px以上、コントラスト4.5:1以上）
  → `redesign_test.py` が自動検査している。美観のために下げないこと
- **配置図の球は40px、番号は16px が下限**
- **`_test/run_all.bat` はUTF-8。** Shift-JISで書き戻すと見出しが化ける。
  **パスの `\t` `\n` をPythonの通常文字列経由で書くとタブ・改行に化ける**
  （`chr(92)` かraw文字列を使うこと）
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

**このセッションで下した主な判断**

1. **配置図は「再現できないまま作りを変える」ことにした** — 3エンジン×14サイズで
   再現しなかったため、原因追及より先に環境に依存しない計算へ置き換えた
2. **カイルンは engine に人数を足さず、専用画面を新設した** — engine はA/B2サイドが
   前提で、そこへ人数を入れると全種目の計算に影響が出る。5-9 と同じ形にそろえた
3. **カイルンの記録は 5-9 と同じ入れ物に入れた** — 履歴とCSVがそのまま使えるため。
   代わりに種目別成績には出ない（5.に記載）
4. **セット制は「点とラックだけ戻す」設計にした** — セーフティ等の記録まで戻すと
   試合全体の集計が壊れる
5. **説明文は「1行に収まる長さ」を実測で確かめた** — 文字数ではなく、
   実ブラウザで行数（Rangeの矩形の数）を数えて0件まで詰めた
6. **横向きでは「● 撞く番」の札を「●」だけにした** — 札が名前の場所を奪って
   スコアが枠の外へ押し出されていたため

**本人確認待ちの事項**:
- 配置図が実機でも潰れなくなったか（**ここが今回いちばん確認してほしい点**）
- カイルンの成功ボタンの文言と段階の進み方が実際の遊び方と合っているか
- セット数の意味が「何セット先取で試合の勝ち」で合っているか
- 横向きの詰め方（「● 撞く番」を「●」だけにした）が許容できるか

---

### 次の安全なアクション（1つだけ）

**本人がスマホの実機で https://hiroshit164820-create.github.io/pool-score/ を開き、
今回の20件が意図どおりか確認する。**

すでにホーム画面に追加している場合は、`CACHE_VERSION` を v23 に上げてあるので
開き直せば新しい画面に入れ替わる。入れ替わらなければ一度削除して追加し直す。

とくに見てほしい点は5つ。

1. **配置図**（台が潰れていないか／グレーのラシャ／ポイントがレールの上）
2. **ローテーション**（スコアが出るようになったか）
3. **カイルン**（3人以上で始められるか／成功ボタンの3段階）
4. **セット数**（勝利条件の下のボタン。3セット先取などが意図どおりか）
5. **横向き**（Aが左・Bが右。1画面に収まるか）

### 再開者が仮定してはいけないこと

- **「検証26本が通った＝スマホで使える」と仮定しないこと。** 実機での確認は一度もしていない
- **「台の潰れの原因が分かった」と仮定しないこと。** 再現できていない
- **「Chromiumで直った＝iOS Safariでも直った」と仮定しないこと**
- **「`git status` が clean ＝公開版が最新」と仮定しないこと。** 4.の curl で実物を見る
- **「カイルンの記録が成績に出ている」と仮定しないこと。** 履歴には出るが集計には入っていない
- **「セット制がJPAでも動く」と仮定しないこと。** 一般種目だけに付けている
- **`layout_fit_test.py` を2分で切らないこと。** 種目×画面の全数を回すため3分ほどかかる
- **落ちたテストを「本物の不具合」とも「flaky」とも決めつけないこと。** まず再実行する

---

## 2026-08-20（16:27時点）— 第3便9件を実装し、**公開まで完了**

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開URL: https://hiroshit164820-create.github.io/pool-score/

本人がスマホ実機で触った結果の**第3便の指摘9件をすべて実装し、
ローカル25本・公開版19項目＋今回ぶん8項目の検証を通し、push して公開まで反映した**。

**次のセッションの目標**: 本人が実機で再確認してからの指摘待ち。
新しい指示が無ければ「5. 未着手の範囲」から選ぶ。

---

### 2. 変更点（このセッションで実際に変更したもの）

コミット1件 `bbf4d1c`。push 済み（`origin/main` = `bbf4d1c`、16:25:52時点の `git log` で確認）。

#### 本人の指摘と、それぞれ何をしたか

| 指摘 | やったこと |
|---|---|
| スマホで配置図が壊れる。球の上下に線が入る | **原因は `button::after`**。8bit風の立体感を出すため上下に明暗の線を敷いているが、四角のまま円い球にも乗っていた。`border-radius: inherit` を足して球の陰影に変えた（試合画面の球ボタンも同時に直る） |
| テーブルをできる限り大きく、球を少し小さく | 台を 218px → **242px**（390幅の端末）。左右のボタン列を 76px → 60px に細くし、外側の余白ぶんも台へ回した。球は 44px → **40px**、番号の札は 26px → 24px |
| ボールセットの項目は削除 | 「使うボール」の欄・見出し・まとめの行を削除。**配色は標準セットのまま盤面に効き、試合記録にも残る** |
| 通知が気になる。2.6秒は長い | 表示を **1.3秒**に短縮。さらに試合画面（通常の試合・5-9/5-10）では**重ねずに帯の直下へ差し込み、その高さぶんスコア側を縮める**。スコアも名前も覆わなくなった |
| 獲得ポイントが2行になる／「JPA公式の〜」削除 | 見出しを「獲得ポイント（JPA）」→「**JPAポイント**」に短くして1行に収めた。名前と点数の塊は途中で折り返さない。説明文は削除 |
| スコア表示が重複、上側を削除 | 終了画面の上側にあった「A 75点 対 B 32点」の行を削除 |
| 8ボールの説明削除 | 「スコンクは3-0、リーチまで来ていたら2-1…」の説明文を削除 |
| 14-1では「3-7先」を表示しない | 点数で決める種目からラック用のボタンを外した。代わりに**その種目で実際に使う点数**（14-1なら50/100/150）をボタンにした。プルダウンには1〜9点も足した（少ない点数の勝負・ハンデ戦が組めなくなるため） |
| ボウラードでは勝利条件とブレイクを非表示 | 見出しと欄を消した。**`hidden` を付けても消えていなかったのは、`.section-title` の `display` 指定がブラウザ既定の `[hidden]{display:none}` に勝っていたため**。`[hidden]{display:none !important}` を1行入れて全体で効くようにした |

#### 触ったファイル

| ファイル | 変更 |
|---|---|
| `index.html` | `#ballSetSection` 削除。ブレイクの見出しに `id="breakTitle"`、先攻の欄に `id="firstSideField"` |
| `css/v2.css` | `[hidden]` の強制、`button::after` の角丸、台とボタン列の寸法、球のサイズ、通知の押しのけ、終了画面の折り返し |
| `js/ui_setup.js` | `renderBallSet()` 削除、通知（`TOAST_MS` / `TOAST_INFLOW_SCREENS`）、`quickGoalValues()` 新設、ボウラードのブレイク非表示、点数のプルダウンに1〜9追加 |
| `js/ui_match.js` | 終了画面の重複行削除・見出し変更・説明文削除・値を塊で組む |
| `sw.js` | `CACHE_VERSION` を **v21 → v22** |
| `_test/tune3_test.py` | **新設（32項目）** |
| `_test/helpers.py` | `set_goal` / `goal_value` を「50点先取」表記にも対応 |
| `_test/session_test.py` `layout_touch_test.py` `stats2_test.py` `money2_test.py` `run_all.bat` | 上の変更に合わせて更新（25本目を追加） |

#### 変更した外部状態

- **GitHub の `main` を更新した**（`df67053` → `bbf4d1c`）
- **GitHub Pages が再ビルドされ、公開URLの中身が入れ替わった**
- ローカルのデータ（localStorage）には触っていない。検証はすべてヘッドレスの一時プロファイル

---

### 3. 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| ローカルの検証**25本すべて成功**（node 4本＋実ブラウザ21本） | 各テストの終了コード0 | 15:40〜16:20 |
| 新規 `tune3_test.py` が **32/32 成功** | 終了コード0 | 16:18 |
| push が通り `origin/main` = `bbf4d1c` | `git log --oneline origin/main -1` | 16:25:52 |
| **公開版の `CACHE_VERSION` が `v22`**（push前は `v21`） | `curl` でキャッシュ回避パラメータ付き取得 | 16:26:47 |
| 公開版の `index.html` から `ballSetSection` が消え、`breakTitle` が入っている | `curl` + `grep -c` | 16:26:56 |
| 公開版の `css/v2.css` に `border-radius: inherit`、`js/ui_setup.js` に `TOAST_MS = 1300` | 同上 | 16:26:56 |
| **公開URLでの実ブラウザ検証19項目がすべて成功** | `python _test/live_test.py` 終了コード0 | 16:28 |
| **公開URLで今回ぶん8項目がすべて成功**（球の陰影・台242px・球40px・使うボール無し・14-1に3先無し・ボウラード・通知の重なり・通知の長さ） | 公開URL相手の実ブラウザ確認 | 16:30 |

**確認していないこと（完了扱いにしていない）**:
- **本人のスマホ実機では一度も見ていない。** 画面幅390/360/412のヘッドレスChromiumのみ
- **すでにホーム画面に追加している端末で入れ替わるか**は未確認
  （`CACHE_VERSION` を v22 に上げてあるので開き直せば入れ替わる想定）
- **iOS Safari では確認していない。** 検証はすべてChromium

---

### 4. 一次情報の取り直しコマンド（疑わしいときはまずこれ）

```
cd D:\Claudecode\pool-score

# いま公開されているものが新しいか（v22以上なら反映済み）
curl -s -A "Mozilla/5.0" "https://hiroshit164820-create.github.io/pool-score/sw.js?cb=%RANDOM%" | findstr CACHE_VERSION

# 公開版が壊れていないか（実ブラウザで19項目）
python _test\live_test.py

# 今回ぶんの検証だけ（32項目・1分ほど）
python _test\tune3_test.py

# ローカルの全検証（25本・15分ほどかかる）
_test\run_all.bat

# デプロイが詰まっていないか
gh run list --limit 3
```

**`git status` が clean でも「公開版が最新」とは限らない。** 必ず上の curl で実物を見る。

---

### 5. 未着手の範囲

- **JPA 8ボールのポイントは本人の説明のみが根拠。** 公式資料での確認は取れていない
  （2026-08-20に本人が「OK」と実運用で確認済み。ただし公式資料の裏は取っていない）
- **8ボールの「1ボールハンデ」は得点計算に効いていない**（記録と表示のみ）。
  効かせるには engine にグループ球の管理を足す必要がある
- **5-9/5-10 の記録は「種目ごとの成績」に出ていない。**
  `pool_money_results` に別で保存しており、履歴画面には出るが成績の集計には入っていない
- **カイルンとJCLの残り** — JCL 9ボール/8ボールは `games_data.js` に定義だけあって
  種目選択に出していない（目標点が手入力のため）
- 実機での確認全般

---

### 6. 不確実な点・リスク

- **通知の「場所を空ける」方式は試合画面の2つだけに入れてある**
  （`screenMatch` と `screenMoneyMatch`）。他の画面は今までどおり帯の下に固定で出る。
  一覧の途中まで下にスクロールしていると通知が画面の外にある、という状態は起こりうる。
  試合中ではないので実害は小さいと判断した
- **`[hidden] { display: none !important; }` を全体に効かせた。**
  今まで「hidden を付けたのに CSS の display が勝って見えていた」場所が
  他にもあれば、このコミットで一斉に消える。25本の検証では何も落ちていないが、
  もし「あったはずのものが消えた」と言われたらまずここを疑う
- **台を大きくしたぶん、縦640pxの端末では配置図が画面に収まらずスクロールする。**
  変更前も収まっていなかった（358px時点で既に超過）ので悪化ではない
- **`css/v2.css` を `style.css` と分けたままにしてある。**
  畳んでよいが、そのとき `js/app.js` の `loadExtraCss()` と `sw.js` の ASSETS からも消すこと
- **`redesign_test.py` の可読性検査は `style.css` しか見ていない。**
  `css/v2.css` に書いた規則は自動検査の対象外

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v22`）
  → ファイルを変更したら**必ず数字を上げる**。上げないとスマホに古い画面が残る
- **`data/` 配下の数値**（`handicap_data.js` のJPA表など）
  → 一次情報で確認した値。**`JPA_TEAM_POINT_BANDS[9]` は本人が「今のコードが正しい」と確定済み**
- **`js/engine.js` の `result.innings`**
  → 「後攻→先攻に手番が戻った回数」。`engine.test.js` の122項目がこの意味で使っている。
  画面に出すのは `result.inningsPlayed`（= `innings + 1`）のほう
- **`style.css` の可読性の制約**（タップ56px以上、本文16px以上、コントラスト4.5:1以上）
  → `redesign_test.py` が自動検査している。美観のために下げないこと
- **配置図の球は40px、番号は16px が下限。** これ以上小さくすると指で掴めない・番号が読めない
  （`layout_touch_test.py` と `layout2_test.py` が検査している）
- **`_test/run_all.bat` はUTF-8。** Shift-JISで書き戻すと見出しが化ける。
  **パスの `\t` `\n` をPythonの通常文字列経由で書くとタブ・改行に化ける**
  （今回また `_test\tune3_test.py` がタブに化けた。`chr(92)` かraw文字列を使うこと）
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

**このセッションで下した主な判断**

1. **球の上下の線は、球ではなくボタン共通の装飾が原因だった** — 個別に潰さず
   `button::after { border-radius: inherit }` で直したので、試合画面の球ボタンなど
   他の円いボタンも同時に直っている
2. **通知は「短くする」と「重ねない」を両方やった** — 本人の「気になる。2.6秒は長い」に
   対して、時間だけ縮めても重なりは残るため。重ねない方式は試合画面だけに絞った
3. **点数で決める種目のボタンを空にせず、種目ごとの点数に置き換えた** —
   指示は「3-7先を出すな」だが、ボタンを全部消すとプルダウンだけになって
   台の脇で選びづらくなるため。14-1は50/100/150、カイルンは5点
4. **点数のプルダウンに1〜9点を足した** — 「3先」ボタンが無くなったことで
   カイルンの3点先取などが選べなくなっていた（`kailun_test.py` が検出）
5. **`[hidden]` を `!important` で全体に効かせた** — ボウラードの見出しだけ
   個別に消す手もあったが、同じ罠が他にもある作りなので元から断った

**本人確認待ちの事項**:
- 配置図の台の大きさと球の大きさが実機で狙いどおりか
- 通知の1.3秒が短すぎないか（読み切れるか）
- 14-1の勝利条件ボタン（50/100/150点先取）が実際の運用と合っているか

---

### 次の安全なアクション（1つだけ）

**本人がスマホの実機で https://hiroshit164820-create.github.io/pool-score/ を開き、
今回の9件が意図どおりか確認する。**

すでにホーム画面に追加している場合は、`CACHE_VERSION` を v22 に上げてあるので
開き直せば新しい画面に入れ替わる。入れ替わらなければ一度削除して追加し直す。

とくに見てほしい点は4つ。

1. **配置図**（球の上下の線が消えたか／台と球の大きさの釣り合い）
2. **通知**（スコアに重ならなくなったか／1.3秒で読み切れるか）
3. **14-1の勝利条件**（50点先取／100点先取／150点先取）
4. **ボウラード**（勝利条件とブレイクの項目が消えたか）

### 再開者が仮定してはいけないこと

- **「検証25本が通った＝スマホで使える」と仮定しないこと。** 実機での確認は一度もしていない
- **「Chromiumで直った＝iOS Safariでも直った」と仮定しないこと。** iOSでは確認していない
- **「`git status` が clean ＝公開版が最新」と仮定しないこと。** 4.の curl で実物を見る
- **「JPA 8ボールのポイントは公式どおり」と仮定しないこと。** 根拠は本人の説明と実運用の確認のみ
- **「5-9の記録が成績に出ている」と仮定しないこと。** 履歴には出るが成績の集計には入っていない
- **`layout_fit_test.py` を2分で切らないこと。** 種目×画面の全数を回すため3分ほどかかる
- **落ちたテストを「本物の不具合」とも「flaky」とも決めつけないこと。** まず再実行する

---

## 2026-08-20（14:52時点）— 指示43件を実装し、**公開まで完了**

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開URL: https://hiroshit164820-create.github.io/pool-score/

本人から2便に分けて受けた**指示43件（第1便30件＋第2便13件）をすべて実装し、
ローカル24項目・公開版19項目の検証を通し、push して公開まで反映した**。

**次のセッションの目標**: 本人が実機で触ってからの指摘待ち。
新しい指示が無ければ「5. 未着手の範囲」から選ぶ。

---

### 2. 変更点（このセッションで実際に変更したもの）

コミット6件。すべて push 済み（`origin/main` = `6a42887`、14:51:39時点の `git log` で確認）。

| コミット | 内容 |
|---|---|
| `005b8c4` | 配置図7件＋選手一覧のボタン撤去（第1便 指示9〜15） |
| `03323d3` | 設定画面8件（第1便 指示1〜8）＋`run_all.bat` の壊れたパスを修復 |
| `b474c73` | JPA・成績・履歴14件（第1便 指示16〜29） |
| `c0a3c79` | 引き継ぎ更新 |
| `d060455` | JPA8ボールのポイント・5-9/5-10の作り直し・全体共通3件（第2便13件） |
| `6a42887` | 引き継ぎ更新 |

#### 新しく増えたファイル

| ファイル | 役割 |
|---|---|
| `css/v2.css` | 追加ぶんのスタイル。`js/app.js` の `loadExtraCss()` が読み込む |
| `js/csv.js` | 成績・履歴・種目別成績・5-9の記録をCSVに書き出す |
| `js/ui_gamestats.js` | 種目ごとの成績の画面 |
| `_test/layout2_test.py` | 配置図（30項目） |
| `_test/setup2_test.py` | 設定画面（29項目） |
| `_test/stats2_test.py` | JPA・成績・履歴（52項目） |
| `_test/money2_test.py` | 5-9/5-10・JPA8ボール・通知の位置（51項目） |

#### 変更した外部状態

- **GitHub の `main` ブランチを更新した**（`593ae67` → `6a42887`）
- **GitHub Pages が再ビルドされ、公開URLの中身が入れ替わった**
- ローカルのデータ（localStorage）には触っていない。検証はすべてヘッドレスの一時プロファイル

---

### 3. 検証済みの証拠（出所と観測時刻つき）

| 確認したこと | 出所 | 観測時刻 |
|---|---|---|
| ローカルの検証24項目がすべて成功（node 4本＋実ブラウザ20本） | 各テストの終了コード0 | 14:20〜14:45 |
| `layout_fit_test.py` 32項目成功（種目×画面の全数、1画面に収まる） | 同上 | 14:38 |
| push が通り `origin/main` = `6a42887` | `git log --oneline origin/main -1` | 14:51:39 |
| Pages のビルドが `completed/success`（run `32337123345`） | `gh run list` | 14:51:39 |
| **公開版の `CACHE_VERSION` が `v21`**（push前は `v19`） | `curl` でキャッシュ回避パラメータ付き取得 | 14:51:39 |
| 新規ファイル4本が公開URLで HTTP 200（`css/v2.css` `js/csv.js` `js/ui_gamestats.js` `js/ui_money.js`） | `curl -o /dev/null -w "%{http_code}"` | 14:51 |
| 公開版の `index.html` に `js/csv.js` `js/ui_gamestats.js` の script と `startSummary` `moneyPlus` が入っている | `curl` + `grep` | 14:51 |
| **公開URLでの実ブラウザ検証19項目がすべて成功** | `python _test/live_test.py` 終了コード0 | 14:52 |

**確認していないこと（完了扱いにしていない）**:
- **本人のスマホ実機では一度も見ていない。** 画面幅390/360のヘッドレスChromiumのみ
- **すでにホーム画面に追加している端末で入れ替わるか**は未確認
  （`CACHE_VERSION` を v21 に上げてあるので開き直せば入れ替わる想定）

---

### 4. 一次情報の取り直しコマンド（疑わしいときはまずこれ）

```
cd D:\Claudecode\pool-score

# いま公開されているものが新しいか（v21以上なら反映済み）
curl -s -A "Mozilla/5.0" "https://hiroshit164820-create.github.io/pool-score/sw.js?cb=%RANDOM%" | findstr CACHE_VERSION

# 公開版が壊れていないか（実ブラウザで19項目）
python _test\live_test.py

# ローカルの全検証（24項目・10分ほどかかる）
_test\run_all.bat

# デプロイが詰まっていないか
gh run list --limit 3
```

**`git status` が clean でも「公開版が最新」とは限らない。** 必ず上の curl で実物を見る。

---

### 5. 未着手の範囲

- **JPA 8ボールのポイントは本人の説明のみが根拠。** 公式資料での確認は取れていない
- **8ボールの「1ボールハンデ」は得点計算に効いていない**（記録と表示のみ）。
  効かせるには engine にグループ球の管理を足す必要がある
- **5-9/5-10 の記録は「種目ごとの成績」に出ていない。**
  `pool_money_results` に別で保存しており、履歴画面には出るが成績の集計には入っていない
- **カイルンとJCLの残り** — JCL 9ボール/8ボールは `games_data.js` に定義だけあって
  種目選択に出していない（目標点が手入力のため）
- 実機での確認全般

---

### 6. 不確実な点・リスク

- **通知は帯を塞がなくなったが、代わりにスコアの上に2.6秒だけ重なる。**
  文字幅ぶんの小さな札にして覆う面積は減らしたが、5-9の画面はスコア欄が低く重なりが大きい。
  本人に伝え済み。避けたい場合は「通知が出ている間だけ場所を空ける」作りに変えられる
- **プレイヤー選択の「最近使った5人」は、5人に満たないと登録の新しい順で埋めている。**
  指示の文言そのままではない。登録直後の人がボタンに1人も出ず選べなくなったため足した
  （`features_test.py` が検出）。不要なら `splitPlayers()` の埋め込み部分を外す
- **`css/v2.css` を `style.css` と分けたままにしてある。**
  別セッションと同時編集していたときの衝突回避のため。畳んでよいが、そのとき
  `js/app.js` の `loadExtraCss()` と `sw.js` の ASSETS からも消すこと
- **`redesign_test.py` の可読性検査は `style.css` しか見ていない。**
  `css/v2.css` に書いた規則は自動検査の対象外

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v21`）
  → ファイルを変更したら**必ず数字を上げる**。上げないとスマホに古い画面が残る
- **`data/` 配下の数値**（`handicap_data.js` のJPA表など）
  → 一次情報で確認した値。**`JPA_TEAM_POINT_BANDS[9]` は本人が「今のコードが正しい」と確定済み**
- **`js/engine.js` の `result.innings`**
  → 「後攻→先攻に手番が戻った回数」。`engine.test.js` の122項目がこの意味で使っている。
  画面に出すのは `result.inningsPlayed`（= `innings + 1`）のほう
- **`style.css` の可読性の制約**（タップ56px以上、本文16px以上、コントラスト4.5:1以上）
  → `redesign_test.py` が自動検査している。美観のために下げないこと
- **`_test/run_all.bat` はUTF-8。** Shift-JISで書き戻すと見出しが化ける。
  パスの `\` をPythonのヒアドキュメント経由で書くと消えるので注意
  （実際に `note_test.py` `tabbar_test.py` の2行がこれで壊れていた）
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

**このセッションで下した主な判断**

1. **同時に動いていた別セッションと衝突しないよう、触るファイルを分けた**
   （12:25時点で相手が5ファイルを編集中だった）。12:35に相手が完了したのを
   `git log` で確認してから残りに着手した。着手前に
   `D:\Claudecode\_backup\pool-score_20260820_1226\` へ全体をコピーしてある
2. **試合中の帯にセーフティ数を出さないことにした** — 出すと360x640で帯が3行になり、
   下の操作ボタンが26pxはみ出した。指示はセーフティを結果・成績・履歴に求めている
3. **`result.inningsPlayed` を新設した** — engine の `innings` の意味は変えずに、
   画面の「Nイニング目」と一致させるため
4. **5-9/5-10 の記録を別の保存場所にした** — 3人以上で遊ぶため
   A/B2サイドの試合記録には収まらない
5. **`run_all.bat` の壊れを直した** — `[14/20]` で必ず失敗して
   15〜20番の検証が一度も走っていなかった

**本人確認待ちの事項**:
- 通知がスコアに重なる件（上記6.）を、このままにするか場所を空けるか
- プレイヤー選択の「登録の新しい順で埋める」を残すか外すか

---

### 次の安全なアクション（1つだけ）

**本人がスマホの実機で https://hiroshit164820-create.github.io/pool-score/ を開き、
今回の43件が意図どおりか確認する。**

すでにホーム画面に追加している場合は、`CACHE_VERSION` を v21 に上げてあるので
開き直せば新しい画面に入れ替わる。入れ替わらなければ一度削除して追加し直す。

とくに見てほしい点は5つ。

1. **配置図**（ポケットとダイヤ、左右のボタン、盤面の大きさ、球の番号の読みやすさ）
2. **設定画面の「この内容で始めます」** が読み上げどおりになっているか
3. **5-9の得点入力**（+1〜+16、-1/-2）が台の脇で押しやすいか
4. **通知がスコアに重なるのが気になるか**
5. **JPA 8ボールのポイント**（3-0 / 2-1 / 2-0）が実際の運用と合っているか

### 再開者が仮定してはいけないこと

- **「検証24項目が通った＝スマホで使える」と仮定しないこと。** 実機での確認は一度もしていない
- **「`git status` が clean ＝公開版が最新」と仮定しないこと。**
  4.の curl で実物の `CACHE_VERSION` を見る
- **「JPA 8ボールのポイントは公式どおり」と仮定しないこと。** 根拠は本人の説明のみ
- **「5-9の記録が成績に出ている」と仮定しないこと。** 履歴には出るが成績の集計には入っていない
- **`layout_fit_test.py` を2分で切らないこと。** 種目×画面の全数を回すため3分ほどかかる
- **落ちたテストを「本物の不具合」とも「flaky」とも決めつけないこと。** まず再実行する

---

## 2026-08-20（14:00時点）— 指示30件を実装（この時点では未デプロイ。上の14:52版で公開済み）

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開済み: https://hiroshit164820-create.github.io/pool-score/

本人から受けた**指示30件をすべて実装し、23項目の検証をすべて通した**。
**まだ push していない。公開URLには反映されていない。**

**次の安全なアクション**: 本人に push（＝公開への反映）の可否を確認する。
本人が「出していい」と言えば `git push` するだけ。

---

### 2. このセッションの前提（同時実行の衝突）

**もう1つのセッションが同じリポジトリを同時に編集していた。**
着手時（12:25）の観測は次のとおり。

| 事実 | 出所 |
|---|---|
| `index.html` `style.css` `js/ui_setup.js` `data/games_data.js` `sw.js` が未コミットで変更中 | `git status` |
| 新規未追跡ファイル `js/money_game.js` `js/ui_money.js` | 同上 |
| 最終更新 12:16〜12:17 | ファイル更新時刻 |

**取った対策**（同じ状況が再発したときのために残す）:

1. 着手前に作業ツリー全体を `D:\Claudecode\_backup\pool-score_20260820_1226\` へコピーした
2. 衝突する5ファイルには触らず、衝突しないファイル（`ui_layout.js` 等）から着手した
3. 追加ぶんのCSSは `style.css` ではなく **新規 `css/v2.css`** に置き、`js/app.js` から
   動的に読み込ませた（`index.html` を触らずに済ませるため）
4. 12:35 にもう1つのセッションが `593ae67` で完了・コミットしたのを `git log` で確認し、
   そこから残りの項目に着手した

**現在 `css/v2.css` は残したままにしてある。** 読み込み順は `style.css` の後で、
上書きしたい規則だけを置いている。畳みたければ `style.css` の末尾に移してよい
（そのとき `js/app.js` の `loadExtraCss()` と `sw.js` のASSETSから消すこと）。

---

### 3. 変更点（このセッションのコミット3件）

| コミット | 内容 |
|---|---|
| `005b8c4` | 配置図7件＋選手一覧のボタン撤去（指示9〜15） |
| `03323d3` | 設定画面8件（指示1〜8）＋`run_all.bat` の壊れたパスを修復 |
| `b474c73` | JPA・成績・履歴14件（指示16〜29） |

#### 新しく増えたファイル

| ファイル | 役割 |
|---|---|
| `css/v2.css` | 今回ぶんの追加スタイル（衝突回避のため分けた） |
| `js/csv.js` | 成績・履歴・種目別成績をCSVに書き出す |
| `js/ui_gamestats.js` | 種目ごとの成績の画面 |
| `_test/layout2_test.py` | 配置図の検証（30項目） |
| `_test/setup2_test.py` | 設定画面の検証（29項目） |
| `_test/stats2_test.py` | JPA・成績・履歴の検証（50項目） |

#### 設計上の判断（あとから読む人が迷わないように）

- **8ボールの「1ボールハンデ」は得点計算に効かせていない。**
  8ボールは `scoring: "rack"`（ラック単位）で、engine は個々のグループ球を追っていない。
  効かせるには engine にグループ管理を足す必要があり、影響が大きい。
  いまは **記録と表示だけ**（試合画面と履歴に出る）。どの球を残すかは人が決める。
- **`result.innings` の意味は変えていない。** engine の `innings` は
  「後攻→先攻に手番が戻った回数」で、相手に一度も回らずに終わると `0` になる。
  `engine.test.js` の122項目がこの意味で使っているため、値はそのままにして
  **`result.inningsPlayed`（= `innings + 1`）を新設**し、画面と集計はこちらを使う。
  試合中の「Nイニング目」と同じ数え方になる。
- **試合中の帯にセーフティ数は出していない。**
  出したところ360x640で帯が3行に折り返し、下の操作ボタンが26pxはみ出した
  （`layout_fit_test.py` が検出）。指示はセーフティを
  「試合結果・成績・試合履歴」に求めているので、その3か所に出している。
- **CSVはBOM付きUTF-8。** BOMが無いとExcelがShift-JISとして読んで日本語が化ける。

---

### 4. 見つけて直した既存の壊れ（指示外）

**`_test/run_all.bat` の2行が壊れていて、検証がずっと途中で止まっていた。**

- `python _test\note_test.py` が、エスケープではなく**実際の改行**として書かれていた
  （`python _test` のあとに本物の改行が入り、次の行が `ote_test.py` になっていた）
- `python _test\tabbar_test.py` が、同様に**実際のタブ文字**になっていた

`[14/20]` で必ず失敗し、`goto failed` で抜けるため、**15〜20番の検証は一度も走っていなかった。**
両方を直し、今回の検証3本を足して全23項目にした。

---

### 5. 本人に確認したいこと（1件）

**JPAチームポイント早見表の敗者SL9の行が、本人からもらった画像と1マスずつずれている。**

`data/handicap_data.js` の値（出典: JPA公式 teampoint.pdf・本文抽出確認済み）と、
本人が送ってきた画像1の読み取りが、SL9の行だけ食い違う。

| 敗者の得点 | いまのコード | 画像1の読み取り |
|---|---|---|
| 47〜53 | 勝者15 | 勝者15 |
| **54** | **勝者14** | **勝者15** |
| 55〜60 | 勝者14 | 勝者14 |
| **61** | **勝者13** | **勝者14** |
| 62〜67 | 勝者13 | 勝者13 |
| **68** | **勝者12** | **勝者13** |
| 69〜74 | 勝者12 | 勝者12 |

SL1〜SL8の行は画像とコードが完全に一致している。**SL9の行だけ**。
どちらも0〜74を隙間なく埋めるので、計算だけでは正誤を決められない。

**値は変えていない。** コードの出典は公式PDFで、こちらの根拠は解像度の低い画像だから。
影響は「敗者SL9が54点・61点・68点ちょうどで負けたとき、勝者ポイントが1点違う」の3点だけ。
本人が実物の早見表でこの3行を見て、画像が正しければ
`JPA_TEAM_POINT_BANDS[9]` を `[[0,17],[18,24],[25,31],[32,38],[39,46],[47,54],[55,61],[62,68],[69,74]]`
に直す。

**JPA 8ボールのポイント換算は入れていない。** 画像1は9ボール用の表で、
8ボールは別の表（レース制）。表が手に入るまで8ボールは従来どおりスコア表示のまま。

---

### 6. 検証のやり方

```
_test\run_all.bat
```

全23項目（node 4本＋実ブラウザ19本）。1本だけ動かすなら:

```
python _test\stats2_test.py     JPA・成績・履歴（50項目）
python _test\setup2_test.py     設定画面（29項目）
python _test\layout2_test.py    配置図（30項目）
python _test\layout_fit_test.py 1画面に収まっているか（32項目・約3分かかる）
```

**`layout_fit_test.py` は他より時間がかかる（種目×画面の全数を回すため）。**
2分で切らずに待つこと。

公開版に何が出ているかを一次情報で見るコマンド:

```
curl -s -A "Mozilla/5.0" https://hiroshit164820-create.github.io/pool-score/sw.js | grep CACHE_VERSION
```

**いまローカルは `v20`。公開版は `v19` のはず（未 push のため）。**
これが `v20` になっていれば公開に反映されている。

---

## 2026-08-20（12:30時点）— 別セッションの指示4件を完了・デプロイ済み

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開済み: https://hiroshit164820-create.github.io/pool-score/

前セッションで受けた**指示4件をすべて実装し、公開まで完了した**。
本人の指示どおり1件ずつ「実装 → 検証 → コミット」で回した。

**次のセッションの目標**: 本人が実機で触ってからの指摘待ち。
新しい指示が無ければ「5. 未着手の範囲」から選ぶ。

---

### 2. 変更点（このセッションで実際に変更したもの）

コミット5件。すべて公開済み（HEAD = `4cd69a5`）。

| コミット | 内容 |
|---|---|
| `59acab8` | 指示2: 種目のルール説明とボール種の説明を削除 |
| `c093ec9` | 指示3: 配置図の見た目を試合画面と統一＋操作性＋**取り消しの不具合修正** |
| `0120b5c` | 指示1: 自分と対戦相手を分けて登録、ホームの推測を廃止 |
| `a45a67f` | 指示4: ハウスゲームに5-9 / 5-10 を追加 |
| `4cd69a5` | live_test の種目数を14に更新 |

**新しく増えたファイル**
- `js/money_game.js` … 5-9/5-10 の点数計算（UIに依存しない純粋な処理）
- `js/ui_money.js` … 5-9/5-10 の準備画面と試合画面
- `_test/layout_touch_test.py` / `_test/self_player_test.py`
- `_test/money_game.test.js` / `_test/money_ui_test.py`

---

### 3. 検証済みの証拠

**すべて 2026-08-20 12:28〜12:35 に実行した結果。**

| 項目 | 値 | 観測時刻 |
|---|---|---|
| ローカルHEAD | `4cd69a5` | 12:35 |
| 作業ツリー | クリーン | 12:35 |
| Pages API | `status=built` / `commit=a45a67f` | 12:28:53 |
| 配信中の `sw.js` | `CACHE_VERSION = "v19"`（curlで実物確認） | 12:29:04 |
| `js/money_game.js` / `js/ui_money.js` | どちらも HTTP 200 | 12:29:04 |
| 配信中 index.html | `screenMoneySetup` あり／`gameNote` は0件 | 12:29:04 |
| 公開URLでの通し検証 | `live_test.py` 19項目すべて成功 | 12:33 |

**検証の総数: 20本 879項目すべて成功**（node 4本＋実ブラウザ16本）。
`_test\run_all.bat` は 20本を通すように更新済み（番号も振り直した）。

---

### 4. 一次情報の取り直しコマンド

再開時はまずこれを実行すること。**3段とも別の事実なので全部通す。**

```bash
cd D:\Claudecode\pool-score

_test\run_all.bat                    # 検証20本・879項目
python _test\live_test.py            # 公開URLの実物（上とは別）

gh api repos/hiroshit164820-create/pool-score/pages/builds/latest --jq "{status:.status, commit:(.commit[0:7])}"
curl -s "https://hiroshit164820-create.github.io/pool-score/sw.js" | grep "CACHE_VERSION = "

git log --oneline -3 && git status --short
```

---

### 5. 未着手の範囲

- JCL 9ボール／8ボール — ファーゴレート換算式が非公開のため実装しない方針
- イニング単位の詳細入力モード
- JPAのチームポイント表示（`buildResult` で算出済みだが画面に出していない）
- Phase2のクラウド共有
- **5-9の記録は保存していない**（画面を出ると消える）。
  既存の試合とデータの形が違うため、履歴・成績には出ない。
  本人から要望があれば保存を足す

**やったように見えるが未検証のもの**
- **実機のスマホでの確認は本人のみ。** こちらは Playwright だけ
- 実際のビリヤード台の脇で使っていない
- 5-9を**3人以上で実際に遊びながら**記録していない（検証は自動操作のみ）

---

### 6. 不確実な点・リスク

**引き継ぎにあった「再現しない失敗」は原因を特定して直した**

前回「redesign_test が1回だけ落ちるが7回再現せず、失敗項目名も取れていない」
と残っていた件は、**実測で原因まで特定し修正した**（`c093ec9`）。

- 失敗項目は「取り消しボタンが実際に効く」だった
- 原因: ショットクロックONのとき、得点の直後に経過時間の記録
  （`SHOT_CLOCK` / `event:"shot"`、平均タイム算出用の自動記録）が積まれる。
  `undoLast` がこれを「直前の記録」として取り消していたため、
  **得点が残ったまま「取り消しました」と出ていた**
- ショットクロックが動いているときだけ起きるので再現しにくかった
- 修正後、25回連続で正しく戻ることを確認。回帰テストも追加した
- **本番に出ていた不具合。** 実機で「取り消しても点が減らない」ことがあったはず

**5-9のルールは本人からの聞き取りのみが出典**

Web上の一次情報は取っていない。NBA規程に章が無いゲームで、
本人の店・仲間内の取り決めをそのまま実装している。
内容は `04_種目ルール仕様.md` の「5-2」に記録した。
**別の店の5-9とは違う可能性がある。**

**本人に確認して決めた事項（このセッション）**
- 5-9: 5番=1点／9番=2点／ハンデ=1点、相手からもらう、3人以上は全員から
- サイド倍とマスワリ倍は**掛け算で4倍**
- マスワリの倍は**そのラックの得点すべて**にかかる
- ハンデ球は**割り当てられた本人が落としたときだけ**得点
- ハンデは**複数の球**を割り当てられる
- 5番を落としても**盤面はそのまま続行**
- カイルンの「公式規程が無い」案内も**削除してよい**（全部消す）
- 5-9は**専用画面を作って4人以上にも対応**する

**設計上の判断（本人の確認を取っていない）**
- 5-9のマスワリは、ラック終了時にプロンプトで名前を聞く方式にした。
  記録から撞き切りを機械的に判定できないため
- 5-9で人数の上限は設けていない
- 1つの球を2人がハンデに持つことはできない
- 5番とキーボールはハンデに選べない（全員の得点球のため）
- 「自分」は設定に id を1つ持つ形にした（旗を各人に立てる形だと2人立てられる）

**時間経過で腐る観測**
- 上記3の「built」「v19」「HTTP 200」はすべて **12:28〜12:33時点**。取り直すこと

---

### 7. 触ってはいけない領域

前回の一覧に加えて、このセッションで増えたもの。

- **`js/engine.js` の `undoLast` にある `SHOT_CLOCK` の読み飛ばし**
  → 外すと「取り消しても点が減らない」不具合が戻る（今回直したばかり）
- **`js/ui_match.js` の `onUndo` が自動記録をまとめて消す処理**
  → 得点だけ消すと、平均タイムやブレイク側が1つ多い状態でずれる
- **`js/money_game.js` の `tally`**
  → ゼロサムの計算。相手の人数を掛けているので、2人前提に戻さない
- **`sw.js` の資産一覧に `money_game.js` / `ui_money.js` があること**
  → 消すとオフラインで5-9だけ動かなくなる
- **`ui_home.js` の `mainPlayer()`**
  → 推測に戻さない。`STORE.getSelf()` だけを見る
- **`js/ui_layout.js` の `freeSpot`**
  → 台の実寸から間隔を出している。割合の決め打ちに戻すと球が重なって掴めない
- **`.tb-ball` の 44px**
  → 30pxに戻すと指より小さくなる

前回からの継続（そのまま有効）:
`CACHE_VERSION`／`data/` の数値／ローテーションの61点／`fitScoreFont`／
`.panel-flags` の絶対配置／`break-banner` の `display:none`／`makeScorer`／
`displayScore()`／`renderGoalArea` の呼び出し順／`body.match-active`／
`user-select` の除外リスト／`touch-action`／`.pool-table` の height 基準／
可読性の制約（44px・16px・コントラスト）／リポジトリは公開のまま／
コミット作者は noreply アドレス

---

### 8. 最後の判断・関門

**このセッションで下した判断**

1. **着手前に本人へ質問した** — 5-9のルールと「操作性のどこが不満か」は
   推測で作ると作り直しになるため。結果、5-9は想定より複雑な
   マネーゲーム（ゼロサム＋倍率）だと分かり、推測で作らなくて正解だった
2. **5-9を既存エンジンに相乗りさせなかった** — 3人以上に対応するには
   `score:{A,B}` / `other()` / 左右2パネルを作り替える必要があり、
   全種目に影響する。本人にも確認したうえで専用画面にした
3. **落ちていた検証を「flaky」で済ませなかった** — 25回ループさせて
   確実に再現させ、イベント列を出して原因まで特定した。本物の不具合だった
4. **指示3の「操作性」を推測で決めなかった** — 実測して
   「球が30pxで指より小さい」「44pxにすると今度は重なって掴めない」
   という具体的な問題を数字で確かめてから直した

**本人確認待ちの事項**
- 5-9の記録を保存するか（いまは画面を出ると消える）
- 5-9を履歴・成績に出すか
- 実機で触っての指摘

---

### 9. 次の安全なアクション（1つだけ）

**本人に実機で4件を触ってもらい、指摘を受ける。**

4件とも自動検証は通っているが、実機で触ったのは本人だけという状態は
前回と同じ。特に見てほしいのは次の4点。

1. 5-9を実際に3人で遊んで、点の動きが感覚と合うか
2. 配置図の球が44pxになって、掴みやすくなったか
3. 「自分」の登録が分かりやすいか（既存データがある場合の移行も）
4. 取り消しボタンが毎回効くか（不具合を直したので）

---

### 再開者が仮定してはいけないこと

- **「テストが通っている＝スマホで動く」と仮定しない。** 実機は本人だけ
- **「Pagesがbuilt＝新しい中身が配信されている」と仮定しない。** curlで見る
- **5-9のルールを一般的な5-9だと仮定しない。** 出典は本人の聞き取りのみで、
  店によって違う可能性がある
- **「本人が『こう見える』と言った＝そのとおりに壊れている」と仮定しない。**
  マスワリのボタンが「機能していない」が誤認だった実例がある。
  一方で「2人目が隠れる」と、今回の「取り消しが効かない」は実際に壊れていた。
  **まず実物で再現を確認する**
- **落ちた検証を「flaky」と決めつけない。** 今回のように25回回せば
  再現して原因まで辿れることがある

---

## 2026-08-20（10:48時点）— 次セッションの作業指示4件（下の12:30版で完了済み）

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開済み: https://hiroshit164820-create.github.io/pool-score/

**このセッションでは実装作業を行っていない。**
前セッションで指示16件＋実機指摘13件を完了・デプロイしたあと、
本人が実機で確認し、**新たな指示4件**を受け取った。
このセッションでやったのは、その4件の**前提が実物と合っているかの確認**と、
検証の基準線を取り直したことだけ。

**次のセッションの目標**: 下記「9. 次セッションの作業指示」の4件を**スライスして**進める。
本人の指示は「スライスして進めて」。1件ずつ実装→検証→停止の単位で回すこと。

---

### 2. 変更点（このセッションで実際に変更したもの）

**コードの変更なし。** 作業ツリーはクリーンで、HEAD は前セッションの `3f59574` のまま。

このセッションで行ったのは以下の読み取り専用の確認のみ:
- git / GitHub Pages / 配信中ファイルの状態確認
- 検証16本の再実行（基準線の取り直し）
- 指示4件それぞれの「いま実物がどうなっているか」の調査

---

### 3. 検証済みの証拠

**すべて 2026-08-20 10:37〜10:48 に実行した結果。**

**リポジトリと公開状態（10:37時点の観測）**

| 項目 | 値 |
|---|---|
| ローカルHEAD | `3f59574` |
| 作業ツリー | クリーン（未コミットの変更なし） |
| Pages API | `status=built` / `commit=3f59574` / `updated=2026-08-19T22:42:29Z` |
| 配信中の `sw.js` | `CACHE_VERSION = "v14"`（10:37:14にcurlで実物確認） |
| 公開URL | HTTP 200 |

**検証の基準線（10:38〜10:48の実行）**

16本すべて実行し、**752項目中751項目が成功**。
`redesign_test.py` で1項目だけ失敗したが、**その後7回連続で再実行して1度も再現しなかった**
（59/59が7回）。**再現しないため、失敗した項目名を特定できていない。**
→ 詳細は「6. 不確実な点」に記載。**本物の不具合と決めつけないこと。**

**指示4件の前提を実測した結果（10:37〜10:38時点）**

| 指示 | 実測でわかったこと |
|---|---|
| 1. 自分と他プレーヤーを分けて登録 | **「自分」という概念はデータに存在しない。** `store.js` に `isSelf` 等のフラグ無し。ホームは `ui_home.js:34` の `mainPlayer()` が**試合数の最多者を自分と推測**している |
| 2. ルール説明とボール種の説明を削除 | ルール説明＝`#gameNote`（`index.html:59`、生成は `ui_setup.js` の `selectGame`）。ボール種の説明＝`ui_setup.js:718-719` が `BALL_SETS[].note` を描画。**noteは `data/balls_data.js` の4セット分すべてに存在** |
| 3. 配置図の球をパラジウムに | **色はすでに `BALL_SETS.standard`（＝標準（パラジウム））を使っている**（`ui_layout.js:73-78`）。試合画面と違うのは**白い丸の番号枠が無い**こと。試合画面は `ballAppearance()` と `.bb-num` で描いている（`ui_match.js:1033,1053`） |
| 4. 5-9／5-10 を追加 | **どこにも存在しない。** `data/` `js/` `04_種目ルール仕様.md` を検索して0件。**ルールの一次情報も未取得** |
| 4. 個人別ハンデ | **いまはダブルス限定。** `ui_setup.js:599` が `if (g.playersPerSide === 2)` で囲っている。5-9/5-10 を個人戦で作るなら、この条件を外す設計が要る |

---

### 4. 一次情報の取り直しコマンド

再開時はまずこれを実行すること。

```bash
cd D:\Claudecode\pool-score

# 検証をまとめて実行（16本・752項目）
_test\run_all.bat

# 公開URLの実物（上とは別。必ず実行する）
python _test\live_test.py

# 公開状態
gh api repos/hiroshit164820-create/pool-score/pages/builds/latest --jq "{status:.status, commit:(.commit[0:7])}"

# 配信されている中身が新しいか（キャッシュに騙されないため）
curl -s "https://hiroshit164820-create.github.io/pool-score/sw.js" | grep "CACHE_VERSION = "

git log --oneline -3 && git status --short
```

**注意**: 「テストが通った」「Pagesがbuilt」「配信中の中身が新しい」はすべて別の事実。3段とも通すこと。

---

### 5. 未着手の範囲

**このセッションの指示4件はすべて未着手。** 1件も実装していない。

**前セッションから引き継いでいる未実装**
- JCL 9ボール／8ボール — ファーゴレート換算式が非公開のため実装しない方針
- イニング単位の詳細入力モード（JPAスコアシートの表示はあるが入力UIは無い）
- JPAのチームポイント表示（`buildResult` で算出済みだが画面に出していない）
- Phase2のクラウド共有

**やったように見えるが未検証のもの**
- **実機のスマホでの動作確認は本人のみ。** こちらは Playwright しか使っていない
- 実際のビリヤード台の脇で使っていない
- ダブルタップ拡大の抑止は実機のiOS Safariでしか本当の挙動が分からない
- **長押しで1点戻す操作（0.5秒）が実機で誤爆しないか未確認**

---

### 6. 不確実な点・リスク

**検証が1回だけ落ちた（最重要）**

10:48時点の通し実行で `redesign_test.py` が 58/59（1失敗）になった。
その後 **7回連続で再実行したが1度も再現せず、失敗項目名を取れていない**。

- **「本物の不具合」と決めつけない。** 再現していない
- **「flakyだから無視してよい」とも決めつけない。** 原因が分かっていない
- 次に落ちたときは**失敗項目名を必ず記録する**（`python _test\redesign_test.py` の
  「【失敗した項目】」の行）。それが無いと調査できない
- 疑うなら待ち時間（`wait_for_timeout`）が短い箇所。この検証は
  ショットクロックやアニメーションを含み、タイミング依存の項目がある

**指示4（5-9／5-10）のルールが未確定**

**一次情報を持っていない。本人に聞く前提で書く。**
5-9は「5番と9番を落とすと勝ち」系のハウスゲームと理解しているが、
- 5番を落としたら即勝ちか、5番と9番の両方が要るのか
- 5番を落としたあと盤面はどうなるか（戻すのか、そのままか）
- 得点制なのかラック制なのか
- 反則の扱い

これらが分からない。**推測で作ると作り直しになる。**
本人が「分からなければ質問して」と明言しているので、**着手前に必ず聞くこと。**

**設計上の判断で、本人の確認を取っていないもの（前セッションから継続）**
- ホームの「自分」は試合数の最多者を自動選択している
  → **指示1でこれを置き換える。実装後は `mainPlayer()` の推測をやめること**
- ブレイク帯と「いまの番」帯を廃止した（パネル側の札と色で代替）
- セーフティのボタンは名前が長いと省略される（「まみ・たか…」）
- カイルンのハウス設定3つの既定値は仮決め（一次情報が無い）

**時間経過で腐る観測**
- 上記3の「built」「v14」「HTTP 200」はすべて **10:37時点**。再開時は取り直すこと

**推測で進めた箇所（前セッションから継続）**
- JPA 8ボールの対戦表はAPA公式のもの（JPA公式資料での直接確認は取れていない）
- ボールの色は商品画像からの目視。16進コードはどのメーカーも非公開
- チタニウムの配色は公式ページの説明文が別製品(Unity)と混ざっており画像のみが根拠

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v14`）→ ファイルを変えたら**必ず上げる**
- **`data/` 配下の数値** → 一次情報で確認済み。変更前に `04_種目ルール仕様.md` の出典を見る
- **ローテーションの61点** → 公式規程に存在しないことを確認済み。プリセットに戻さない
- **`js/ui_match.js` の `fitScoreFont`**
  → スコアの数字の大きさをパネルの実測から決めている。
  CSSの `vh` だけに戻すと、帯やボタンが増えたときに数字が切れる
- **`.panel-flags` の絶対配置**
  → 通常の積み方に戻すと、ブレイク側のパネルだけ背が低くなり左右で数字の大きさが変わる
- **`#screenMatch .break-banner / .turn-banner` の `display: none`**
  → 戻すと 171px を取り、スコアの数字が 90px → 44px まで小さくなる
- **`js/engine.js` の `makeScorer`**
  → 「相手だけにハンデがある場合はキーボールのみ得点」の分岐を外すと不具合が戻る
- **`js/ui_match.js` の `displayScore()`**
  → ボウラードだけボーナス込みの集計を返す。`st.score` を直接出すと落球数になる
- **`renderGoalArea` → `renderBallHandicap` → `renderBallSet` の呼び出し順**
  → 逆向きに呼ぶと相互再帰になる
- **`body.match-active` の高さ固定**／**`.app::before` `.app::after` の `pointer-events: none`**
- **`user-select: none` の除外リスト**（`input, textarea, .ev-desc, .mc-main, .mc-note, .stat-val`）
- **`touch-action: manipulation`** → 外すと連打で画面が拡大する
- **`.pool-table` の `height` 基準の指定**
  → `max-height` だけに戻すと幅が縮まず、台が横長に崩れる
- **`style.css` の可読性の制約**（タップ44px以上／本文16px／コントラスト4.5:1・数字3:1）
  → `redesign_test.py` `browser_test.py` が自動検査している
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者** → noreply アドレス（`304265170+hiroshit164820-create@users.noreply.github.com`）

---

### 8. 最後の判断・関門

**このセッションで下した判断**

1. **実装に着手しなかった** — 指示が「次のセッションでは」だったため、
   前提の確認と引き継ぎの作成までで停止した
2. **落ちた検証を「flaky」と断定しなかった** — 7回再現しなかったが、
   失敗項目名を取れていない以上、原因不明として残した
3. **5-9／5-10 のルールを調べに行かなかった** — 本人が
   「分からなければ私に質問して」と明言しているため、Web検索で推測を集めるより
   本人に聞くほうが速くて確実と判断した

**本人確認待ちの事項**

- **5-9／5-10 のルール**（下記9-4の質問項目）
- **「自分」の登録を1人に限るか**（複数人で共用する可能性があるか）
- **ルール説明の削除は全種目か**（カイルンの「公式規程が無い」案内も消してよいか）

---

### 9. 次セッションの作業指示（本人から・2026-08-20受領）

本人の指示は**「スライスして進めて」**。1件ずつ「実装 → 検証 → 停止」で回す。

#### 1. 選手登録で「自分」と「他プレーヤー」を分ける／ホームに出す

| 項目 | 内容 |
|---|---|
| 指示 | 選手登録の際に自分の登録と他プレーヤーの登録を分けてできるように。それがホーム画面にくるようにして |
| 現状 | **「自分」の概念がデータに無い。** ホーム（`ui_home.js:34` の `mainPlayer()`）は**試合数の最多者を自分と推測**している |
| やること | `players` に「自分」の印を持たせる（例: `isSelf: true` を1人だけに）。登録画面を「自分を登録」と「対戦相手を登録」に分ける。ホームは推測をやめてこの印を見る |
| 注意 | **`mainPlayer()` の推測ロジックは必ず外すこと。** 残すと印と推測が食い違う。既存の登録済みデータには印が無いので、移行の扱い（最初の1人を自分にする／本人に選ばせる）を決める必要がある |

#### 2. ルール説明とボール種の説明を削除

| 項目 | 内容 |
|---|---|
| 指示 | 種目選択の時のルール説明と、ボール種選択の説明は削除 |
| 現状 | ルール説明＝`#gameNote`（`index.html:59`）。ボール種の説明＝`ui_setup.js:718-719` が `BALL_SETS[].note` を描画 |
| やること | 両方を画面から消す |
| 注意 | **`data/` の `note` 自体は消さない**（出典の記録として残す価値がある。表示だけやめる）。**カイルンの「公式競技規程が無いため店ごとの決め方を選んでください」も `#gameNote` に出ている。** これを消すとハウス設定の意味が分からなくなるので、**本人に確認するか、この1文だけ別の場所に残す**。前セッションで `slice1_test.py` が「注意書きが種目の直下に出る」を検査しているので、その項目も直すことになる |

#### 3. 配置図の球をパラジウムにして、操作性を良くする

| 項目 | 内容 |
|---|---|
| 指示 | 配置図のボールはパラジウムにして、操作性をもっと良くして |
| 現状 | **色はすでにパラジウム**（`BALL_SETS.standard` を使用、`ui_layout.js:73-78`）。試合画面と違うのは**白い丸の番号枠が無い**点。試合画面は `ballAppearance()` ＋ `.bb-num` で描いている |
| やること | 見た目を試合画面と揃える（`ballAppearance()` を使い回す）。操作性の改善 |
| 注意 | **「操作性をもっと良くして」は具体的な不満が書かれていない。** いま分かっている作りは「タップでどける／ドラッグで移動（しきい値6px）」。**着手前に本人へ「どこが操作しづらいか」を聞くのが確実。** 推測で挙げるなら: 球が30pxで指より小さい／どけるつもりが動く・動かすつもりが消える／重なった球を掴み分けられない／取り消しが無い |

#### 4. ハウスゲームに「5-9」「5-10」を追加＋個人別ハンデ

| 項目 | 内容 |
|---|---|
| 指示 | 種目選択のハウスゲームに「5-9」「5-10」を追加。これも各個人にハンデボールをふれるように。**分からなければ私に質問して** |
| 現状 | **どこにも存在しない**（`data/` `js/` `04_種目ルール仕様.md` すべて0件）。個人別ハンデは**ダブルス限定**（`ui_setup.js:599` の `if (g.playersPerSide === 2)`） |
| やること | まず**本人にルールを聞く**。そのうえで `rules_data.js` `games_data.js` に定義を足し、`GAME_GROUPS` の `house` カテゴリに追加。個人別ハンデをシングルスでも出せるようにする |
| **本人に聞くこと** | ① 決着条件（5番を落とせば勝ちか、5番と9番の両方が要るか）<br>② 5番を落としたあと盤面はどうするか（フットスポットに戻すか、そのままか）<br>③ 得点制かラック制か（何点先取／何ラック先取）<br>④ 反則の扱い（相手にボールインハンドか、減点か）<br>⑤ ブレイク方式（勝者ブレイク／交互）<br>⑥ 「各個人にハンデボールをふる」の意味（シングルスで2人に別々の得点球を割り当てる、で合っているか） |

#### 着手順の提案（本人の合意は取っていない）

1. **2**（説明の削除。影響範囲が小さい。ただしカイルンの案内の扱いだけ確認）
2. **3**（配置図。見た目は試合画面の流用で済む。操作性は不満点を聞いてから）
3. **1**（自分の登録。データ構造とホームの両方に手が入る）
4. **4**（5-9／5-10。ルールを聞いてから。個人別ハンデの設計変更を含むため最後）

---

### 次の安全なアクション（1つだけ）

**本人に、指示4（5-9／5-10）のルールと、指示3の「操作性のどこが不満か」を質問する。**

指示4は一次情報が無く、本人が「分からなければ質問して」と明言している。
指示3も「もっと良くして」だけでは直す対象が決まらない。
**この2つを聞いてから着手すれば、作り直しが避けられる。**

質問の答えを待つ間に手を動かすなら、**指示2（説明の削除）**が最も安全。
ただしカイルンの案内文の扱いだけは先に確認すること。

### 再開者が仮定してはいけないこと

- **「配置図の球がパラジウムでない」と仮定しない。** 色はすでにパラジウム。
  違うのは番号の白い丸枠。**まず実物を見てから直す**
- **5-9／5-10 のルールを推測で決めない。** 一次情報が無く、本人が質問を求めている
- **「操作性が悪い」の中身を推測で決めない。** 具体的な不満を聞く
- **落ちた検証（redesign_test）を「本物の不具合」とも「flaky」とも決めつけない。**
  7回再現していないが原因不明。**次に落ちたら失敗項目名を必ず記録する**
- **「本人が『こう見える』と言った＝そのとおりに壊れている」と仮定しない。**
  過去にマスワリのボタンが「機能していない」という指摘が誤認だった実例がある。
  一方で「2人目が隠れる」は実際に壊れていた。**まず実物で再現を確認する**
- **「テストが通っている＝スマホで動く」と仮定しない。** 実機で触ったのは本人だけ
- **「Pagesがbuilt＝新しい中身が配信されている」と仮定しない。** 4.のコマンドで実物を見る

---

## （以下は過去の引き継ぎ）

---

## 2026-08-20（07:40時点）— 指示16件＋実機指摘13件を完了

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開済み: https://hiroshit164820-create.github.io/pool-score/

**本人指示の16件と、その後の実機スクリーンショットによる指摘13件をすべて実装・デプロイ済み。**

次にやることは決まっていない。**本人の実機確認待ち。**

---

### 2. このセッションで完了したもの

**指示16件（すべて完了）**

| # | 内容 | コミット |
|---|---|---|
| 1 | ダブルスで選んだ人を塗りつぶし＋白文字 | `611a563` |
| 2 | スコア表のマスを読める大きさに（22x20px→32px以上） | `611a563` |
| 3 | スコアの数字の位置／影を外す／「/目標点」を右へ | `611a563` `322fd9c` |
| 4 | マスワリ・ブレイクエースをブレイク側のパネル内へ | `322fd9c` |
| 5 | ローテーションの2人目が隠れる → **360x640で再現し修正** | `611a563` |
| 6 | ルール説明を選んだ種目の直下へ | `611a563` |
| 7 | 標準ボールに「パラジウム」 | `611a563` |
| 8 | 1人目を2人目の候補から除外 | `611a563` |
| 9 | マスワリを押すとスコアが増える（予約式をやめる） | `0ee1d81` |
| 10 | ローテーション先取点 120/180/240/300 | `0ee1d81` |
| 11 | 試合メモ | `0ee1d81` |
| 12 | 下部タブ（戻るを内包） | `e6d11b6` |
| 13 | タブの中身＋ホーム（個人ダッシュボード） | `e6d11b6` |
| 14 | カイルン（ハウス設定つき） | `335a19f` |
| 15 | 選手登録と一覧の統合 | `e6d11b6` |
| 16 | 練習配置の保存・呼び出し | `102bcbe` |

**実機スクリーンショットによる指摘13件（すべて完了、`322fd9c`）**
先取点の1行化／セーフティの人別カウント／スコアの影・目標点の位置／
マスワリの置き場／チーム名表示／個人別ボールハンデ／潰れの修正／
交代の色差／チーム内交代／説明文の削除と移動／長押しで減点。

---

### 3. 検証済みの証拠

**すべて 2026-08-20 07:38〜07:40 に実行した結果。**

| 項目 | 値 |
|---|---|
| ローカルHEAD | `102bcbe` |
| 作業ツリー | クリーン |
| Pages API | `status=built` / `commit=102bcbe`（07:39:35） |
| 配信中の `sw.js` | `CACHE_VERSION = "v14"`（07:39:44に実物をcurlで確認） |
| 公開URL | HTTP 200 |
| ローカル検証 | **16本・752項目 全通過** |
| 公開URL上の動作確認 | 7項目 全通過（タブ7つ・台の比率・配置の保存呼び出し・ホーム） |

**注意**: 「テストが通った」「Pagesがbuilt」「配信中の中身が新しい」は別の事実。
再開時は下記4のコマンドで3段とも取り直すこと。

---

### 4. 一次情報の取り直しコマンド

```bash
cd D:\Claudecode\pool-score

# 検証をまとめて実行（16本・752項目）
_test\run_all.bat

# 公開URLの実物（上とは別。必ず実行する）
python _test\live_test.py

# 公開状態
gh api repos/hiroshit164820-create/pool-score/pages/builds/latest --jq "{status:.status, commit:(.commit[0:7])}"

# 配信されている中身が新しいか（キャッシュに騙されないため）
curl -s "https://hiroshit164820-create.github.io/pool-score/sw.js" | grep "CACHE_VERSION = "

git log --oneline -3 && git status --short
```

---

### 5. 未着手・未確認の範囲

**実装として残っているもの**
- JCL 9ボール／8ボール — ファーゴレート換算式が非公開のため実装しない方針
- イニング単位の詳細入力モード（JPAスコアシートの表示はあるが入力UIは無い）
- JPAのチームポイント表示（`buildResult` で算出済みだが画面に出していない）
- Phase2のクラウド共有

**やったように見えるが未検証のもの**
- **実機のスマホでの動作確認は Playwright のみ。本人の実機確認はこれから**
- 実際のビリヤード台の脇で使っていない
- ダブルタップ拡大の抑止は実機のiOS Safariでしか本当の挙動が分からない
- **長押しで1点戻す操作は、実機のタッチで誤爆しないかを見ていない**
  （0.5秒。短すぎれば誤操作、長すぎれば反応が鈍い）

---

### 6. 不確実な点・リスク

**設計上の判断で、本人の確認を取っていないもの**
- **ホームの「自分」は、試合数がいちばん多い人を自動で選んでいる。**
  明示的な設定を持たせていないので、2人以上で共用すると「自分」が入れ替わる
- **ブレイク帯と「いまの番」帯を廃止した。** パネル側の BREAK 札・「● 撞く番」札・
  枠の色で同じ情報が出ているという判断。情報が足りなければ戻す必要がある
- **セーフティのボタンは名前が長いと省略される**（「まみ・たか…」）
- **カイルンのハウス設定3つは既定値を仮に決めている**
  （ミス=最初から／1手番=何点でも／反則=自分が1点減る）。
  一次情報が無いため、本人の店の決め方に合っているかは未確認

**時間経過で腐る観測**
- 上記3の「built」「v14」「HTTP 200」はすべて **07:38〜07:40時点**。再開時は取り直す

**推測で進めた箇所（前セッションから継続）**
- JPA 8ボールの対戦表はAPA公式のもの（JPA公式資料での直接確認は取れていない）
- ボールの色は商品画像からの目視。16進コードはどのメーカーも非公開
- チタニウムの配色は公式ページの説明文が別製品(Unity)と混ざっており画像のみが根拠

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v14`）→ ファイルを変えたら**必ず上げる**
- **`data/` 配下の数値** → 一次情報で確認済み。変更前に `04_種目ルール仕様.md` の出典を見る
- **ローテーションの61点** → 公式規程に存在しないことを確認済み。プリセットに戻さない
- **`js/ui_match.js` の `fitScoreFont`**
  → スコアの数字の大きさをパネルの実測から決めている。
  CSSの `vh` だけに戻すと、帯やボタンが増えたときに数字が切れる
- **`.panel-flags` の絶対配置**
  → 通常の積み方に戻すと、ブレイク側のパネルだけ背が低くなり左右で数字の大きさが変わる
- **`#screenMatch .break-banner / .turn-banner` の `display: none`**
  → 戻すと 171px を取り、スコアの数字が 90px → 44px まで小さくなる
- **`js/engine.js` の `makeScorer`**
  → 「相手だけにハンデがある場合はキーボールのみ得点」の分岐を外すと不具合が戻る
- **`js/ui_match.js` の `displayScore()`**
  → ボウラードだけボーナス込みの集計を返す。`st.score` を直接出すと落球数になる
- **`renderGoalArea` → `renderBallHandicap` → `renderBallSet` の呼び出し順**
  → 逆向きに呼ぶと相互再帰になる
- **`body.match-active` の高さ固定**／**`.app::before` `.app::after` の `pointer-events: none`**
- **`user-select: none` の除外リスト**（`input, textarea, .ev-desc, .mc-main, .mc-note, .stat-val`）
- **`touch-action: manipulation`** → 外すと連打で画面が拡大する
- **`.pool-table` の `height` 基準の指定**
  → `max-height` だけに戻すと幅が縮まず、台が横長に崩れる
- **`style.css` の可読性の制約**（タップ44px以上／本文16px／コントラスト4.5:1・数字3:1）
  → `redesign_test.py` `browser_test.py` が自動検査している
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者** → noreply アドレス

---

### 8. このセッションで下した判断

1. **「2人目が隠れる」は推測で直さず、再現してから直した。**
   360x640 で再現（スコアボードが `overflow:hidden` のまま中身より低くなり、
   2つ目のパネルが名前ごと切り落とされていた）。
   ついでに種目8×画面サイズ4の全数で洗い出したところ、**着手前に19通り壊れていた**ので
   0件になるまで直し、`layout_fit_test.py` として自動検査に残した

2. **ボールハンデは表示のみにした。** 本人の回答が
   「誰がどのハンデボールを持っているかがわかればいい。スコア入力は目視で手入力」
   だったため、エンジンの得点計算には手を入れていない

3. **カイルンのハウス設定は選択式にした。** 公式規程が無いので既定を決め打ちしない

4. **既存テストのうち、仕様変更に追随させたものがある**（テストを通すためではない）:
   マスワリの置き場／セーフティの数え方／ブレイク帯の廃止／1人用の隠し方／
   種目数11→12／カテゴリ2→3／固定戻るボタン→タブ

---

### 9. 次の安全なアクション（1つだけ）

**本人の実機確認を待つ。**

実装は16件すべて完了しデプロイ済み。自動検証は752項目通っているが、
**実機のスマホで触ったのは本人だけ**で、こちらは Playwright しか使っていない。
次の指摘が来る前に別の作業を始めると、手戻りになる可能性がある。

もし作業を進めるなら、上記5の「実装として残っているもの」から。

---

### 10. 再開者が仮定してはいけないこと

- **「本人が『こう見える』と言った＝そのとおりに壊れている」と仮定しない。**
  前々セッションでマスワリのボタンが「機能していない」という指摘が誤認だった実例がある。
  一方で今回の「2人目が隠れる」は**実際に壊れていた**。**まず実物で再現を確認する**
- **「テストが通っている＝スマホで動く」と仮定しない。** 実機確認は本人のみ
- **「Pagesがbuilt＝新しい中身が配信されている」と仮定しない。** 4.のコマンドで実物を見る
- **ホームの「自分」が固定だと仮定しない。** 試合数で自動的に決まる
- **カイルンのハウス設定の既定値が正しいと仮定しない。** 一次情報が無い

---

## （以下は過去の引き継ぎ）

---

## 2026-08-20（03:35時点）— 次セッションの作業指示

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開済み: https://hiroshit164820-create.github.io/pool-score/

**このセッションでは実装作業を行っていない。** 本人から次セッション向けの指示16件を受け取り、
着手前の状態確認と、指示の前提が実物と合っているかの検証だけを行った。

**次のセッションの目標**: 下記「9. 次セッションの作業指示」の16件を**スライスして**進める。
本人の指示は「スライスして進めてください」。1件ずつ実装→検証→停止の単位で回すこと。

---

### 2. 変更点（このセッションで実際に変更したもの）

**コードの変更なし。** 作業ツリーはクリーンで、HEAD は前セッションの `d4cc0b1` のまま。

このセッションで行ったのは以下の読み取り専用の確認のみ:
- git / GitHub Pages / 配信中ファイルの状態確認
- 指示16件のうち3件について、前提が実物と合っているかをブラウザで実測

---

### 3. 検証済みの証拠

すべて **2026-08-20 03:31〜03:34 に実行**した結果。

**リポジトリと公開状態（03:31時点の観測）**

| 項目 | 値 |
|---|---|
| ローカルHEAD | `d4cc0b1` |
| 作業ツリー | クリーン（未コミットの変更なし） |
| Pages API | `status=built` / `commit=d4cc0b1` / `updated=2026-08-19T18:08:51Z` |
| 公開URL | HTTP 200 |
| 配信中の `sw.js` | `CACHE_VERSION = "v8"` |

**指示の前提を実測した結果（03:33〜03:34時点）**

| 指示 | 実測結果 | 判断 |
|---|---|---|
| 「ローテーションの2人目の表示が隠れる」 | **375x667 / 375x812 / 390x844 の3サイズすべてで `#panelB` `#scoreB` は画面内。再現しなかった** | **未再現。原因不明のまま残す**（下記6参照） |
| 「スコア表のテキストが重なって見づらい」 | JPAのスコアシートのマスは **22x20px・文字10px**。可読性の下限（16px）を大きく下回る | **再現条件と一致。マスの作りが原因** |
| 「スコアの数字がカードのだいぶ下」 | `#scoreA` はパネル上端から **18%** の位置。ただし縦方向の中央寄せで、上に名前とハンデ表示が乗る | **表示位置は数値で確認済み** |
| 「ルール説明がJPAの下に来る」 | `#gameNote` は `#gameGroups` の**後ろ**にあるため、JPAカテゴリの下に出る（`index.html:48`） | **再現。DOMの順序が原因** |
| 「ターン交代の点線が見づらい」 | `.score-panel.is-turn` は `outline: 3px dashed var(--ink)` （`style.css:1687`）。地の色と同系の黒 | **再現。色が地と近い** |
| 「標準ボールにパラジウムと付ける」 | `data/balls_data.js:21` は `label: "標準"` | **現状を確認** |
| 「ローテーションは61点ではなく複数の先取点」 | `data/games_data.js:92` が `goalDefault: 61` / `goalHidden: true` | **現状を確認** |
| 「カイルンの追加」 | `data/games_data.js:124` に定義あり。**`ui_setup.js` の GAME_GROUPS には無くUIに出ていない** | **データはある。UIが未実装** |

---

### 4. 一次情報の取り直しコマンド

再開時はまずこれを実行すること。

```bash
cd D:\Claudecode\pool-score

# 検証をまとめて実行（9本・537項目）
_test\run_all.bat

# 公開URLの実物（上とは別。必ず実行する）
python _test\live_test.py

# 公開状態
gh api repos/hiroshit164820-create/pool-score/pages/builds/latest --jq "{status:.status, commit:(.commit[0:7])}"

# 配信されている中身が新しいか（キャッシュに騙されないため）
curl -s "https://hiroshit164820-create.github.io/pool-score/sw.js" | grep "CACHE_VERSION = "

git log --oneline -3 && git status --short
```

**注意**: 「テストが通った」「Pagesがbuilt」「配信中の中身が新しい」はすべて別の事実。3段とも通すこと。

---

### 5. 未着手の範囲

**このセッションの指示16件はすべて未着手。** 1件も実装していない。

**前セッションから引き継いでいる未実装**
- **カイルン** — `data/` に定義はあるが**UIに出ていない**（今回の指示にも含まれる）
- **JCL 9ボール／8ボール** — 目標点は手入力の方針。**ファーゴレート換算式は非公開なので実装しない**
- イニング単位の詳細入力モード（JPAスコアシートの**表示は作ったが入力UIは無い**）
- JPAのチームポイント表示（`buildResult` で算出はしているが画面に出していない）
- Phase2のクラウド共有

**やったように見えるが未検証のもの（前セッションから継続）**
- **実機のスマホでの動作確認は一度もしていない**（Playwrightのみ）
- **実際のビリヤード台の脇で使っていない**
- ダブルタップ拡大の抑止は**実機のiOS Safariでしか本当の挙動が分からない**

---

### 6. 不確実な点・リスク

**「ローテーションの2人目が隠れる」が再現しなかった（最重要）**

03:34時点で 375x667 / 375x812 / 390x844 の3サイズを実測したが、
`#panelB` も `#scoreB` も画面内に収まっていた。考えられる可能性:
- 本人の端末がこれより縦の短い環境（横向き、ブラウザのURLバーが出た状態など）
- ボールセット選択やハンデ設定など、**別の状態と組み合わさったときだけ起きる**
- 前セッションで `body.match-active` の高さ固定を入れる**前**の画面を見ていた
  （その修正は `402483d` に含まれる。本人が古いキャッシュを見ていた可能性）

→ **推測で直さないこと。** 着手前に本人へ「どの端末・どの向き・どの種目設定で起きたか」を確認するか、
　 実機のスクリーンショットをもらうこと。**直したつもりで直っていない**のが最悪の結果になる。

**時間経過で腐る観測**
- 上記の「built」「v8」「HTTP 200」はすべて**03:31時点**のもの。再開時は取り直すこと

**推測で進めた箇所（前セッションから継続）**
- JPA 8ボールの対戦表はAPA公式のもの（JPA公式資料での直接確認は取れていない）
- カイルンの未確認事項2点（1イニング内の連続得点可否／ミス時のステップリセット）
- **ボールの色は商品画像からの目視**。16進コードはどのメーカーも非公開
- **チタニウムの配色**は公式ページの説明文が別製品(Unity)と混ざっているため画像のみが根拠

**本人確認を取っていない判断（前セッションから継続）**
- 8bitデザインで**任天堂の意匠は使っていない**（著作権・商標のため様式のみ）

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v8`）→ ファイルを変えたら**必ず上げる**
- **`data/` 配下の数値** → 一次情報で確認済み。変更前に `04_種目ルール仕様.md` の出典を見る
- **マスワリ・ブレイクエースのボタン**
  → **機能している**。前セッションで本人の「機能していない」が誤認だったことを実測で確認済み。
  消すと成績のマスワリ率が0になる。**今回の指示9はこのボタンをスコアボード内へ移す話であって、削除ではない**
- **`js/engine.js` の `makeScorer`**
  → 「相手だけにハンデがある場合はキーボールのみ得点」の分岐を外すと不具合が戻る
- **`js/ui_match.js` の `displayScore()`**
  → ボウラードだけボーナス込みの集計を返す。`st.score` を直接出すと落球数になる
- **`renderGoalArea` → `renderBallHandicap` → `renderBallSet` の呼び出し順**
  → 逆向きに呼ぶと相互再帰になる
- **`body.match-active` の高さ固定**
  → 外すと帯が増えたときに下部ボタンが画面外に出る
- **`.app::before` / `.app::after` の `pointer-events: none`**
  → 外すと地面の装飾が下部ボタンを覆い「見えているのに押せない」壊れ方をする
- **`user-select: none` の除外リスト**（`input, textarea, .ev-desc, .mc-main, .stat-val`）
- **`touch-action: manipulation`** → 外すと連打で画面が拡大する
- **`style.css` の可読性の制約**（タップ56px／本文16px／コントラスト4.5:1・数字3:1）
  → `redesign_test.py` が自動検査している
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者** → noreply アドレス（`304265170+hiroshit164820-create@users.noreply.github.com`）

---

### 8. 最後の判断・関門

**このセッションで下した判断**
1. **実装に着手しなかった** — 指示が「次のセッションで」「スライスして進めて」だったため、
   引き継ぎの作成までで停止した
2. **「2人目が隠れる」を再現できないまま残した** — 推測で直すと直ったかどうか判定できなくなるため。
   本人への確認事項として明記した

**本人確認待ちの事項**
- **「ローテーションの2人目の表示が隠れる」の再現条件**（端末・向き・種目設定）
- **指示16の「テーブル上の配置をデバイス上で再現」の具体像**（下記9-16参照）

---

### 9. 次セッションの作業指示（本人から・2026-08-20受領）

本人の指示は**「スライスして進めてください」**。
1件ずつ「実装 → 検証 → 停止」の単位で回すこと。まとめて着手しない。

#### A. 表示の修正（比較的小さい・先に片付けやすい）

| # | 指示 | 補足・調査済みの事実 |
|---|---|---|
| 1 | **ダブルスで誰を選んだか分かるようにする。チームAは赤で塗りつぶし白文字、チームBは青で塗りつぶし白文字** | 現在は選択済みの人が視覚的に区別されない。**注意: 現在のテーマは `--side-a` が青、`--side-b` が赤（左が青）。本人の指示はA=赤・B=青で逆になっている。** 指示どおりに変えると既存のスコアパネルの色と食い違うため、**着手前に本人へ確認するか、スコアパネル側も揃えるかを決めること** |
| 2 | **スコア表のテキストが重なって見づらい** | 実測: JPAスコアシートのマスは**22x20px・文字10px**。可読性の下限16pxを下回る。マスを大きくするか、連番の全表示をやめて別の見せ方にする |
| 3 | **スコアの数字がカードのだいぶ下にある** | 実測: パネル上端から**18%**の位置。上に名前・ハンデ表示が乗るため下がっている |
| 4 | **ターン交代を表す黒い点線が見づらい。周囲と色の差があるものに** | 現在 `style.css:1687` で `outline: 3px dashed var(--ink)`（黒）。地の色と近い |
| 5 | **ローテーションのスコアボードで2人目の表示が隠れる** | **⚠ 3サイズで再現しなかった（上記6参照）。本人に再現条件を確認してから着手する** |
| 6 | **競技種目のルール説明が「JPA」の下に来るのは変** | 原因判明: `#gameNote`（`index.html:48`）が `#gameGroups` の後ろにある。選択中の種目の近くに移すか、種目行の中に出す |
| 7 | **ローテーションの「標準」ボールに「パラジウム」と付ける** | 現在 `data/balls_data.js:21` が `label: "標準"`。**注意: パラジウム(Palladium)は前セッションで調査済みで、配色は通常と同じだが数字枠が白い三角。「標準＝パラジウム」と名付けてよいかは本人の意図を確認したほうがよい** |

#### B. 操作の変更（中くらい）

| # | 指示 | 補足 |
|---|---|---|
| 8 | **プレーヤー選択で1人目を選んだら、その人を2人目の候補から除外する** | `playerPicker()` に除外リストを渡す形になる |
| 9 | **スコアボード内にマスワリとブレイクエースのボタンを置き、押すとスコアが増えるようにする** | **⚠ 現在は「予約してから次のラックで効く」方式。「押すとスコアが増える」に変えると、マスワリ＝ラック取得と同時記録になる。** 現行の `flags` 方式を「マスワリを押す＝そのラックをその人が取った＋マスワリ」に変える設計になるので、engine のイベント設計まで確認してから着手すること |
| 10 | **ローテーションは61点先取ではなく、120点・180点・240点・300点先取も選べるようにする** | 現在 `goalHidden: true` / `goalDefault: 61`。**前セッションで「先取点の入力を出さない」という指示を受けて隠したので、今回はそれを戻す方向の変更**。61点も残すか置き換えるかを確認すること。JAPA公式はA級180点/B級120点（`04_種目ルール仕様.md` 参照） |
| 11 | **各試合記録にメモ機能を追加** | `match` にメモ欄を持たせ、履歴と試合中の両方から書けるようにする |

#### C. 画面構造の変更（大きい・独立したスライスにする）

| # | 指示 | 補足 |
|---|---|---|
| 12 | **左下の固定戻るボタンをやめ、下に固定タブバーを付ける。その中に戻るボタンを入れる** | 現在は `#globalBackBtn`（`index.html:360`）。**下部には既に `.bottom-bar` があるため、タブバーとの共存・すみ分けを設計してから着手すること** |
| 13 | **タブバーの内容: トップページ（個人ダッシュボード表示）／種目選択／選手登録・選手一覧（うまくまとめて）／成績一覧／試合履歴** | **「トップページ（個人ダッシュボード）」は新規画面。** 何を出すかは未定義なので本人に確認するか、勝率・直近の試合・中断中の試合あたりで提案する |
| 14 | **競技種目にカイルンを追加** | `data/games_data.js:124` に定義済みだが `ui_setup.js` の `GAME_GROUPS` に無い。**未確認事項2点（1イニング内の連続得点可否／ミス時のステップリセット）は一次情報が無いため、ハウス設定の選択式にする方針**（前セッションからの継続判断） |
| 15 | **選手登録と選手一覧をうまくまとめる**（13に含まれる） | 現在は同一画面（`#screenPlayers`）で登録フォームが折りたたみ。タブ構成に合わせて整理する |
| 16 | **テーブル上の配置をデバイス上で再現できるようにしたい** | **⚠ 最も大きく、仕様が未定義。** 台の俯瞰図に球を配置する機能と思われるが、目的（セーフティの記録？練習配置の保存？）が不明。**着手前に必ず本人へ用途を確認すること。** 独立した大きなスライスとして最後に回すのが安全 |

#### 着手順の提案（本人の合意は取っていない）

1. **A群の1〜4, 6, 7**（表示の修正。影響範囲が小さく検証しやすい）
2. **B群の8, 11**（操作の追加。既存の設計を壊さない）
3. **10**（先取点。前セッションの指示と逆方向なので確認してから）
4. **9**（マスワリの記録方式変更。engine まで波及する可能性）
5. **14**（カイルン。ハウス設定の設計が要る）
6. **12, 13, 15**（タブバーとダッシュボード。画面構造の作り替え）
7. **16**（テーブル配置。仕様確認から）
8. **5**（再現しないので、情報が来てから）

---

### 次の安全なアクション（1つだけ）

**`_test\run_all.bat` を実行し、537項目が通ることを確認して基準線を取る。**

前セッションの終了時点では全項目成功していたが、それは**03:06時点の観測**であり、
このセッションでは再実行していない。指示16件の実装に入る前に、
「元から壊れていたのか、自分が壊したのか」を切り分けられる状態を作ること。

### 再開者が仮定してはいけないこと

- **「本人が『こう見える』と言った＝そのとおりに壊れている」と仮定しない。**
  前セッションでマスワリのボタンが「機能していない」という指摘が**誤認だった**実例がある。
  今回も「2人目が隠れる」が**3サイズで再現しなかった**。**まず実物で再現を確認する**
- **指示1の色（A=赤・B=青）を、現在の配色と整合すると仮定しない。**
  現在は逆（A=青・B=赤）。そのまま変えると既存表示と食い違う
- **指示9を「ボタンを移動するだけ」と仮定しない。**
  「押すとスコアが増える」は記録方式そのものの変更で、engine のイベント設計に関わる
- **指示10を「単なる追加」と仮定しない。**
  前セッションで本人が「先取点を出すな」と指示した箇所を戻す変更になる
- **指示16の仕様を推測で決めない。** 用途が不明なまま作ると作り直しになる
- **「テストが通っている＝スマホで動く」と仮定しない。** 実機確認は一度もしていない
- **「Pagesがbuilt＝新しい中身が配信されている」と仮定しない。** 4.のコマンドで実物を見る

---
---

## 過去の引き継ぎ

## 2026-08-20（03:10時点）

### 1. 現在の目標

ビリヤードのスコア記録アプリ。公開済み: https://hiroshit164820-create.github.io/pool-score/

このセッションでは本人の指示13件を実装した。
指示は作業中に順次追加されたため、番号は受け取った順。

1. よく使う種目の欄を出さない
2. ダブルスは1人目を入れてから2人目の選択を出す
3. ローテーションでは「何点先取か」を出さない
4. 14-1にイニングの概念を入れる
5. マスワリ・ブレイクエースのボタンが機能していないので削除 → **調査の結果、機能していた**
6. JPAダブルスの専用得点表に沿ったスコア表
7. ボウラードの追加
8. セットマッチで次のラックへの移行とオルタネートのブレイク交代を表示
9. 交代ボタンが効いているか分からない／JPAでいまどちらの番か分かるように
10. 素早くタップすると画面が拡大される
11. 試合を途中保存して続きから再開
12. ローテーションの番号をボールの色に対応（3種のセットから選択）
13. 前の画面に戻るボタンを常時固定表示

**次のセッションの目標**: 本人からの新しい指示待ち。

---

### 2. 本人の指示と実際の差分（重要）

#### 「マスワリ・ブレイクエースのボタンは機能していない」→ **機能していた**

実際に操作して確認した（2026-08-20 01:52）。押すと `aria-pressed=true` になり、
その後ラックを取ると記録に `masuwari: true` が保存されていた。
プレーヤー別成績の「マスワリ ◯回（◯%）」「ブレイクエース ◯回」の集計元。

**削除すると成績のマスワリ率が永久に0になる**ため、本人に確認したうえで削除せず、
「押した瞬間には何も起きず、次にラックを取ったときに効く」という**順序が伝わる表示**に直した。
- 押すと「✓ マスワリ」に変わる
- 「マスワリ を付けて記録します。スコアをタップしてください。」と文章で出す
- 押した時点でトーストも出す

#### 「プラチナム」の正体

本人の最初の指示は「チタニウム」、その後「プラチナムでした」と訂正。
**Dynaspheres公式サイトを調べたところ、公式の "Platinum" はキャロム用の3球セット
（Platinum 615）で番号球が無い**。この時点で本人に確認したところ、
国内販売店のページ（キューショップジャパン）を教えてもらい、
**「Dynaspheres Platinum プラチナム（的球15＋手球2）」という15球セットが実在**することが判明。
JAPA公認品。メーカー公式サイトには載っていない。

商品画像から配色を読み取り、**本人の申告どおり7番がターコイズ（薄い水色）**、
さらに6番がグレーであることを確認した。

---

### 3. 変更点

コミット `402483d`

#### 設定画面
- **「よく使う種目」の欄を削除**
- **ダブルスは1人目を入れてから2人目の欄を出す**。
  1人目に入力するか呼び出しボタンを押すと自動で開く／「＋ 2人目を選ぶ」でも開く
- **ローテーションは先取点の入力を出さない**（`games_data.js` の `goalHidden`）。
  **61点で決着**する。1ラック120点の過半数を取れば相手が追いつけなくなるという算術で、
  「61点先取」という制度が公式規程にあるという意味ではない（前セッションで確認済み）
- **ボウラードを追加**（1人用・10フレーム・1フレーム2投・最高300点）。
  相手の入力欄も先取点の入力も出さない

#### 試合画面
- **14-1にイニング表示**（`games_data.js` の `showInnings`）。
  数え方はJPAと同じで engine が全種目で数えている
- **得点後に次のラックへの移行を表示**。オルタネートではブレイク権の交代を
  トースト＋バナーの点滅で明示
- **「いまの番」バナー**を追加。撞いている側のパネルにも破線の枠を付ける
- **ダブルタップ拡大を抑止**（`touch-action: manipulation` ＋ viewport の `user-scalable=no`）
- **試合画面を1画面に収めた**。帯が増えて下部ボタンが画面外に出ていたのを、
  `body.match-active` で高さを固定して解決

#### 中断と再開
- **記録は元から操作のたびに保存されていた**（機能自体は前からあった）。
  ただし履歴画面まで見に行かないと気づけなかったため、
  **設定画面のいちばん上に「続きから再開する」カード**を出すようにした。
  種目・両者の名前・そこまでのスコア・ラック数・いつのものかを表示
- 中断すると**設定画面に戻す**（履歴を経由しない）

#### 戻るボタン
- **どの画面からでも1つ前に戻れるボタンを左下に固定表示**。
  画面遷移の履歴を `screenStack` に積んで戻る
- **試合中は出さない**（記録中に誤って抜けるのを防ぐ。専用の「中断」がある）

#### スコアシート（`js/ui_sheet.js` を新設）
- **JPA公式スコアシートの記入方式に沿った表示**。
  1点=1マスの連番を消していく形で、ラックの区切りに×印。
  出典: http://www.poolplayers.jp/rule/9ball-scorekeeping.pdf （2026-08-20に本文抽出）
  詳細は `04_種目ルール仕様.md` 8.5節
- **ボウラードのスコア表**。ストライク/スペアのボーナス計算付き

#### ボールセット（`data/balls_data.js` を新設）
- ローテーションの盤面を**実物のボールの色**で描く
- **標準／ダイナスフィア プラチナム／ダイナスフィア チタニウム／アラミス ブラック**の4種
- **数字枠の意匠も再現**（プラチナム=白い三角、チタニウム=白い菱形）
- 選んだセットは記憶され、試合の記録にも残る
- 配色の出典は `04_種目ルール仕様.md` 8.6節

---

### 4. 検証済みの証拠

すべて **2026-08-20 02:55〜03:06 に実行**した結果。

| 検証 | 結果 |
|---|---|
| `node _test/engine.test.js` | **119項目 成功/0失敗**（ボウラードのスコア計算24項目を追加） |
| `node _test/shotclock.test.js` | **60項目 成功/0失敗** |
| `node _test/chessclock.test.js` | **35項目 成功/0失敗** |
| `python _test/browser_test.py` | **78項目 成功/0失敗** |
| `python _test/features_test.py` | **42項目 成功/0失敗** |
| `python _test/redesign_test.py` | **59項目 成功/0失敗** |
| `python _test/roster_handicap_test.py` | **58項目 成功/0失敗** |
| `python _test/rotation_goal_test.py` | **43項目 成功/0失敗** |
| `python _test/session_test.py` | **43項目 成功/0失敗**（新規） |
| **ローカル合計** | **537項目 成功/0失敗** |
| `python _test/live_test.py` | **19項目 成功/0失敗**（公開URLの実物） |

**ボウラードのスコア計算は数値で検証済み**
- オールストライク（12投）= **300点**
- オールスペア(5-5)＋最後に5 = **150点**
- 9本+ミスを10回 = **90点**
- ガター10回 = **0点**
- ストライクのボーナス: 10+3+4 = 17点

**公開状態（03:06時点の観測）**
- Pages API: `status=built` / `commit=402483d`
- **配信されている実物を確認済み**: `sw.js` が `v8` ／ `balls_data.js` と `ui_sheet.js` が
  HTTP 200 ／ `games_data.js` に `bowlard` あり
- git: 作業ツリーはクリーン。ローカルHEADとorigin/mainは共に `402483d`

---

### 5. 一次情報の取り直しコマンド

```bash
cd D:\Claudecode\pool-score

# 検証をまとめて実行（9本・537項目）
_test\run_all.bat

# 公開URLの実物（上とは別。必ず実行する）
python _test\live_test.py

# 公開状態
gh api repos/hiroshit164820-create/pool-score/pages/builds/latest --jq "{status:.status, commit:(.commit[0:7])}"

# 配信されている中身が新しいか（キャッシュに騙されないため）
curl -s "https://hiroshit164820-create.github.io/pool-score/sw.js" | grep "CACHE_VERSION = "

git log --oneline -3 && git status --short
```

---

### 6. 未着手の範囲

**実装していない競技種目**
- **カイルン** — 3ステップUIが未実装。未確認事項2点は一次情報が無く、
  ハウス設定の選択式にする方針
- **JCL 9ボール／8ボール** — 目標点は手入力にする方針。
  **ファーゴレート換算式は非公開なので実装しない**

**実装していない機能**
- イニング単位の詳細入力モード（JPA公式スコアシート相当の入力）。
  **表示は作ったが、イニングごとの入力UIは作っていない**
- JPAのチームポイント表示（`buildResult` で算出はしているが画面に出していない）
- Phase2のクラウド共有

**今回の変更で残した論点**
- **JPAダブルスの専用スコアシートは、シングルスと同じ構造**で作った。
  公式シートを確認したところ、ダブルスも記入方式は同じで名前欄が2人になるだけ。
  ペアのスキルレベルは2人の合計（`04_種目ルール仕様.md` 8節）
- **ボウラードのスコア表は入力と表示のみ**で、履歴一覧での見え方は調整していない
- ボールセットの色は**近似値**。16進コードはどのメーカーも非公開
- **チタニウムの配色は公式ページの説明文が別製品(Unity)と混ざっている**ため、
  画像のみを根拠にしている。実物と違う可能性がある

**やったように見えるが未検証のもの**
- **実機のスマホでの動作確認は一度もしていない**（Playwrightのみ）。
  **特にダブルタップ拡大の抑止は、実機のiOS Safariでしか本当の挙動が分からない**
- **実際のビリヤード台の脇で使っていない**

---

### 7. 不確実な点・リスク

**時間経過で腐る観測**: 上記の「built」「v8」は**03:06時点**のもの。取り直すこと

**推測で進めた箇所**
- JPA 8ボールの対戦表はAPA公式のもの（JPA公式資料での直接確認は取れていない）
- カイルンの未確認事項2点
- **ボールの色は商品画像からの目視**。実物と完全一致はしない

**本人確認を取っていない判断（前セッションから継続）**
- 8bitデザインで**任天堂の意匠は使っていない**（著作権・商標のため様式のみ）

---

### 8. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v8`）→ ファイルを変えたら必ず上げる
- **`data/` 配下の数値** → 一次情報で確認済み。変更前に `04_種目ルール仕様.md` の出典を見る
- **マスワリ・ブレイクエースのボタン**
  → **機能している**。消すと成績のマスワリ率が0になる（本人の「機能していない」は誤認）
- **`js/engine.js` の `makeScorer`**
  → 「相手だけにハンデがある場合はキーボールのみ得点」の分岐を外すと不具合が戻る
- **`js/ui_match.js` の `displayScore()`**
  → ボウラードだけボーナス込みの集計を返す。`st.score` を直接出すと落球数になる
- **`renderGoalArea` → `renderBallHandicap` → `renderBallSet` の呼び出し順**
  → 逆向きに呼ぶと相互再帰になる
- **`body.match-active` の高さ固定**
  → 外すと帯が増えたときに下部ボタンが画面外に出る
- **`.app::before` / `.app::after` の `pointer-events: none`**
- **`user-select: none` の除外リスト**
- **`touch-action: manipulation`** → 外すと連打で画面が拡大する
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者** → noreply アドレスを使う

---

### 9. 最後の判断・関門

**このセッションで下した主な判断**
1. **マスワリのボタンを削除しなかった** — 実際には機能しており、
   消すと成績が壊れるため。本人に確認して「分かりやすく直す」を選んでもらった
2. **ローテーションの決着点を61点にした** — 「先取の概念がない」という指示に対し、
   1ラック120点の過半数という算術で決着させた。公式規程の制度ではない旨をコメントに明記
3. **プラチナムの配色は国内販売店の画像を根拠にした** — メーカー公式に載っていないため。
   本人の申告（7番が水色）と一致することを確認済み
4. **戻るボタンは試合中に出さない** — 記録中に誤って抜けるのを防ぐため
5. **ボウラードは1人用として画面を組み替えた** — 相手側のスコア欄・ブレイク・交代を隠す

**本人確認待ちの事項**: なし

---

### 次の安全なアクション（1つだけ）

**本人がスマホの実機で公開URLを開き、今回の13件を確認する。**

https://hiroshit164820-create.github.io/pool-score/

特に見てほしい点。
1. **素早く連打しても画面が拡大しないか**（実機のSafariでしか確認できない）
2. **ローテーションのボール色が手元の実物と合っているか**（プラチナムを選ぶ）
3. **ボウラードのスコア表が使えるか**
4. **中断→再開が期待どおりか**

※ホーム画面に追加済みなら `CACHE_VERSION` を v8 に上げてあるので開き直せば入れ替わる。

### 再開者が仮定してはいけないこと

- **「本人が『機能していない』と言った＝壊れている」と仮定しない。**
  今回マスワリのボタンは実際には動いていた。**まず実物で確認する**
- **「テストが通っている＝スマホで動く」と仮定しない**
- **「Pagesがbuilt＝新しい中身が配信されている」と仮定しない**
- **ボールの色を「公式の値」と仮定しない。** 画像からの目視による近似値
- **ローテーションの61点を「公式規程の先取点」と仮定しない。** 120点の過半数という算術
- **`#goalSame` `#goalA` `#goalB` があると仮定しない。** 廃止済み

---
---


## 2026-08-20（00:40時点）

### 1. 現在の目標

ビリヤードのスコア記録アプリ。Phase 1.0 は 2026-08-19 に完成・公開済み。

このセッションでは、本人の指示により
**アイコン・ボタン・サイトデザイン全体を8bitプラットフォーマー風（スーパーマリオの世界観）に作り直し**、
あわせて操作性の改善6件を実装した。

**次のセッションの目標**: 本人からの新しい指示待ち。
未着手として残っているものは「5. 未着手の範囲」を参照。

---

### 2. 変更点（実際に変更したもの）

コミット `26aec84`「デザインを8bitプラットフォーマー風に刷新し、操作性を6点改善」

#### デザイン（本人指示の主題）

| 対象 | 変更内容 |
|---|---|
| `style.css` | `:root` の配色トークンを空色・レンガ・緑の地面・金ブロックに置換。末尾に「8bitプラットフォーマー層」を追加（約340行） |
| 背景 | `.app::before` に雲、`.app::after` に地面の帯を CSS のみで描画（画像を使わない） |
| ヘッダ | レンガ模様の帯 |
| ボタン | ハテナブロック風（上辺ハイライト＋下辺影）。決定ボタンは金色 |
| 見出し | ハテナブロックの看板（四隅に鋲） |
| アイコン4点 | `_test/make_icons.py` を書き直し、16x16のドット絵（9番ボール）で再生成 |
| `manifest.json` | `theme_color` を `#c84c0c`、`background_color` を `#5c94fc` に |
| `index.html` | `<meta name="theme-color">` を `#c84c0c` に |
| `sw.js` | `CACHE_VERSION` を **v3 → v4** に更新 |

**意匠について**: 既存ゲームの画像・ロゴ・スプライト・書体・効果音は一切使っていない。
すべて CSS の図形と、16x16のドットを矩形で置いた独自の図案。
公開リポジトリに置いても問題ない構成にしてある。

**書体の方針**: 本文は丸ゴシック（M PLUS Rounded 1c）のまま。
ピクセル書体（DotGothic16）は**見出しと数字だけ**に留めた。
日本語の本文をドット書体にすると、小さい画面で判読できなくなるため。

#### 機能（本人指示の追加6件）

| # | 指示 | 実装 |
|---|---|---|
| 1 | プレーヤー入力で登録済みプレーヤーを呼び出せるように | 「登録した人から選ぶ」ボタン列を追加。JPA種目では**スキルレベルも一緒に反映**し、勝利条件が自動で決まる |
| 2 | 勝利条件のフリガナ削除 | `data/games_data.js` の「5先（ゴサキ）」「7先（ナナサキ）」を「5先」「7先」に |
| 3 | プレーヤー登録でJPAスキルレベルも登録 | `store.js` に `skill: {nine, eight}` を追加。9ボール(SL1-9)と8ボール(SL2-7)を別々に保持。登録時と一覧の「スキルレベル」ボタンから設定・変更・未設定に戻すことができる |
| 4 | ショットクロックの自動延長は1ラック1回（既定） | `shotclock.js` に `scope` を追加。既定 `"rack"` で `countPerRack: 1`。ラックが変わると `resetRack()` で戻る。従来の試合通算は `scope: "match"` で選べる |
| 5 | ブレイク権をもっとはっきり | **3か所**で示すようにした：①パネル内の `BREAK` バッジ ②パネル自体を金色に強調（`.has-break`）③スコア下の大きなバナー（名前を19pxで表示） |
| 6 | 競技選択をもっとスマートに | カテゴリ折りたたみ（一般／JPA）＋ダブルスを行内の切替スイッチ化＋「よく使う種目」（選択回数の多い順に最大3件）の3段構え。9件の平坦な一覧が、初期表示4行になった |

#### 不具合修正（作業中に発見）

- **通知（トースト）が積み上がって操作を覆っていた**
  `UI.toast()` が古い通知を消さずに追加していたため、連続操作すると3件以上が重なり、
  ショットクロックと下部の操作ボタンを完全に隠していた。
  常に最新1件だけを**画面上部**に出すよう修正（下部には操作ボタンが並ぶため）。
  この問題はデザイン変更前から存在していた。

---

### 3. 検証済みの証拠

すべて **2026-08-20 00:35〜00:40 に実行**した結果。

| 検証 | 結果 | 内容 |
|---|---|---|
| `node _test/engine.test.js` | **87項目 成功/0失敗** | スコア計算（変更なし・回帰確認） |
| `node _test/shotclock.test.js` | **60項目 成功/0失敗** | 前回39項目＋**延長のラック単位を21項目追加** |
| `node _test/chessclock.test.js` | **35項目 成功/0失敗** | チェスクロック（変更なし・回帰確認） |
| `python _test/browser_test.py` | **76項目 成功/0失敗** | 前回72項目＋ブレイク表示の検証を追加 |
| `python _test/features_test.py` | **42項目 成功/0失敗** | JPA・プレーヤー・成績・チェスクロック |
| `python _test/redesign_test.py` | **57項目 成功/0失敗** | **新規**。今回の変更6件＋可読性の制約を検証 |
| **ローカル合計** | **357項目 成功/0失敗** | |
| `python _test/live_test.py` | **19項目 成功/0失敗** | **公開URLの実物**をiPhone相当で確認 |

**公開状態（00:39時点の観測）**
- Pages API: `status=built` / `commit=26aec84`
- 公開URL: HTTP 200、`style.css` 200、`icon-192.png` 200
- **公開されている実物の中身を確認済み**:
  - `style.css` に「8bitプラットフォーマー層」が含まれる
  - `sw.js` の `CACHE_VERSION` が `v4`
  - `manifest.json` の色が `#5c94fc` / `#c84c0c`
- git: 作業ツリーはクリーン。ローカルHEADとorigin/mainは共に `26aec84`

**redesign_test.py が守っている制約**（デザインを触るたびに自動で確認される）
- タップ領域44px以上／本文16px以上
- スコアの数字のコントラスト比3:1以上（左右とも実測）
- 本文がドット書体になっていないこと
- 通知が下部の操作ボタンを覆っていないこと
- **地面の装飾が下部ボタンを覆っていないこと**（`elementFromPoint` で実判定し、実際に押して効くことまで確認）

---

### 4. 一次情報の取り直しコマンド

再開時はまずこれを実行すること。

```bash
cd D:\Claudecode\pool-score

# 検証をまとめて実行（6本・357項目）
_test\run_all.bat

# 公開URLの実物が動いているか（上とは別。必ず実行すること）
python _test\live_test.py

# 公開状態の確認
gh api repos/hiroshit164820-create/pool-score/pages/builds/latest --jq "{status:.status, commit:(.commit[0:7])}"
curl -s -o /dev/null -w "%{http_code}\n" -L "https://hiroshit164820-create.github.io/pool-score/"

# 公開されている実物が新しいか（キャッシュに騙されないため）
curl -s "https://hiroshit164820-create.github.io/pool-score/sw.js" | grep "CACHE_VERSION = "

# gitの同期状態
git log --oneline -3 && git status --short
```

**注意**: 「テストが通った」ことと「公開URLで動いている」ことは別。
さらに「Pages が built」と「配信されている中身が新しい」も別。
上の3段（テスト → live_test → 実物のgrep）をすべて通すこと。

---

### 5. 未着手の範囲

**実装していない競技種目**（データ定義とエンジンは実装済み・テスト済みだが、**UIに出していない**）
- ローテーション（`rotation`）— ボールグリッド画面が未実装
- カイルン（`kailun`）— 3ステップUIが未実装。未確認事項2点はハウス設定の選択式にする方針
- JCL 9ボール／8ボール（`jcl_9ball` `jcl_8ball`）— 目標点は手入力にする方針

**実装していない機能**
- イニング単位の詳細入力モード（JPA公式スコアシート相当）
- ボールハンデのUI（**エンジンは対応済み・テスト済み**、UIだけ未実装）
- JPAのチームポイント表示（`buildResult` で算出はしているが画面に出していない）
- Phase2のクラウド共有

**今回の変更で残した論点**
- **ダブルスのスキルレベル自動反映は、両方の欄に登録済みの人が入ったときだけ効く。**
  片方だけ選んだ時点では上書きしない（合計が確定しないため）。手で選び直すことは可能。
- **「よく使う種目」は試合を開始した時点で回数が増える。** 種目を選んだだけでは増えない。

**やったように見えるが未検証のもの**
- **実機のスマホでの動作確認は一度もしていない。**
  確認したのは Playwright の Chromium（iPhone相当のUA・画面サイズ）のみ。
  **特に今回はドット書体（DotGothic16）を導入したため、実機のSafariでの表示は未確認。**
  Webフォントが落ちた場合は丸ゴシックにフォールバックする作りにしてあるが、実物では見ていない。
- **実際のビリヤード台の脇で使っていない。** 片手で押せるか、立ったまま数字が読めるかは実地で未検証。
  **8bit配色は前回の明るいクリーム色より背景が濃いため、店内の照明下での見え方は要確認。**

---

### 6. 不確実な点・リスク

**前回記録された「テストが稀に落ちる」現象**
- 前回（08-19 23:44）に browser_test と features_test が同時に1回だけ落ちた記録がある
- **今回のセッションでは一度も再現しなかった**（各テストを複数回実行している）
- 引き続き「1回の失敗で機能が壊れたと判断せず、まず再実行する」方針を維持する

**時間経過で腐る可能性のある観測**
- 上記の「HTTP 200」「status=built」「CACHE_VERSION=v4」はすべて**00:39時点**のもの。再開時は取り直すこと

**推測で進めた箇所（前回から継続）**
- **JPA 8ボールの対戦表はAPA（米国本部）公式のもの**。JPA公式資料での直接確認は取れていない。
  `data/handicap_data.js` に注記あり
- カイルンの「1イニング内の連続得点可否」「ミス時のステップリセット」は一次情報が無い

**今回のデザイン変更で判断したこと（本人確認を取っていない）**
- **マリオのキャラクター・ロゴ・効果音・スプライトは使っていない。**
  それらは任天堂の著作物・商標であり、公開リポジトリに置けないため。
  「1980年代の横スクロールアクションの様式」というデザイン言語だけを使い、
  図案はすべて CSS の図形とドット絵で独自に起こした。
  **この判断は本人に事前確認していない**（着手前の確認質問が本人の指示より先に流れたため）。
  もし実際のキャラクター等を想定していた場合は、その旨を確認すること。

---

### 7. 触ってはいけない領域

- **`sw.js` の `CACHE_VERSION`**（現在 `v4`）
  → ファイルを変更したら**必ず数字を上げる**。上げないとスマホに古い画面が残る
- **`data/` 配下の数値**（`handicap_data.js` のJPA表など）
  → 一次情報で確認した値。変更するなら `04_種目ルール仕様.md` の出典を確認してから
- **`js/engine.js`**
  → 種目名の分岐を書かない設計。`engine.test.js` の最後で自動チェックしている
- **`style.css` の可読性の制約**
  タップ領域56px以上、本文16px以上、コントラスト比4.5:1以上（数字は3:1以上）。
  **これは `redesign_test.py` が自動で検査している。** 美観のために下げないこと
- **`.app::before` / `.app::after` の `pointer-events: none`**
  → 外すと地面の装飾が下部ボタンを覆い、「見えているのに押せない」壊れ方をする。
  `redesign_test.py` の⑨がこれを検査している
- **`UI.toast()` が1件だけ出す仕様**
  → 積み上げに戻すと時計と操作ボタンが隠れる。`redesign_test.py` の⑧が検査している
- **リポジトリの公開設定** → 現在**公開**。非公開に戻すとPagesが止まる
- **コミット作者のメールアドレス** → noreply アドレスを使う

---

### 8. 最後の判断・関門

**このセッションで下した主な判断**

1. **マリオの意匠は使わず、80年代アクションゲームの「様式」だけを使った** — 著作権・商標のため（上記6.参照）
2. **本文はドット書体にしなかった** — 日本語の本文をドット化すると小さい画面で読めなくなる。
   ピクセル書体は見出しと数字だけに留めた
3. **`countPerSide` の意味を変えなかった** — 既存の39項目のテストが「試合通算」として使っていたため、
   `scope` を新設して既定を `"rack"` にした。古い呼び出しは `"match"` と解釈される
4. **競技選択は同時に1カテゴリしか開かない** — 一覧を短く保つため。
   その結果、テストは「全部開いて数える」ができないので `helpers.py` で1つずつ開いて足し上げる
5. **通知を画面上部に移した** — 下部には「交代」「取り消し」「訂正」「試合終了」が並んでおり、
   そこに重ねると操作を塞ぐため

**本人確認待ちの事項**:
- 6.に書いた「マリオの実際の意匠を想定していたかどうか」

---

### 次の安全なアクション（1つだけ）

**本人がスマホの実機で公開URLを開き、8bitデザインが読めるか確認する。**

https://hiroshit164820-create.github.io/pool-score/

見てほしい点は3つ。
1. **ドット書体（見出しと数字）が実機で読めるか。** 潰れて読めなければ丸ゴシックに戻す
2. **空色の背景が店内の照明下で見づらくないか**
3. **ホーム画面に追加したときのアイコン**（ドット絵の9番ボール）

※すでにホーム画面に追加している場合、`CACHE_VERSION` を v4 に上げてあるので
　開き直せば新しい画面に入れ替わる。入れ替わらない場合は一度削除して追加し直す。

### 再開者が仮定してはいけないこと

- **「テストが通っている＝スマホで動く」と仮定しないこと。** 実機での確認は一度もしていない
- **「Pagesがbuiltなら新しい中身が配信されている」と仮定しないこと。**
  4.のコマンドで実物の `CACHE_VERSION` を grep して確かめる
- **「1回テストが落ちた＝壊れた」と仮定しないこと。** まず再実行する
- **「デザイン変更はCSSだけ」と仮定しないこと。**
  アイコン（`_test/make_icons.py`）、`manifest.json` の色、`index.html` の `theme-color`、
  `sw.js` の `CACHE_VERSION` がすべて連動している
- **`redesign_test.py` を「デザインのテストだから」と軽く扱わないこと。**
  可読性の制約とタップの通り道を守っている実質的な回帰テスト
